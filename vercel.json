{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/auth", "destination": "/" },
    { "source": "/auth/:path*", "destination": "/" }
  ]
}
