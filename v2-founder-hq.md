# Omni V2 — Founder HQ Board

**As of:** 2026-08-24
**Primary authority:** Nature Way product and proof gates
**Active milestone:** Restore a complete, reference-faithful Species across Buyer, Seller and Reviewer/Admin before advancing the field-pilot operations gate.

## Board

| Area | Current truth | Authority / record | Gate or status | Owner | Next action | Capacity / risk |
|---|---|---|---|---|---|---|
| Active milestone | The canonical Species remains in review, but the repair slice is deployed and bounded-proven: idle globe rotation is interruptible, zoom responds, duplicate pin painting is removed, text search settles globally, and Buyer/Seller/Reviewer/Admin have explicit contextual states. Buyer loading→ready now includes a bounded world→continent→country→region→city→results camera reveal; Seller read-only workspace, Reviewer lock and Inbox empty states are authenticated and non-mutative proven at the available Sandbox viewport. Compact public normal/reduced-motion proof, post-reveal zoom, focus-name, no-overlap and bounded user-marker path are also recorded. | `v2-species.md`, `v2-species-audit-2026-08-24.md`, `v2-tasks.md` | `partial / Canopy bounded proof recorded; parent gate open` | Manus + owner review | Run authenticated compact result-sheet/Seller/Reviewer focus-recovery proof and real permission/recovery checks. | Do not advance roles, OSM or pilot expansion while the full Species proof is open. |
| Product proof | Private claimant upload → server verification → submitted-claim proof is complete for one non-sensitive test artifact; this is bounded evidence, not marketplace or production proof. | `v2-buyer-trunk-heartwood-evidence.md`, `v2-field-pilot-registry-root.md`, `omni-v2-claim-upload-submit-observations.md` | `bounded / residual gates open` | Manus | Keep this proof stable while correcting Species. | Avoid destructive cleanup and avoid presenting the test claim as certification. |
| Buyer Species | Map-first shell, dock/sheet language and public-facility boundary remain intact. Public canonical proof covers idle globe, enabled zoom and guest Auth handoff; authenticated Buyer search now proves loading→ready for `Marche de Hanoukope` through a query-only global request, followed by a bounded camera reveal and final result framing with map/pin continuity and separated dock/sheet geometry. Post-reveal zoom, compact public normal/reduced-motion, one focus-name observation and a bounded user-marker path are proven; authenticated compact result-sheet, facility-focus/back recovery, empty/error/retry and full accessibility matrix remain open. | `v2-species.md`, `v2-species-audit-2026-08-24.md`, Buyer proof record | `verified / bounded authenticated proof; parent gate open` | Manus | Capture authenticated loading, result-ready, empty, error, facility-focus and recovery states at supported widths. | Visual fixes must not break claim/availability ownership or public trust semantics. |
| Seller Species | Seller has the shared map-mounted workspace, explicit requests/catalogue tabs, safety copy and a read-only authorized request queue. The authenticated Sandbox proof shows `Contexte vendeur autorisé`, two demo request rows and the locked handoff boundary without response/rebind controls being used. Canonical all-width, response-flow and role-bootstrap proof remain open. | `v2-species.md`, `v2-seller-reviewer-mini-species.md`, `v2-tasks.md` | `verified / read-only visual proof; broader gates open` | Manus | Prove the locked state first, then authorized read-only states only after separate role authorization. | No role bootstrap or seller mutation is implied. |
| Reviewer/Admin Species | Reviewer queue, Inbox and locked states exist; the validation-workspace summary matches the Seller sheet hierarchy. The authenticated Reviewer/Admin proof shows `Rôle reviewer non ouvert` with no decision controls or inputs, and Inbox proof shows a truthful empty state with Inbox-before-PWA boundary. No active reviewer exists in the current session. | `v2-seller-reviewer-mini-species.md`, `v2-field-pilot-registry-root.md`, `v2-tasks.md` | `verified / locked read-only proof; active role remains manual` | Manus + owner | Capture the locked state without granting roles; schedule authorized read-only proof only after manual role bootstrap. | Neon Auth `admin` must remain separate from Omni business roles. |
| Release / operations | GitHub branch `omni-v2-rebuild` is linked to Vercel; deployment `dpl_B46QuQiAxUWnqymZ5HPtdmBVNBMA` is READY for commit `bc8e730`; the Canopy browser proof ran against this READY source deployment, which reports exactly 12 Node functions and the canonical production alias. | Git history, Vercel deployment metadata, `v2-species-audit-2026-08-24.md` | `controlled / no expansion` | Manus | Use GitHub-triggered releases only; verify each canonical build after a real interaction. | Preserve exactly 12 functions and keep generated bundles out of commits. |
| Decisions | Re-enter Species before additional Root/Trunk gates; compare with `origin/main`; fix state ownership rather than layering cosmetic patches. The map/search foundation and authenticated Buyer/Seller/Reviewer visual states are bounded-proven, and Canopy now has explicit reveal, zoom, normal/reduced-motion compact public and no-overlap evidence. The full Species gate remains open for authenticated compact surfaces, complete keyboard/focus, real location/recovery and facility-focus proof. | `v2-species.md` Species gate; `v2-species-audit-2026-08-24.md`; `v2-tasks.md` | `review now` | Owner + Manus | Approve the remaining focused Canopy proof set; keep operational branches paused until it passes. | Revisit if the main-branch behavior is not the intended reference or if the maquette requires clarification. |

## Current gate

> **Species gate remains open, with the map/search, reveal and role-surface proof slice bounded-verified.** The next smallest action is authenticated compact result-sheet/Seller/Reviewer focus-recovery proof plus real location and facility-focus/back recovery checks. Do not advance operational branches until that evidence is recorded. No role assignment, OSM import, new evidence action, payment or transaction work is authorized by this board.

## Founder decision record

The owner’s latest observation is treated as a material product signal: the current surface does not yet feel like the approved Species even though parts of the underlying Trunk exist. Nature Way therefore routes this work back to Species/Canopy-quality diagnosis instead of advancing the operational field-pilot gate. The proof class is `observed / user-reported`; it must be reproduced with browser evidence before the fix is accepted.

## Capacity and release guardrail

For this work block, Species repair displaces role bootstrap, OSM importer work, PWA/Web Push and further marketplace breadth. The release guardrail is: do not call the surface complete unless the map remains stable, zoom is reachable, search has explicit loading and ready states, the grid resolves honestly, surfaces do not overlap, and Buyer/Seller/Reviewer/Admin inherit the same approved spatial language at supported widths.

## Next review

The read-only audit now includes the Canopy implementation, deployment, normal/reduced-motion compact evidence, camera reveal, request cadence, focus and bounded location-marker checks. Keep Global Root `review` and Species `partial/verified` until authenticated compact/focus/recovery and facility-focus evidence supports a gate decision.

## Latest evidence checkpoint — 2026-08-24

The GitHub-triggered deployment `dpl_8gPXTT1Dyde8RwmbHDtXQDPHYehd` is READY for commit `2c63cda` on `omni-v2-rebuild`, with the canonical alias present and exactly 12 Node functions. A public-only 390×844 Playwright run observed `data-rotation="rotating"`, enabled map controls and a zoom change from `1.35` to `2.35`. The same run showed that idle rotation no longer produced a repeated facility-fetch cadence; only two initial readiness requests and one explicit post-zoom request were observed. Guest search deliberately opened the Account sheet while keeping the map mounted behind it.

This advances the shared map/zoom foundation from code diagnosis to `bounded canonical proof`. It does not close the Species gate: compact-width responsive certification, Buyer empty/error/recovery/facility-focus states, full keyboard/focus/reduced-motion coverage and Seller all-width/interactive reachability remain open. No business mutation is required for the next proof.

## Latest evidence checkpoint — authenticated read-only pass

Deployment `dpl_B1HfPNbXJaiyq4WEtj7JNWyQW3xD` is READY for commit `6e9c335` on `omni-v2-rebuild`, with exactly 12 Node functions. The canonical authenticated Sandbox replay at 891×765 showed the query-aware Buyer loading state followed by `nearby-state-ready` and `Ouvrir Marche de Hanoukope`; the query request carried no camera bounds, preventing idle-globe movement from filtering the text result. The public endpoint independently returned HTTP 200 with one safe public name match. The map/canvas, public pin, zoom control, J5 owner and separated result surface remained present.

The same read-only session opened Seller through the visible mode control and showed `Contexte vendeur autorisé`, two request rows, catalogue tab and `Handoff encore verrouillé` without response or rebind. J5 then opened `Revue des claims`, which showed `Rôle reviewer non ouvert` and no decision inputs, followed by `Inbox Omni` with `Inbox vide`, `Actualiser` and the explicit Inbox-before-PWA boundary. These are bounded authenticated visual proofs at the available Sandbox viewport, not role, operational or production-readiness proof. The submitted private test claim and all historical rows were preserved.
