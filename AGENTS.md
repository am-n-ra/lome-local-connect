# AGENTS.md — lome-local-connect (Omni)

Repository memory for OpenHands agents working on Omni.

## What this project is
Omni: a map-first, constraint-based search engine for local supply (Lomé field pilot first). A buyer finds where something exists near them, learns whether it can satisfy their request now, and converts that into a QR-traced Omni transaction. Sellers of any shape become supply without maintaining a full inventory.

## Active operating plan
- **Founder HQ Master Plan:** `docs/founder-hq/founder-hq-master-plan.md` (`HQ-OMNI-2026-09-02`).
- **Board:** `docs/founder-hq/founder-hq-board.md`.
- **Specialist plan (CURRENT):** `docs/nature-way/intra-skill-plan-NW-PROD-OMNI-SEED2-01.md`.
- **Intent Brief V2 (founder-confirmed, CURRENT):** `docs/nature-way/omni-intent-brief-v2-2026-09-23.md`.
- **System Dependency Map (CURRENT):** `docs/nature-way/omni-system-dependency-map-2026-09-23.md`.
- **Species maquette (CURRENT):** `docs/maquette/omni-species-v2-interactive.html`.
- **Coherence/debt register (CURRENT):** `docs/nature-way/omni-v2-coherence-and-debt-2026-09-23.md`.
- Method skills live in `.agents/skills/` (nature-way ecosystem). Always start via Nature Way Founder HQ; route product work to `/nature-way`.

## ⚠️ CURRENT GATE — read this first (as of 2026-09-23)
**Seed reconciliation + Species reconciliation — RÉOUVERTE 2026-09-23** (par le fondateur).
> Source de vérité : `docs/founder-hq/current-state.md`.
Founder: « on a assez tourne en rond… je pense qu'on a rate tout le process depuis Species » and
« j'ai moi-même assez oublié tout ce que je veux qu'Omni fasse » → asked to **restart from Seed**.

- **Seed V2: CLOS `founder-confirmed` 2026-09-23** — `docs/nature-way/omni-intent-brief-v2-2026-09-23.md`,
  **34 décisions `S-01…S-34`**, founder: « c'est ça ». Supersedes the 2026-09-02 Intent Brief.
- **Species V2: RÉOUVERTE** — maquette `docs/maquette/omni-species-v2-interactive.html` (72 écrans).
  The 2026-09-23 screen audit was measured against the **OLD MV1 registry**, not the V2 Seed
  (`COH-V2-18`) → it does **not** prove V2 conformance. Missing surfaces: **S-06** (échelle 0→4),
  **S-11** (double niveau entité/offre), **S-13** (filtres carte), **S-32** (intégrité/réputation d'offre).
- **Next:** `T-11` re-baseline plan on `S-01…S-34` → `T-12` Species V2 conformance audit → maquette to founder.
- **Décisions fondateur appliquées:** `D-1a` fusion maquette+app ; `D-2a` l'offre appartient à l'ENTITÉ.
- **Do NOT** restart Root/Trunk/Branches/Canopy, and **do NOT push to prod**, while this gate is open.
- Historical V1 gates (G-02a–d, Gate 6「 Go with limits 」) are **superseded, not current**.

### Historical gate detail (V1, 2026-09-02 — superseded)
- G-02a (audit existing maquettes): **done** → `docs/nature-way/omni-species-audit-G02a-2026-09-02.md`.
- G-02b (Admin/operator maquette set): **ACCEPTED (founder 2026-09-02)** → `docs/maquette/omni-species-maquette.html` + spec `docs/nature-way/omni-admin-operator-maquette-set-G02b-2026-09-02.md`. **Renamed 2026-09-02 to unified master file.**
- G-02c (Seller maquette): **ACCEPTED (founder 2026-09-02)** — same sheet/grid inheritance, no divergence → in same unified `omni-species-maquette.html` + spec `docs/nature-way/omni-seller-maquette-set-G02c-2026-09-02.md`. Next: G-02d Buyer.

- **G-03 Root System (2026-09-02):** inspection report `docs/nature-way/omni-root-inspection-G03-2026-09-02.md`. **RD-1 (founder decision): v2-canonical — v2_* is the only root schema; public.* frozen for new tables.** Migration `038_v2_g03_root_gaps.sql` closes: RG-1 product availability state + freshness (D-02/D-03), RG-2 StockEvent ledger (S5), RG-3 saved searches (B19), claim requests ported to v2 (S1). **APPLIED: founder executed 038 on Neon SQL editor 2026-09-02, all statements success (manual proof class).**
- **G-05 Canonical migration EXECUTED (2026-09-08):** audit found persistent v2 gaps on `omni-v2-rebuild` branch: migration 003 root guardrails wholly absent, tables 008 (`v2_web_push_subscriptions`( et 011 (`v2_transaction_messages`( missing, guards 009 + index `v2_products_active_offer_idx` missing,, unique indexes 001 (`one_free_slot`( et 007-claims (`one_active_claim`( missing. Consolidated into **`db/migrations/041_v2_canonical_gaps.sql`** (commit `a5b6647`( — idempotent, additive-only. **APPLIED via `scripts/apply-migration.mjs`**  on Neon branch `omni-v2-rebuild`, 23 statements, checksum `5c3952e…`. Registry `public.omni_schema_migrations` backfilled pour 002,004,005,006,007-admin,010,038,039 (8 fichiers déjà couverts en entier( — 9 entrées now. **Zéro perte vérifiée:** 35→37 tables v2, 17 wallet recharge intents intactes, auth/users/comptes/wallets/ledger unchanged. Proof d'abord validée sur branch éphémère `migration-canonical-proof` (br-plain-cloud-amdnf6or( puis appliquée. 040 probe et supabase legacy hors registre (par design(.


- **G-04 Trunk VERIFIED ON PROD (2026-09-02):** deployed to `omni.sparkafrika.online` (commit 8ef1400, bundle `index-rQmYubTH.js`).
  - **Live test results with `demo@seller.omni` (Omni@2026):**
    1. Neon Auth sign-in → JWT token issued (200 OK)
    2. `GET /api/v2/seller/catalogue` returns 6 real products from "Omni Demo Seller Hub" with new G-03/G-04 fields (`availabilityState: 'a_valider'`, `availabilityProEligible: false`)
    3. `POST /api/v2/seller/catalogue/:id/availability` rejects non-Pro facility with **409 Conflict** (D-04 entitlement guard verified on live prod)
    4. `POST /api/v2/wallet/pro` validates wallet balance and slot rules on live prod
    5. `GET /api/v2/seller/catalogue/:id/stock-events` route active and protected (401 without auth)
  - Vercel build pipeline stabilized on `npm@10.9.8` with single maintained `package-lock.json`.
- **G-05 UI comportements (2026-09-04, commits 51c24a6→8658542):** V1.1 maquette = référence. T-10j : **REVERT régression T-10i** (navpill 'en haut' était faux — fondateur a clarifié « en bas toujours mais au-dessus de sheet » = z-index pas position) → navpill `bottom:14px` + `z-index:60` (au-dessus de sheet z-50). **Dock contextuel V1.1 (dockFor)** : 3 boutons re-déduits selon sheet actif + rôle. T-10k : **REMOVE accent dot** (fondateur « active dot not necessary »). **Sheets 1:1 V1.1** : `.omni-sheet` FULL WIDTH `left:0;right:0` (was centered 520px), radius 24px (was 30), shadow `0 -14px 44px` (was `0 -12px 40px`), padding `10px 15px 92px`, border-top only (was full white border). `.sheet-handle` 38px×4px `#dcdcda`. `.sheet-head h2` 16px (was clamp 20-28px). `.section-kicker` 8px/800/1.1px (was 11px). `.primary-button` 42px/14px/10.5px/`#0f0f0f` ink (was 45px/forest). `.searchdock .fld` 36px (was 40px), input 11px. **Fixed broken doubled CSS selectors** (`.btn .btn`, `.status .status`, `.primary-button .primary-button` — botched sed): all single, aligned to V1.1 (`.btn` 42px/14px/10.5px/ink, `.status` ok/ink/gray/dash/warn). **Content aligned** : `.omni-hcard` 136px/16px/thumb 66px/vmark 13px+ring/body b 9px, `.omni-pitem` 13px/font 8.5px/chk 17px, `.maquette-cardbox` 16px/sh-sm, vars `--maquette-line #e8e8e6`/`--maquette-panel #f6f6f4`. T-10h (pin/grille/QR) + T-10i (globe zoom/sheets bas/scrollbar/dock recherche) déjà poussés. Tests 296/296, build `index-BuK-E-ZY`, prod === local. **V1.1 restants non implémentés :** aucun — switch rôle glissant et sélection multi-produits par facilité implémentés (T-10l, commit 374ec41). **T-10p (2026-09-04, commit ca1ed22, founder voice directive) :** (1) **sheets bottom-anchored** — desktop aussi `bottom:0` (was floating `bottom:22px`), `left:50%;translateX(-50%)`, keyframes split mobile/desktop ; (2) **carte zoomée** au chargement — initial zoom 1.35→11.5 (Lomé city level), cameraMode manual_navigation (was resting_globe), projection mercator (was globe) ; (3) **globe tourne pendant la phase 'world'** de la recherche (setCenter lng+0.35/frame) ; (4) **filtres rayon étendus** — 1km(quartier)/5/10/25km(ville)/100km(région)/0=monde entier avec labels de scope. **T-10q (2026-09-04, commit 1c7b3fb, founder mobile behavior) :** (1) **bottom gradient blur** `.omni-bottom-gradient` 120px z-55 entre sheets et navpill — contenu fondu dans gradient blanc avant le dock ; (2) **sheet 100% bottom-anchored** desktop — `right:auto` ajouté (conflit avec `left:50%` corrigé) ; (3) **grille apparaît APRÈS globe reveal** — `beginSearch` n'ouvre plus la grille immédiatement, `handleRevealStateChange` l'ouvre quand le reveal se termine ; (4) **claim flow vérifié** — bouton "Commencer la revendication" connecté (crée draft → panel 'claim' → ClaimSheet). **T-10q2 (2026-09-04, commit 176c622) : clavier mobile** — search sheet `omni-keyboard-aware` (remonte au-dessus du clavier, `bottom: var(--omni-keyboard)`) ; navpill remonte avec le clavier (`bottom: 14px + keyboard inset`, transition 0.22s) ; app shell ne bouge jamais (`position:fixed` + `100svh`, role switch `top:14px` fixe en haut). **T-10q5 vérifié fait :** `pointsForResultFraming` inclut `userPosition` — recadrage caméra sur pins majoritaires + position user après reveal. **T-10r (2026-09-04, commit 1162000, retours fondateur PC) :** (1) **BUG reveal jamais déclenché** — staleness React : `setCommittedQuery(demand.rawQuery)` puis `beginSearch()` relisait le state `query` stale → écrasait avec `''` → `searchRevealKey` null. Fix : `beginSearch(event?, explicitQuery?)`. (2) **Sheet décalée desktop** — `.omni-sheet-enter` fill-mode `both` : son keyframe final `transform: translateY(0)` écrasait le `translateX(-50%)` du centrage. Fix : centrage via propriété CSS **`translate`** (indépendante de `transform`, immunisée aux keyframes) sur `.omni-sheet` + `.nearby-sheet`. (3) nearby-sheet desktop bottom-anchored (was floating 22px). (4) gradient blur pleine largeur + discret (72px, z-6 : la CARTE fond vers le dock, UI nette au-dessus). (5) spin monde = boucle rAF lerp zoom + rotation longitude (plus de conflit easeTo/setCenter). (6) fallback grille : recherche vide/erreur ouvre la grille au lieu de pendre
- **T-10w (2026-09-05) — coquille desktop V1.3** : la maquette V1.3 fournie fondateur (`HO-OMNI-05`( introduit `body.desktop` (≥1040px(: recherche = **barre permanente haute**, résultats = **rail gauche** (346px(, autres sheets = **tiroirs droits**, dock = **rail latéral gauche**. Implémenté sur `omni-v2-rebuild` (option 1, réversible( : classes `omni-sheet-search` + `omni-sheet-results` + wrapper `omni-results-surface`, prop `constraintsPersistent` dans `LiquidSearchDock` (chips visibles sans frappe sur top bar(, bloc CSS `@media (min-width:1040px)` dans `v3.css`. Mobile/tablette inchangés (sheets bottom-anchored + centrées(. Bundle `index-BYPWhr5c.js` ≠ prod ; **PUSH PROD = ordre fondateur explicite requis** (guardrail T-07d(. Tests 296/296 ✅, `lint` ✅, `check:boundary` ✅, build ✅, preuve browser statique ✅ (chips persistantes top bar(, rail vide pré-recherche + `aria-modal` desktop = dette sémantique documentées en register. .
- **Gate  ​5 RE-OUVERT `review_founder` (2026-09-05(**: fondateur « on est assez loin maquette et même le motion de recherche n'est pas en place ». Audit `docs/nature-way/omni-species-reopen-audit-G5-2026-09-05.md` (F1–F14,: la cinématique recherche **existe en code** (`TrunkMap`, `map-reveal.ts`: étapes monde→continent→pays→région→ville→cadrage + spin(, se déclenche sur prod (F2–F4(, mais **ce sandbox la bloque** (`mapStatus=error` → `searchRevealKey=null`) et la chorégraphie est **appauvrie** (pas de pavé tilt/rotate, pins d'un bloc — stagger pins absent(‪. Gears vs maquette: rolepill glissant, countmark « 206 », vignettes riches, rail pré-recherche élégant, chips-démos, tilt — absents/simplifiés (F5–F14(‪. **Aucun code modifié** — 6 questions fondateur §4: niveau de fidélité motion (a/b/c(, stocker HTML V1.3 au repo, coquille desktop garder/désalligner, polish surfaces, chips, env spot-check réel. **Desktop delta T-10w reste done (654ee22(.
- **G-04 Trunk — reconciliation pass (2026-09-03, HO-OMNI-04):** founder asked « développer la V1 » with a maquette reference. Founder HQ Master Plan + Board reconciled (they were stale, still showed G-02c active): Gates 1–3 `done`, **Gate 4 Trunk active**. Evidence observed this session: `npm ci` + `npm test` → **44 files / 274 tests pass @ `deb5072`**; `npm run build` → `index-DzG1YV4e.js`; prod `omni.sparkafrika.online` serves **the same hash** → **2026-09-02 stale-deploy blocker RESOLVED**. Local plan `docs/nature-way/intra-skill-plan-NW-PROD-OMNI-01.md` extended: T-07a Admin slice inventory/proof `ready` (single active slice), T-07b Seller, T-07c Buyer, T-08 integrated proof `planned`; T-07d deploy-freshness guardrail `done`. Open founder question: is the mentioned maquette the accepted unified `docs/maquette/omni-species-maquette.html` or a NEW artifact (→ Species re-audit before Trunk inherits)?
- **G-04 Trunk — T-07a Admin slice IMPLEMENTED (2026-09-03, HO-OMNI-04):** founder confirmed the unified maquette is the authority (Species stays closed). Inventory vs accepted A1–A8 (`docs/nature-way/omni-trunk-admin-inventory-T07a-2026-09-03.md`): A2/A4/A5 already present; closed A1 console (`GET /api/v2/admin/console` + `AdminConsoleSheet` w/ counts + next-action + chips; Admin pill → console), A3 operational state (migration **`039_v2_operational_state.sql`** + `POST /admin/facilities/:id/operational-state`, admin-guarded, reason + audit), A6 exceptional counter correction (`POST /admin/facilities/:id/sales-counter`, 0–3, no-op rejected, **trust_state untouched**), A8 audit log (`GET /admin/audit-events` + `AdminAuditSheet` + hop-to-map), R-03 (`ReviewQueueItem` lat/lng + `TrunkMap.focusTarget` easeTo ≥14 on claim select / audit hop). **Proven:** 285/285 tests (+11), tsc clean, build `index-BCg5G0cM.js`, boundary clean; sandbox browser shell proof only (no DB/Auth locally). **Status 039: APPLIED — founder executed `039_v2_operational_state.sql` on Neon SQL editor 2026-09-03 (manual proof class, same class as 038).** **Push exécuté (ordre fondateur explicite) : routes admin 401 en ~60s, bundle prod `index-BCg5G0cM.js` === build local (guardrail T-07d ✅).** **Prod proof PASS 9/9 (2026-09-03, correlation `16e0997f-…`, compte admin fondateur `kheirlissi@icloud.com`)** — console authorized + counts, op-state round-trip + DB check, audit events + coords, counter no-op rejected. Auth-chain finding: Neon session token is opaque; sign-in cookie `__Secure-neon-auth.session_token` exchanged at `GET {authUrl}/token` for the JWKS JWT (in `scripts/prove-v2-admin.mjs`). Probe `040_v2_admin_proof_probe.sql` (cast `u.id::text` — neon_auth uuid vs v2_accounts text) confirmed admin role already active. **T-07a CLOSED → T-07b Seller slice next.** **T-07b CLOSED (2026-09-03): prod proof 8/8 PASS (correlation `6978e7ea-…`, identity `demo@seller.omni` → demo-rebind fixture `Omni Demo Seller Hub`)** — routes 401, catalogue 6 produits, garde 409 non-Pro (D-04), queue autorisée, wallet S8, DB v2-only. Positive Pro-setter path = unit coverage `trunk-repository.test.ts` « Product availability Root seam ». Note: `demo@seller.omni` créé cette session via sign-up (n'existait pas dans neon_auth); `demo@buyer.omni`=buyer, `juniorkheir@gmail.com`=operator:active. → **T-07c Buyer next.** **T-07c CLOSED (2026-09-03): prod proof 8/8 PASS (correlation `9839cc09-…`, identity `demo@buyer.omni`)** — B19 saved-searches était le seul gap réel (table 038 sans route/surface): routes `GET/POST/DELETE /api/v2/saved-searches` + repository + `BuyerSearchPolicyError` + client/types construits; round-trip create/list/delete prouvé + cleanup DB vérifié. → **T-08 integrated proof next (les 3 slices Admin/Seller/Buyer fermées).** **T-08 CLOSED (2026-09-03): preuve intégrée PASS 16/16 (correlation `65488663-91f4-4347-ba46-4fd9d600991d`, commit `ebd9d80`)** — `scripts/prove-v2-integrated.mjs`: boucle complète offre→dispo→réponse→intent→QR→paiement→fulfilment→received→rated→**closed** (10 événements v2), vrai vendeur `demo@seller.omni` + acheteur `demo@buyer.omni`. Découvertes: (1) catalogue démo `quantity_allocated_omni=0` → fixture « offre réelle » allouée/publiée; (2) race de visibilité serverless (rating après `received`) → retry documenté; (3) routage serverless **by design** (12 bundles + catch-all Vercel, pas de divergence). 285/285 tests. **Gate 4 Trunk: volet opérateur à confirmer par le fondateur avant clôture.**
- G-02d (Buyer maquette): **ACCEPTED (founder 2026-09-02)** — audit complete, PAYMENT/SAVED/ACCOUNT added. Species gate now closed across all four roles (Admin/Seller/Buyer/Operator). Next gate: **Root System (G-03)** — schema, contracts, invariants.
- **Art direction locked (founder 2026-09-02): liquid glass** — inherit the REAL `LiquidGlass` tokens from `src/components/ui/LiquidGlass.tsx` (ink #1A1C1B, forest #234D40→#1A3B31, accent coral #F08F5A; surfaces bg-white/70..95 backdrop-blur-md..2xl border-white/40..80). **Logo = location pin with an eye inside, 3D-shaped** (SVG gradients + iris). All flows épurés/minimalistes, map-centric, contextual (surfaces = frosted overlays over the map, not separate dashboards).
- **Admin screen set A1–A8** (founder accepted): A1 Console équipe, A2 File de revue, A3 Confiance & état opérationnel (two dimensions per D-01), A4 Activation/suspension vendeurs, A5 Gestion rôles, A6 Correction compteur (exceptionnelle), A7 Sorties opérateur & push, A8 Audit & mesure. Public trust label `Non revendiquée`/`Non confirmée`/`Confirmée`; `Certifiée` = internal milestone; separate operational state `Ouvert`/`Fermé`/`Temp. indispo.`.
- Founder rules: **R-01** dock = minimal floating pill at bottom, **3 icons** (search ⌕ · QR ▦ · menu ≡), contextual per role (Buyer ⌕→search dock, ▦→scan facility public QR, ≡→menu grid; Admin pin/search → admin sheet); **R-02** simple/direct; **R-03** map is the heart for EVERY flow, **always fully visible**, content = partial bottom sheets/grids only; **admin is a role in the top pill** (Buyer default, +Seller/+Admin/+Operator if eligible — one identity + capability, D-06); facility pin-click shows **buyer info vs admin review sheet** (different per role).
- **FINAL PALETTE (locked 2026-09-02): monochrome** — ink `#0f0f0f`, white, panel `#f7f7f7`; **one accent `#2e8b6f`** (desaturated green, close to ink/white dominance) = "En stock / Vérifié" only. Map = white base, **REAL continent silhouette**, single circular count marker. **Naming (unambiguous):** `En stock`, `Vérifié`, `À valider`, `Non revendiquée`, `Bientôt`.
- **ORDER OF EXISTENCE (locked):** Admin → Seller → Buyer. Contractually: **Facility before Offer**; **verification before availability is shown**; place → listing → discovery → intent → transaction.
- **EVERYTHING CONTEXTUAL (locked):** dock, search, plans, constraints all adapt to **screen + focused sheet + role + active constraints**. Horizontal **result grid** with thumbnails; **search dock compact at first** (constraints reveal on typing, per-role); **facilities/products have visuals**; **unclaimed facilities** show a claim body; **comparison + bulk availability**; **plans differ by role** (Seller ≠ Buyer); **search is role-scoped** (Seller→own companies/facilities, Admin/Operator→review objects).
- **REFINEMENTS (transaction-room pass, 2026-09-02):** **transaction room** = one sheet where **QR is the gateway** into the locked flow; inside it a **state-tracking timeline intent → rating** and **transaction-scoped chat**; **back disabled only inside the locked transaction flow** (cancel/advance), free elsewhere; **nothing overlays the bottom dock**; **scrollbars hidden**; **SVG line icons** (not unicode stickers) aligned with Omni V1 master.
- Root and Trunk stay **planned**; **no code** until Species accepted. Do not resume PR 2–6 of `OMNI-V3-MASTER-PLAN.md`.

## Confirmed decisions (D-01…D-07, see Intent Brief)
D-01 keep 9 internal trust states + derive public label + separate operational state (open/closed/temporarily_off). D-02 Product→Offer in contracts; add StockEvent ledger. D-03 deterministic auto-availability in V1 (facility_pro-gated). Freshness 4 h fresh / 24 h expired, facility-level setting. D-04 entitlements per facility (seller), per account (buyer); $20 bonus locked until confirmed. D-05 browse without account; search/availability/buy require auth. D-06 one identity + capability toggle. D-07 nothing pre-accepted.

## Key authoritative artifacts
- MV1 screen spec: `docs/omni-v1-screen-and-state-specification.md` (§4 screen registry, §75–77 completion/contract, §78 build order). Note: MV1 lists NO dedicated admin/operator screen — admin is implied by X02 Certification + governance parent in SDM; the founder's Admin-first build order fills that gap.
- Species blueprint: `docs/omni-species-blueprint-2026-08-27.md` (§4D Admin/Reviewer/Operator; §2 design system).
- Existing prototype (buyer/seller/admin stubs): `docs/omni-species-html/index.html` + `app.js`. Design tokens in `:root` of index.html.

## Branch rule( founder locked 2026-09-02; amended 2026-09-05; push-wait superseded 2026-09-07(
**Only `omni-v2-rebuild` is touched.** Never merge to `main`. "Merge" means "push prod branch"; feature branches merge into `omni-v2-rebuild` only if founder explicitly names one. **Push rule (amended 2026-09-07, founder order(: push vers prod SANS attendre de signal—APRÈS CHAQUE ÉTAPE SIGNIFICATIVE(; vérifier hash prod === local (guardrail T-07d( avant continuer.**
**T-13 (2026-09-05, commit  ​81869c9(: refonte UI 1:1 — slice V-1 coquille livrée( shell `TrunkAppV13`( remplace `TrunkApp`( (mount main.tsx(; atoms 1:1 `ui-v13.css` extrait du HTML maquette; rolepill/navpill/countmark/sheets SEARCH-RESULTS-FACILITY-MENU-ACCOUNT-AUTH réels, backend réel via `api.ts` + auth Neon email/password; tests 297/297, tsc clean, build+prod hash `index-CbuxGUOp.js` === local.

## T-11 (2026-09-23) — registre décisions V2 + SP-1 livré (maquette, 0 code produit)
- **Registre :** `docs/nature-way/omni-species-v2-decision-registry-2026-09-23.md` — les **34 décisions `S-01…S-34`** cartographiées → surface maquette → statut (`OK`/`PARTIEL`/`ABSENT`/`CODE`), **0 orpheline**. Méthode : grep + inventaire des **72 `SHEETS`** de `docs/maquette/omni-species-v2-interactive.html`.
- **Ordre validé fondateur 2026-09-23 :** `SP-1` → `SP-2` → `SP-3` = le fond (caractéristiques d'offre, échelle 0→4, double niveau entité/offre) ; puis `SP-4` (intégrité/réputation visibles au **choix**), `SP-5`, `SP-6`.
- **SP-1 LIVRÉ (preuve navigateur) :** panneau **« Caractéristiques de l'offre »** sur la fiche offre — les **7 caractéristiques du modèle jour 1** (quantité/déplétion, **unicité**, position, temporalité, retrait/livraison, état, **prix négociable ?**), **deux profils** (commerce renouvelable vs **pièce unique** particulier). `S-01` passe **PARTIEL → OK** sur la fiche.
- **Bug réel corrigé en livrant SP-1 :** `openOffer()` réaffectait `S.product` **sans** `carac` → la fiche aurait **planté** (`undefined`). Le commerce passait par chance, le **particulier** aurait cassé. **Règle : un champ ajouté à un objet partagé doit être ajouté à TOUS ses producteurs, pas seulement à l'initialiseur.**
- **Méthode de mesure (leçon) :** **grep seul produit des faux négatifs.** Le premier registre a déclaré **S-07/S-32/S-06** « absents » à tort — la vérification navigateur a démenti 3 lignes (filtres carte `Tout/Commerces/Particuliers/Transport` **présents** ; `Intégrité/Réputation de l'offre` **présents** sur la fiche ; chip `Transactable` **présent**). **Méthode désormais : grep → ouvrir l'écran → lire le libellé réel.** Corrections consignées §8bis.
- **Preuve :** maquette rechargée en navigateur, panneau rendu ; JS `node --check` **OK**, **72 écrans** intacts, 0 doublon défini ; `check:state` vert. **Zéro code produit modifié.**

## SP-2 (2026-09-23) — échelle d'existence 0→4 (maquette, 0 code produit)
- **Le chip `Transactable` filtrait mais n'enseignait rien.** SP-2 livre l'échelle : **0 Présente · 1 Revendiquée · 2 Offre publiée · 3 Disponibilité vivante · 4 Transactable** (S-06). Surfaces : **fiche offre** (`levelLine` : 5 segments + numéro + libellé + **preuve datée**) et **cartes de résultat** (badge `Niv. n · libellé`). `S-06` **PARTIEL → OK**.
- **Preuve navigateur :** Spaghetti `Niv. 4 · Transactable` · Ordinateur `Niv. 2 · Offre publiée` · Épicerie `Niv. 0 · Non revendiquée` — les 3 surfaces × 2 profils. JS `node --check` OK, 72 écrans, 0 doublon.
- **Bug 1 corrigé — base 0 vs base 1 :** `LEVELS` était indexé en base 1 alors que `level` est un numéro base 0 → « Niveau 5 » hors échelle, puis « Niveau 2 · Revendiquée » au lieu d'« Offre publiée ». Fix : libellé = `LEVELS[level]`, barres = `level + 1`. **Même classe d'erreur d'indexation que précédemment.**
- **Bug 2 corrigé — incohérence révélée :** le statut d'en-tête de la fiche était **hardcodé « À confirmer »** → la même offre était « En stock » dans les résultats et « À confirmer » sur sa fiche. Fix : statut **dérivé du niveau**. **Ajouter une dimension révèle les contradictions qu'un champ hardcodé masquait.**
- **Ordre :** `SP-1` ✅ · `SP-2` ✅ · **`SP-3` à faire** (double niveau entité/offre) → `SP-4`–`SP-6`.

## SP-3 (2026-09-23) — double niveau entité / offre (maquette, 0 code produit)
- **S-11 clos.** Sélecteur **« Chercher une entité / une offre »** : placeholder, phrase et bouton changent ; en mode **entité** les contraintes d'offre (distance/budget/quantité) sont **masquées** (vérifié par style calculé `OBLOCK_DISPLAY=none,none` — l'extraction texte inclut le caché, ne pas s'y fier). **Deux états vides** : `results-empty` (offre) et **`entity-empty`** (entité, avec revendication d'un lieu reconnu). **Page entité** enrichie : *Nature*, *Niveau de l'entité*, *Ses offres* avec niveaux, + la règle **confiance = entité / réputation = offre**.
- **Test de non-régression S-11 livré : `scripts/check-maquette-v2.mjs`** (`npm run check:maquette`), **zéro dépendance** (pas de `node_modules`). Garde : inventaire `SHEET` sans doublon / non réduit · `LEVELS[lv]` jamais `LEVELS[lv-1]` (échelle **exactement 5**) · parité `carac` ≡ `level` sur tous les producteurs. **Falsifié** : réintroduire `LEVELS[lv-1]` → exit 1 ; retirer `entity-empty` → FAIL (2). **73 écrans**, 0 doublon.
- **Preuve navigateur :** `entity-search` · `entity-empty` · `entite-publique`. **Zéro code produit.**
- **Fond terminé : `SP-1` ✅ · `SP-2` ✅ · `SP-3` ✅.** Restent `SP-4` (intégrité/réputation **au choix**, S-32) · `SP-5` (S-07/S-10) · `SP-6` (S-22/S-25).

## T-12 (2026-09-23) — audit de conformité Species V2 : le registre redevient vrai
- **Défaut trouvé :** le registre `omni-species-v2-decision-registry-2026-09-23.md` **contredisait nos propres livraisons** — en-tête « 72 écrans » (réel **73**), `S-01` encore **PARTIEL** (livré par SP-1), §2 « négociable : 0/ABSENT » (les 7 caractéristiques sont montrées), écart `S-06` « aucune surface ne montre le NIVEAU » (SP-2 le montre), §7 listant les 3 **P0** comme manquants. **Cause : modifier la maquette sans mettre à jour la carte.**
- **Correctif durable :** `scripts/check-maquette-v2.mjs` §6 « registry truth » — le garde vérifie désormais que le registre **annonce le vrai nombre d'écrans**, que `S-01/S-06/S-11` sont **OK** après SP-1/2/3, et qu'aucun écart ne nie une surface livrée. **Falsifié (3 modes) :** 99 écrans → FAIL exit 1 ; `S-06` re-PARTIEL + écart rétabli → FAIL (2) ; `S-01` re-PARTIEL → FAIL exit 1.
- **État :** `SP-1` ✅ `SP-2` ✅ `SP-3` ✅ · `T-12` ✅ · restent `SP-4` (S-32) `SP-5` (S-10 immobilier + origine géo) `SP-6` (S-22/S-25/économie). **Zéro code produit.**

## SP-4 (2026-09-23) — confiance au moment du choix (S-32)
- **Gap :** intégrité + réputation existaient **uniquement sur la fiche** → l'acheteur ne les voyait qu'**après** avoir ouvert l'offre.
- **Livré :** marque **par carte de résultat** (« 4,6 ★ · Achetée 12× · intégrité ✓ », distincte par offre), ligne **Intégrité** dans le **comparateur**, fiche qui **lit** `S.product.integ`/`rep` (plus de texte en dur), et « Pas d'offre · rien à évaluer » sur l'entité non revendiquée. Monochrome (accent réservé `.vmark`/`.status.ok`).
- **Garde §5bis + registre :** marque présente sur les cartes + parité `integ` ≡ `rep` + ligne registre `S-32` OK. **Falsifié (3 modes).** **Leçon :** un garde qui compte une classe **globalement** ne prouve rien (il passe avec 5 marques retirées) ; `.trust` est aussi une classe de `.stepline` → **scoper au bloc `results`**.
- **État :** `SP-1..SP-4` ✅ · restent `SP-5` (S-10 immobilier + origine géo) `SP-6` (S-22/S-25/économie). **Zéro code produit.**

## SP-5 (2026-09-23) — le modèle n'est pas limité au physique (S-10)
- **Gap :** **immobilier** = 0 surface ; **origine géo** du digital jamais montrée.
- **Livré :** carte + fiche **immobilier** complètes ; **origine** portée par la **Position** (« Immatérielle — origine : Lomé », pas de 8e champ) ; résultats 3→5 ; catégories nomment l'immobilier. **Zéro code produit.**
- **⚠️ 2 défauts réels trouvés en LISANT l'écran rendu :** (1) emoji de fiche **binaire** → immo/digital affichaient 🍝 ; (2) **« Itinéraire » proposé sur une offre en ligne** = mensonge sur lequel l'acheteur agit. Corrigés.
- **Garde §5ter :** parité **stricte** des 7 champs sur les 4 formes + pas d'itinéraire pour l'immatériel. **Falsifié (4 modes).** **Leçon :** un **plancher** `>= 5` laisse une forme perdre un champ — exiger la **parité**.
- **État :** `SP-1..SP-5` ✅ · reste `SP-6` (S-22/S-25/économie).

## SP-6 (2026-09-23) — compléments + **correction de 4 fausses « absences » du registre**
- **Livré :** S-25 ownership **explicite** (« Cette offre appartient à… » + « Le lieu — il dit *où*, jamais *à qui* ») ; **bonus 20 USD → 3 ventes à des acheteurs distincts** (seller-pro) ; **S-14 seuil par volume** « 1 vente (particulier) · 3 ventes (commerce) » (seller-verif).
- **⚠️ 2 des 4 « gaps » déclarés étaient FAUX :** `Partager (WhatsApp/SMS)` (S-22) et `3 / 20 (gratuit)` (plafond) **existaient déjà**. **Cause :** le registre mesurait des **chaînes littérales** (`20 offres`, `origine`) au lieu du **fond**. C'est la **classe T-12** (déjà frappé S-07/S-32/S-06). **Règle : mesurer ce que l'écran DIT, jamais le mot qu'on a choisi d'y chercher.**
- **Garde §5quater :** les **5** affirmations épinglées (épreuves **et** déjà-présentes, pour que ça ne dérive ni dans un sens ni dans l'autre). **Falsifié (5 modes).**
- **État (CORRIGÉ 2026-09-23) :** `SP-1..SP-6` **livrés** — **PAS validés, PAS clos**. La clôture de Species V2 est une **décision fondateur** (`SP-VALIDATION`).

## ⛔ INCIDENT 2026-09-23 — deux étapes sautées (règle ajoutée)
Le fondateur a demandé « est-ce qu'on a fini avec seed et species ? tu ne sautes pas des étapes ? » — **il avait raison, deux étapes ont été sautées dans la même session :**
1. **« Species V2 est close » a été affirmé après la livraison de SP-1…SP-6** — or le plan (`T-13`) dit : *bloqués sur validation fondateur*. **Livrer ≠ faire accepter.**
2. **Un audit de porte Root a été produit et poussé** (`omni-root-gate-v2-assessment-2026-09-23.md`, `6a9b7fc`) — or la board dit : *« Un seul gate actif. Ne pas empiler Root/Trunk/Canopy au-dessus d'une Species non conforme. »* → **PRÉMATURÉ, parqué** ; mesures gardées comme renseignement, demande `R-1/R-2/R-3` **retirée**.
- **Règle : livrer une tranche ne clôt pas une porte ; une porte se clôt par une décision fondateur enregistrée. Ne jamais ouvrir une porte aval « pour préparer ».**
- **Action courante = validation fondateur de `SP-1…SP-6`.** Contradiction `T-12` (`LIVRÉ` barré vs `planned` vs board « requis ») = `T12-STATUS`, à trancher fondateur (défaut gardé, `planned` par précaution).

## Commands
- `npm install`, `npm test` (29 files / 184 tests as of last run), `npm run lint` (tsc). Build: Vite. Deploy: Vercel (prod frozen for product changes).

## Conventions
- French UI (vouvoiement). Mobile-first PWA. MapLibre only (no Google Maps). Neon Postgres. FedaPay for wallet reloads only.
- Preserve existing DB records/identities by default; destructive migration needs a confirmed decision.
- Maquette before pixels, contract before code. Evidence > optimism. No orphaned layers.
* All UI must reference `docs/design.md` vocabulary (committed); legacy `omni-sheet`, `glass-*`, `liquid-*` aliases are retired — use `.sheet`, `.pitem`, `.hcard`, `.status`, `.btn`, `.cardbox`, `.searchdock`, `.chip`, `.compte`, `rolepill`, `.navpill` only. Trust accent `#2E8B6F/#EEF4F1` only for `.vmark` / `.status.ok`.

**T-12 (2026-09-05, commit 04c4df2) : chorégraphie recherche V1.3 implémentée.** A: `easeTo` prise-de-hauteur `{pitch:35, bearing:8, zoom: z−1, duration:250, easing:t*(2−t)}`; B: sur `moveend`, `flyTo({ center: résultats, curve:1.7, speed:1.2, pitch:0, bearing:0, essential:true })` durée dynamique 900–1400ms + labels paliers live (`labelForZoom`: `<4 monde / 4–6 Afrique de l’Ouest /  ​6–9 Togo /  ​9–12 Région Maritime /  ​12–14 Lomé / >14 rue`); C: sur `moveend` final, pins révélés en 3 vagues 60ms（ pop-in stagger-like（ + cadrage `fitBounds`（ reduced-motion → jumpTo direct. Tests migrés `map-reveal.test.ts` → `computeSearchFlight`（→ suite **297/297**, tsc clean, build ok. HTML maquette V1.3 `docs/maquette/omni-species-maquette.html` déjà commité — décision §4(2( satisfaite. Alignement naturel (§4(3() intégré（.**T-13b (2026-09-05, commits 357a5a2 + 4f5660e( — MAJ référence maquette V1.3:** le fondateur a fourni la version **complète** du code maquette (« replace the existing with the updated version »(() — remplacé dos `docs/maquette/omni-species-maquette.html` (22 sheets + coquille desktop `body.desktop` + moteur JS clean-room node-verified(; register `omni-maquette-v1.3-reference-register-2026-09-05.md` MAJ. **Re-audit G06** (`docs/nature-way/omni-v13-reaudit-v1.3-G06-2026-09-05.md`(: coquille V13 **alignée 1:1** vs la nouvelle maquette — statuts FULL/PARTIAL/ABSENT par surface (code-verified(. **Dettes**: BULK multi-facilités + COMPARE absents (décision fondateur requise: développer en V-6 ou endetter(; facilité mobile absente; fraîcheur/expired variantes; aria-modal desktop. **+V13-ONLY**: claim complet, wallet FedaPay réel, saved CRUD, chat transaction réel, auth Neon réelle. **Push**: jusqu'à `357a5a2` poussé; `4f5660e` (re-audit docs( local—ahead-1 (flake token fin de session(.

**V-6 (pending fondateur decision(:** combler écarts acceptés (BULK/COMPARE ou dette( + fraîcheur/expired + aria-modal desktop + audit M-01…M-17; clôture Gate  ​6 verdict `Go with limits`.
**T-13c (2026-09-05, commit  ​26eeb3d(**: V-6a/V-6b livrés — BULK + COMPARE réels intégraux (sheet `bulk`/`compare` in TrunkAppV13, données réelles via `api.ts` requestAvailability/getAvailabilityResponses(, comparateur module propre `src/trunk/v13-compare.ts` (clean-room ASCII(; CSS 1:1 from maquette (sortbar/sortchip/freshbar/chk/dockmask/icon-in/navpill.morph/selbar + press-states( (dockmask élément monté z10(; tsc 0, tests  ​295/295, build ok (; **push TRÈS BLOQUÉ — jeton GH non autorisé** → remonter au fondateur pour un token `repo`; opérations: V-6c+ restantes (desktop MQ/body.desktop rail layout, aria-modal/rolepill glissant, sheets restantes SG sur données réelles(, V-7 legacy decommission déjà commité (bcc6143(, push, puis clôture Gate 5/6 selon fondateur.

**T-13d (2026-09-06(: fallback map G-06.** Quand MapLibre/tuiles échouent, `src/trunk/fallback-map.ts` rend une vraie carte monde animée avec les **vraies facilities lat/lng** (`.vdot`/`.cmark`/`.usermarker`, labels `.pin-label`, pop-in `vdot-reveal` différé( et la **même surface méthode MapLibre** (`easeTo`/`flyTo`/`once`/`on`/`off`/`getZoom`/`getCenter`/`cameraForBounds`/`jumpTo`/`zoomIn`/`zoomOut`/`resize`/`remove`/`setFacilities`/`setUserLocation`/`dragRotate.disable`/`touchZoomRotate.disableRotation`(. **CSS fallback porté byte-exact** de la maquette dans `src/trunk/ui-v13.css` (`.usermarker`, `.pop-hidden`, `.vdot-reveal`, `.vdot.dim`/`.cmark.dim`, `:active`, `.mobile`, `@keyframes locpulse`/`pulse`/`popIn`, vars `--warn`/`--warn-soft`/`--sh-sm`/`--sh-md`, `.meloc`, `.mapflash`, + `.pin-label` extension(.**Tests unitaires** `src/trunk/fallback-map.test.ts` (8 it,, jsdom per-file `@vitest-environment jsdom` pragma — pas de config globale changée(: repères réels, surface méthodes,, moveend une fois par mouvement,, setFacilities/setUserLocation, clamp zoom 0–18 + cameraForBounds,, remove sans fuite,, pop-in révélé. **Suite 303/303** (48 files(,, `lint` ✅, `check:boundary` ✅. Note authoring: les canaux d'écriture altèrent les paires `);`/`];` dans les longues chaînes — du coup,, écrire le test via de **petits blocs file_editor atomiques vérifiés au byte** (plutôt que de longs heredocs/creates mono-bloc( rester fiable.

**T-13e (2026-09-06, commit 0336a5c (: coquille FULL-BLEED — plus de cadre téléphone.** Retour fondateur : « l’ui actuelle est un viewport contenant tout le design et qui se retrouve sur un espace blanc plus large, ce qui n’est pas normal pour une app ». `html,body` perdaient `display:grid;place-items:center;padding:18px;gap:14px` (qui centrait un cadre 360×720 sur fond gris → bbox non-blanc `(4,8,986,1078)` = mockup téléphone. `:root` perdait les mêmes déclarations (→ `width:100%;height:100%;overflow:hidden`. `.omni-v13-stage` perdait `width:360px;height:720px;border-radius:46px;box-shadow+inset` (→ `width:100%;height:100%`. Ajout `.omni-v13-stage .map-stage{width:100%!important;height:100%!important}`. **Preuves** : après bbox non-blanc `(4,8,1906,1078)` (pleine largeur. Probe `(961,30)` = `(26,28,27)` (début rail top-left. Transitions y=30 `[0,938,982]` (rail + bord droit. Grille coarse 60px×80px: rail gauche 0–~342px chaque rangée (pas de colonne. X+ en bas-droite cells (14,44/(15,45)). **Poussé** `996066c..0336a5c`; poll prod ~60–90s → hash prod `index-BT2Rt8go.js`/`index-DsUen-ki.css` === build local (guardrail T-07d ✅. CSS prod vérifié contient `html,body{width:100%;height:100%;…overflow:hidden}`+`.omni-v13-stage{width:100%;…border-radius:0;margin:0}`. Tests inchangés 303/303 (diff CSS pur.
**T-13f (2026-09-06, commits fb06ae5→58f8180): responsive mobile + rail permanent desktop  (clôture fondateur(). Retour après première passe T-13f : « je ne parlais pas du dock de navigation mais de l’espace blanc à côté qui contient les informations qui sont des sheets sur mobile ». **Vérité** : le dock n'était PAS le problème — à côté du dock, le desktop doit afficher le contenu des sheets ((results/facility/bulk/compare/flow/claim/seller…), pas du blanc. **Décisions fondateur** : (1( le **panneau latéral gauche ne doit pas apparaître par défaut** (carte pleine au repos(; (2( le **dock gauche PC ne doit pas disparaître** (. **Implémenté** : (a( dock permanent — `journeySheets` étendu à tous les sheets,y compris menu/account/home/wallet/plans/saved/auth, suppression de `display:none` au repos : `body.desktop .navpill{left:14px;top:64px;bottom:14px;flex-direction:column;…z-index:20}` (rule `[data-journey]` dupliquée supprimée(; (b( map remplit l'emplacement gauche **par défaut** — `.mapbase{left:64px}` (était 410px( + `.countmark{left:calc(64px + 90px)` + `.map-legend{left:80px}`; quand un sheet parcours ouvre(results/facility/bulk/compare/flow/seller/claim,, les sélecteurs `.omni-v13-stage[data-sheet=…] .mapbase{left:410px}` shiftent la carte à droite (le panneau 346px prend l'emplacement(; les tiroirs droit (menu/account…) restent overlays map (. **Preuves** : lint ✅, suite **303/303**, boundary ✅, builds `index-CEL6xHhd.js`+`index-BLdtN9Cb.js` (2 commits eb98ca0 + 58f8180(; prod `omni.sparkafrika.online` === local hash → **T-07d ✅**. Scan pixels preview 1920×1080 au repos : colonne x≈10–90 = rail foncé permanent pleine hauteur; reste blank = fallback map floor-blanc (tiles MapLibre indispo sur preview, « Carte indisponible » + boutons Réessayer( — le blanc n'était PAS un vide manquant de sheets, mais la géographie blanche temporaire(.


## NW-12 (clôturé 2026-09-10, tranche « la recherche est le cœur : l'intention derrière »(
- **Recherche = cœur:** chips contraintes câblées vers `SearchOptions` **réels** ( Quantité 10 / ≤ 15 000 FCFA / ≤ 10 km / Ouvert (; `Livraison`/`Transactable` + chips équipe = **`bientôt`** désactivées avec tooltip ( jamais décoratives; `runSearch` passe `chipsToSearchOptions(activeConstraints)` et — après réussite — **`setFacilities(result.data)`**: les pins de la carte deviennent les vrais résultats.
- **Stati opérationnel serveur:** `SearchOptions.operationalState` câblé api/http/repo (`and (f.operational_state = 'ouvert' or f.operational_state is null)`(.
- **Fallback carto:** `map-style-fallback.ts` + handler erreur + watchdog 7 s + bouton Réessayer ( basemap CARTO light_all raster si le style vectoriel/tuiles plantent( — l'animation doit jouer même en dégradé.
- **Équipe · Rôles ( surface admin:** contrat serveur `role-management` GET/POST **prouvé** par 4 nouveaux tests (`role-management.test.ts`(; `AdminV13` liste les comptes + grant/revoke `operator`/`reviewer` avec motif obligatoire audité — le fondateur pourra faire d'un user un operator/team depuis l'UI. Preuves: tsc clean, **344/344 tests**, build `index-2zqnh1PG.js`, `check:boundary` clean.
- Dossier: `docs/nature-way/omni-nw-12-search-intent-and-team-2026-09-10.md`.

## NW-13 (brouillon à valider, 2026-09-10): re-spécification entrée Seller
- Le switch doit donner accès à l'espace seller normalement; sans facilité = état vide avec Créer / Revendiquer ( mêmes ClaimSheet que côté buyer(; la garde Facility-before-Offer ne s'applique qu'à la publication/disponibilité( pas à l'accès à l'espace
- Doc: `docs/nature-way/omni-nw-13-seller-entry-respec-2026-09-10.md` + 4 décisions **D-A…D-D** en attente du fondateur avant tout code

## V-7d seller reply flow ( 2026-09-09, commit  ​773bc4f(
- **Buyer AVAIL captures `deliveryMode` (`retrait`/`livraison`) + optional `note`** — collected in `BuyerFlowV13`, passed through `requestAvailability` API/route/repository end-to-end; buyer pending stage displays both as context.
- **New `SellerReplyV13` sheet ( seller-reply(**: loads seller availability queue `getSellerAvailabilityQueue`, shows buyer mode/note/product/qty/budget, inline reply composer (status/quantity/price/message( via `requestSellerAvailabilityResponse`; wired into `SellerV13` (「Demandes en attente」 button ( and `TrunkAppV13` (( `'seller-reply'` in `Sheet` union + mount + `journeySheets`(. TypeScript clean, **313/313 tests pass**.
- **Migration `db/migrations/043_v2_availability_request_mode_note.sql`** — adds `delivery_mode` (text not null default 'retrait', check retrait/livraison( + `request_note` (text nullable, ≤500 chars check( to `v2_availability_requests`, additive+idempotent. **Prepared+verified on Neon temp branch `br-delicate-cake-am8g44hd`** ( columns/constraints/defaults + idempotent re-run prove(; **APPLIED on canonical `br-dawn-hill-am5amy22`** (vérifié 2026-09-09: colonnes + contraintes présentes via pg_constraint; **mais entrée `omni_schema_migrations` ABSENTE pour 043** → backfill registre requis (pattern 041(; code `773bc4f` **déjà déployé prod**: prod sert `index-BXtDUEoU.js` === build local (guardrail T-07d ✅, 49 files/313 tests, tsc clean, build OK(.
- Replay-guard parity: `createAvailabilityRequest` idempotency comparison now includes delivery_mode+request_note ( tests updated for canonical row fixture

## Wallet recharge webhook (2026-09-09 discovery)

- Deployed Vercel app `omni.sparkafrika.online` writes to **Neon branch `omni-v2-rebuild`** (`br-dawn-hill-am5amy22`; parent `br-bitter-math-amrlbym6`(. The prod default branch has NO v2 tables — live wallet queries must target `br-dawn-hill-am5amy22`.
- FedaPay live webhook route: `POST /api/v2/fedapay/webhook` (header `X-FEDAPAY-SIGNATURE`(. Bare `/webhook` and `/api/fedapay/webhook` → 404. Dashboard delivered to bare `/webhook` → 404 → recharges never credited (founder report(.
- Real FedaPay live webhook shape: top-level `object` is the plain string `"transaction"`; the transaction lives under **`entity`** (`entity.id`, `entity.amount` major units, `entity.currency.iso`, `entity.custom_metadata.omni_recharge_id`(. Old inline parser read only `object.transaction` → empty txn id / NaN amount / `FedaPay webhook payload is invalid`. Fixed: exported `extractFedaPayTransaction` (entity → nested object.transaction → data.transaction → payload fallback( + regression testsin `src/server/http.test.ts` (. Suite 313/313 ✅, lint ✅,, boundary ✅,, prod `index-Dh8qrW5j.js` === local.
- `reconcileWalletRecharge` verifies txn exists + optional omni_recharge_id + exact amount/currency + `status='pending'` + account not suspended,,the insert ledger `('recharge', amount_minor,, 'confirmed', 'fedapay:<txn>')` on-conflict (wallet_id,,kind,,reference( dedupe,,and flips intent → confirmed with `provider_event_id` (unique(.
- XOF math: FedaPay `amount` = FCFA units → handler ×100 → minor (matches `amount_minor`; 100 F → 10000 minor(.
- 2026-09-09 05:48Z: txn `113034942` (100 F( confirmed via retried webhook → wallet `924b39b1…` balance 10000 minor; ledger row `fedapay:113034942`. Other 19 intents still pending (no event( — they hit the old 404 URL:  ​112778127,, 112778658,,, 112778908,,, 112779543,,, 112785391,,, 112915551,,, 112987319,,, 113021641,,, 113031040,,, 113031097,,, 113031172,,, 113031206,,, 113031222,,, 113031394,,, 113031736,,, 113031756,,, 113032463,,, 113034257,,, 113034878. Re-drive:: in FedaPay dashboard set webhook URL to `/api/v2/fedapay/webhook` and "resend/retry" those events — replay-safe (idempotent(. Legacy `wallet_deposits` ids  ​112561706,, 112570311,,, 112579860,,, 112653152 are pre-v2 — do NOT reconcile into v2 ledger.

## FedaPay wallet admin re-verify + UX return (2026-09-09, commits 5acc32d,, e0f3182(
- **`POST /api/v2/admin/reconcile-recharges`** (admin-guarded(: re-vérifie chaque intent pending via `fetchFedaPayTransaction` (live API( et crédite celles `approved` via `reconcileWalletRecharge` (eventId `fedapay:<txn>:admin-reconcile`, ledger same on-conflict logic(. Admin console `AdminV13` card « Recharges Wallet · Re-vérifier » l'invoque (session token( — le founder la presse dans son navigateur,, pas d'échange de credentials requis. Unauthorized →401..
- UX retour FedaPay:: le bouton checkout était `target="_blank"` → la page FedayPay ouvrait Omni eine un autre onglet et le callback ne revenait pas au Wallet. Maintenant « Continuer le paiement FedaPay » fait `window.location.assign(checkoutUrl)` (même onglet(;; un effet au montage parse `?wallet=recharge&status=approved&id=…` → ouvre la sheet Wallet + rafraîchit le solde (+bannière( puis `history.replaceState(…, pathname(` nettoie les params. `callbackUrl` reste `${origin}/?wallet=recharge`.
- 2026-09-09 06:40Z reconcile runtime: root cause du 500 = soudure SQL dans le guard admin (tokens fusionnés sans espaces( → Postgres « syntax error at or near ar ». Fix `b425a62` (espaces restaurés, bytes od-vérifiés(; re-press « Re-vérifier » → **2 confirmées** (`113034878`, `113034257`( à 06:37:13Z (3 total confirmed(; **17 inchangées** — old pre-reference intents (27 Aug–8 Sep(, `omni_recharge_id` absent chez FedaPay et/ou statut provider ≠ approved (probablement jamais payés/abandonnés(. `9e316ca`: reconcile renvoie désormais `skipped[]` (reason `not_approved`|`missing_reference` + providerStatus( → toast admin détaille. Route 401 sans session vérifié. Suite 313/313;; prod client hash `index-PmQViBWD.js`. NB **terminal word-corruption**: `and`→`et`/`idand` via CLI — toujours vérifier le SQL par bytes (script python créé via file_editor, approuvé par od( avant push; ne jamais écrire SQL sensible via heredoc CLI.
- As of 2026-09-09 06:15Z: encore 19 pending (FedaPay ne re-livre que sur action dashboard/manuelle(; appuyer sur « Re-vérifier » dans la Console admin (ou resend FedayPay dashboard( pour les créditer. Suite 313/313, prod hash `index-PmQViBWD.js`.
- 2026-09-09 07:00Z environment note: `node_modules` absent at session start in this sandbox → `npm ci` restored it (vitest + tsc present(; `npx tsc --noEmit` clean; `npm test` → **49 files / 313 tests pass (5.37s(**; `npm run build` → dist `index-Bw-YG3lI.js`; prod `omni.sparkafrika.online` serves **the same hash** (guardrail T-07d ✅(. Suite/prod both advanced from `index-PmQViBWD.js` — 313/313 remains current.
- Gate  ​6 arbitrage: les décisions **D-1…D-5**（ §4 re-audit（ sont le seul contrôle ouvert pour clôture「 Go with limits 」؛ D-4 palette pins = **RESOLVED** via T-1（ V-7d（（ push `898e88f`（（； V-7 register + plan local Gate-6 section MAJ 2026-09-09（ commits `9bf0549`（ docs-only（ + pending docs-only commit（.
- **V-8 dettes COH closure ( 2026-09-09, HEAD `7849140`;** 11/18 COH closed,6 restantes（ — recalibration code-vérité: les dettes listées étaient majoritairement **déjà closes** en code courant（ ( COH-02 rolepill gated par capabilities serveur;COH-04 scanner bouton+permission+visibility stop;COH-05 wallet prop réel+label honnête;COH-06 toggle wire;COH-07 label dynamique;COH-08 bouton disabled;COH-12 freshbar single-flow;COH-01/03 serveur lat/lng réels（. Travail neuf: `b17d6bf` (**dead `PendingAction.claim` + resume `(0,0)` océan retiré**（;`a3c8fd6` (**tiroirs desktop `role="dialog" aria-modal="false"` + `trapDrawerFocus`**（;`7849140` ( register docs（. Restantes: COH-09/11/13 `deferred`;COH-14/17 checklist/decision;COH-15 `watch` fondateur;COH-18 `manual` humain. **Preuve**: 331/331 tests,tsc,boundary;build `index-CS6Peqms.js`;prod JS+CSS===local（ T-07d ✅（. Gate  ​6 verdict fondateur「 Go with limits 」requis.（
- **NW-13a/b (2026-09-10, HEAD `87085ef`+`68b9107` live):** Entrée Seller **universel** — le switch expose Seller à tout compte connecté `eligibleRoles` (`if (sessionUser)` hors `sellerAvailable`); rolepill click Seller si pas encore vendeur ouvre la sheet seller **P2 « Bienvenue — espace vendeur »** (`SellerV13`) avec « Ouvrir la carte pour revendiquer » (`onMap` → `setSelectedId(null);setSheet('none')`) + « Créer une facilité » (hint honnête « arrive avec la spec Free/Pro »). Prod `index-CMHAUJH7.js` === local. **NW-13b spec Plans Free/Pro verrouillée fondateur**( amendée 2026-09-11(: bulk=crédits + catalogue vendeur illimité en Pro )**: D-E validé ( 1 facilité tous produits = ​1 établissement actif légitime (, multi-facilités = slots payés par zone de service (; D-F 3 types strictes fixe/mobile/digital + zone/rayon mobile (; D-G bulk = **crédits de volume** ( 3 crédits/mois Free (, **Pack bulk Pro = base mensuelle ≈ 100/mois** ( 1 besoin =1 bulk(,, 30 insuffisant(; consommables par demande multi-facilités (; D-H bonus ​20 USD verrouillé après ​3 ventes QR à **users distincts**; D-I auto-renouvellement Pro via wallet + opt-out (; D-J **facility slot ​1 000 XOF à confirmer**; D-K **Acheteur Pro maintenant**. Tranches planifiées: NW-13c création facility ( 3 types (, , NW-13d crédits bulk, NW-13e bonus confiance, NW-13g renouvellement auto (, NW-13h acheteur Pro, NW-13f analytics (, , NW-13i mobile money (, , NW-13j publicité. Tsc?? ( diff docs seul(; tests inchangés. **Git identity repo-local rétabli** (`openhands@all-hands.dev`) — la config globale est perdue en nouvelles sessions.

## NW-14( clôturé 2026-09-10, tranche « la recherche est le cœur — audit des flows + exposition UI »(: audit des 4 plaintes fondateur( recherche animation/fallback, recherche négligée, contraintes filtres, flows admin/operator/team( — constats **code-vérifiés**):
- **P0 recherche ( cœeur(**:** P0-A le fallback `createFallbackMap` est **orphelin** — écrit+testé mais jamais importé→ `mapStatus!=='ready'` bloque `computeSearchFlight`+reveal+stagger+pins; P0-B cinématique complete( monde→continent→pays→région→ville→cadrage, `labelForZoom`, 3 vagues, countmark( à rejouer sur fallback comme MapLibre; P0-C chips contraintes réels( rayon 1/5/10/25/100/0 + chips métier par rôle(.
- **P1 entrée vendeur réelle**( NW-13c…j(,, **P2 gouvernance admin/operator/team**( routes `team`/`invite` absentes( — **validation fondateur de l’ordre P0→P1→P2 effectuée 2026-09-11 — P0/P1/P2 VÉRROUILLÉS, code débloqué sur ces tranches**( NW-14 §4(.
- Docs-only, HEAD `aab6966` poussé; **Git token désormais opérationnel**( push OK(; Dossier: `docs/nature-way/omni-nw-14-audit-flows-recherche-2026-09-10.md`; handoff HQ `HO-OMNI-12`( `docs/founder-hq/handoff-receipt-HO-OMNI-12.md`(; plan/board MAJ 2026-09-11(. **P0 livré (2026-09-11):** P0-A fallback map surface (`952df28`(, P0-B countmark V1.3 (`5cd175c`(, P0-C chips rayon 1/5/10/25/100/Monde câblées `rayon_km` (`5cd175c`( — tsc/lint/build/boundary verts, 55f/347t; registre `docs/nature-way/omni-nw14-p0-evidence-register-2026-09-11.md` + plan § NW-14 P0. **Prochaine décision = fondateur: verdict Gate 6「 Go with limits 」; puis P1 ( NW-13c→d→e→g(.**


## NW-13c (P1-A, livré 2026-09-11, branche `omni-v2-rebuild`)
- **Facility creation serveur+UI** (décisions NW-13b D-A/D-B/D-F sp): **3 types strictes** `fixe`/`mobile`/`digital` ( migration `044_v2_facility_type_rayon.sql`: `facility_type` CHECK + `rayon_km` CHECK mobile ≤500 + lat/lng `DROP NOT NULL` = **digital sans point géo** ( additive, idempotente; **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 via Neon MCP** + registre `omni_schema_migrations` (checksum `454c66b5…`( + preuve INSERT digital/mobile puis cleanup(.
- **Repo** `createSellerFacility`: valide type/coords/rayon; **provisionne un slot free si absent** (`on conflict (account_id) where source='free'`, D-J(; trust `unconfirmed` (D-B(; **garde `seller_ready` retirée** → vendeur universel (D-A(: un compte 'new'/'buyer_ready' peut créer. `listSellerCatalogue` aligné (garde retirée + expose `facilityType`/`rayonKm`/`trustState`(.
- **HTTP**: `validateSellerFacilityCreate` pur ( coords requises fixe/mobile, rayon requis mobile, digital sans coords (, route `POST /api/v2/seller/facilities` → catch-all `availability.js?__path=` → `handleApi` ( vercel.json (.
- **Client**: `api.ts createSellerFacility` + types `FacilityType`, `CreateSellerFacilityResult`; `SellerCatalogueFacility` étendu.
- **UI SellerV13**: hint « arrive avec la spec Free/Pro » **remplacée** par formulaire minimal ( 3 types, nom, catégorie/description, adresse, coords si fixe/mobile + « Me localiser », rayon si mobile, **zéro point si digital** ( → POST → refresh → P3 (toast « complétez le parcours de preuve »(.
- **Preuve**: tsc clean; **55 files/360 tests** (+13: repo 5, http 6, api 2(; build `index-CBKUqllX.js`; boundary clean; 12 bundles serverless régénérés. **Migration 044 APPLIED canonical 2026-09-13 via Neon MCP** (registre `454c66b5…`, preuve INSERT digital/mobile clean(. Residues honnêtes restants: proof navigateur réel non exécuté (sandbox sans DB/Auth(, route HTTP create non exercée end-to-end en prod ( pas de push (; prod non poussé (guardrail T-07d(.
- Dossier: `docs/nature-way/omni-nw13c-facility-creation-evidence-2026-09-11.md`; handoff `HO-OMNI-13` ( `docs/founder-hq/handoff-receipt-HO-OMNI-13.md` (; plan/board MAJ. Gate 6 reste CLOSED (verdict「 Go with limits 」 2026-09-11(; Gate 7 Venture Lifecycle = watch.

## NW-13d (P1-B, livré 2026-09-13, branche `omni-v2-rebuild`)
- **Crédits bulk serveur** ( décision D-G verrouillée: 3 crédits/mois Free, Pro base mensuelle ≈ 100/mois; 1 besoin = 1 bulk; demande à N facilités consomme N; surplus rachetable en packs (: **migration `045_v2_buyer_credit_accounts.sql`** → `v2_buyer_credit_accounts` ( compteur mensuel: plan free/pro, monthly_quota 3/≈100, period_month YYYY-MM, credits_used, extra_credits packs; UNIQUE(buyer_account_id), FK v2_accounts ON DELETE CASCADE ( + `v2_availability_credit_ledger` ( audit: kind `bulk_debit`/`bulk_monthly_grant`/`pack_credit`/`reversal`, amount signé ≠0, reason, request_id FK SET NULL (; additive+idempotent (12 statements(; **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 via Neon MCP** + registre `omni_schema_migrations` (checksum `f8917577…`( + preuve live ( reset mensuel period_month→courant credits_used→0; pack extra 20 → remaining 23; débit → 22; 0 trace (.
- **Repo**: `getOrCreateCreditStanding` ( upsert v2_accounts + reset mensuel, quota free 3 ( , `getBuyerCreditSummary` ( lecture solde ( , `createAvailabilityRequest` débite **1 crédit par facilité** (`cardinality(facility_scope)`( dans le même guarded statement ( CTE `credit_spend` + `credit_ledger_insert` ( : **idempotence sans re-débit** (replay: request_insert vide → pas de débit (; garde race `is_new && !debited → InsufficientCreditsError`; pré-vérif `remaining >= cost` → `InsufficientCreditsError` (mapped HTTP 403 `INSUFFICIENT_CREDITS`(. Retour étendu `creditCost/creditsRemaining/monthlyQuota/plan`.
- **HTTP**: `GET /api/v2/buyer/credits` ( 401 sans auth / 200 summary / 404 pas encore de compte (; `POST /api/v2/availability` validé par validator pur `validateAvailabilityRequestCreate` ( uuid product/facility, quantity int ≥1, idempotency ≥8 (; catch `InsufficientCreditsError` → 403.
- **Client/types**: `getBuyerCreditSummary` + `BuyerCreditSummary` type; `AvailabilityResult` étendu ( creditCost/creditsRemaining/monthlyQuota/plan (.
- **UI BuyerFlowV13**: compteur « Crédits bulk — X restant(s) / quota » chargé au montage + après envoi; si `creditsRemaining <= 0` → bouton disabled + « Vos crédits bulk de ce mois sont épuisés — rechargez en packs pour continuer. »
- **Preuve**: tsc clean; **55 files/367 tests** (+7: repo 3, http 3, api 1(; build `index-1lygK87G.js`; boundary clean; 12 bundles serverless régénérés ( availability.js porte 11 hits crédits (. **Migration 045 APPLIED canonical 2026-09-13 via Neon MCP** (registre `f8917577…`, preuve live reset/pack/débit, cleanup 0 trace(. Résidus honnêtes: proof navigateur réel non exécuté ( sandbox sans DB/Auth (; achat packs réel ( tunnel paiement Mobile Money = **NW-13i**; `extra_credits` capacité servie, pas de vente (; plan buyer Pro ≈100 = **NW-13h** ( quota 3 free appliqué (; prod non poussé ( guardrail T-07d (.
- Dossier: `docs/nature-way/omni-nw13d-bulk-credit-evidence-2026-09-13.md`; handoff `HO-OMNI-13`; plan/board MAJ. **Prochaine: sur ordre fondateur push prod → proof navigateur + prod-hash; puis NW-13e.** Gate 6 reste CLOSED ( verdict「 Go with limits 」 2026-09-11 (; Gate 7 Venture Lifecycle = watch.

## NW-13d-2 (P1-B2, livré 2026-09-13, branche `omni-v2-rebuild` — amendement D-G « oui approuve go »)
- **Nouveau modèle de facturation bulk** ( remplace le débit 1/facilité de NW-13d (: vérif **manuelle (1 facilité) = 0 crédit**; **bulk = `ceil(N/100)` crédits SANS plafond** (1–100 → 1, 101–200 → 2, 2001–2100 → 21(; insuffisant → **`InsufficientCreditsError` avec « Missing M » chiffré** (pre-check ET race guard(; UX: sélection facilités **et/ou rayon** + coût en direct.
- **Repo**: `createAvailabilityRequest` ne débite plus ( `creditCost: 0`, pas de `credit_spend`/pre-check ( ; `createBulkAvailabilityRequest` ( `facilityIds[]` ≥2 distincts, `facility_scope`=array complet, débit `Math.ceil(N/100)` dans UN guarded statement ( CTE `credit_spend` + `credit_ledger_insert` raison `bulk-availability over N facilities` ( , produit publié dans **toutes** les facilités via `valid_selection`, paramétrisation **`(${uniqueIdList})::text[]::uuid[]` sans concaténation `sql.unsafe`** ( sécurité, ; rejet <2 facilités `AvailabilityPolicyError`; doublons rejetés; replay idempotent sans re-débit.
- **HTTP**: `validateBulkAvailabilityRequestCreate` ( ≥2 facilités uuid distinctes, quantity ≥1, idempotency ≥8 ( + route **`POST /api/v2/bulk-availability`** → 201; `INSUFFICIENT_CREDITS` 403 message exact « This bulk addresses N facility(ies) = K bulk credit(s). You have R. Missing M. Recharge… ».
- **Client/types**: `requestBulkAvailability` ( POST JSON `facilityIds[]` + header `Idempotency-Key` ( + type `BulkAvailabilityResult` ( requestId/productId/facilityIds/facilityCount/creditCost/creditsRemaining/… (.
- **UI TrunkAppV13** (sheet bulk refondu): encart coût live ( N sélectionnées = K crédits, solde, après-envoi, carte `.dash` + « il manque M » si insuffisant (; filtre rayon 5/10/25/50 km ( centre des bounds, helper `bulkKmBetween`/`bulkBoundsCenter` (; cases à cocher + « Tout cocher »/« Aucune »; bouton `Envoyer la demande bulk (N commerces = K crédit(s))` désactivé si vide **ou** insuffisant; **UN** `requestBulkAvailability` + poll groupé `getAvailabilityResponses`; multi-produits d'une facilité = demandes manuelles gratuites affichées en rows; produit de référence = premier produit avec stock des facilités ciblées. **BuyerFlowV13**: « Vérification manuelle gratuite », plus de blocage à solde zéro.
- **Preuve**: tsc clean; **55 files/375 tests** (+8: repo 5, http 4, api 1, adaptés 4(; build `index-5yeSIqD5.js`; boundary clean; 12 bundles serverless régénérés ( nouvelle route bulk (; **preuve canonique live Neon MCP T1-T5** ( manuel 0 / bulk 2→1 / refus 250 → `ceil=3` > remaining 2 → « Missing 1 » guard refusé 0 rows / cleanup 0 trace (; **aucune migration requise** ( 045 déjà appliquée supporte le scope array (.
- Dossier: `docs/nature-way/omni-nw13d2-bulk-cost-evidence-2026-09-13.md`; handoff `HO-OMNI-13`; plan/board/master-plan MAJ. **Prochaine: sur ordre fondateur push prod → proof navigateur (encart coût bulk( + prod-hash; puis NW-13e.** Résidus honnêtes: route bulk non exercée end-to-end en prod ( pas de push (; proof navigateur réel non exécuté ( sandbox (; achat packs réel = **NW-13i**; plan buyer Pro ≈100 = **NW-13h**; Gate 6 CLOSED maintenu.
## NW-13e (P1-C, livré 2026-09-13, branche `omni-v2-rebuild` — D-H « Bonus confiance »)
- **D-H verrouillée fondateur**: bonus **20 USD (10 000 XOF minor)** verrouillé, débloqué après **3 ventes transactions Omni à users distincts**; grant **non retirable** (kind `bonus_grant` dans le wallet v2); objets spec `seller_unlocks` + `pro_test_credit`.
- **Migration `046_v2_seller_bonus_trust.sql`** → `v2_seller_unlock_progress` ( PK (facility_id, buyer_account_id) = « users distincts », FK CASCADE, first_sale_at ( + `v2_seller_unlocks` ( UNIQUE (facility_id, unlock_type), unlock_type `pro_test_credit_20_usd` CHECK, distinct_buyer_count ≥0, required_count 3, status locked/eligible/granted CHECK, amount_minor 10000 CHECK >0, granted_at, index (facility_id, status) (; additive+idempotent; **preuve branche temp Neon `br-odd-credit-amrpqgev` T1-T5** (3 buyers distincts → 3 lignes même rejoué; upsert → eligible; re-upsert → granted préservé; CHECK `unlock_type` rejette `pro_bonus_bogus`; cleanup 0 trace) — **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 12:15Z ('registre `3c6b08bb…`')**.
- **Repo**: `submitTransactionRating` CTE réécrit — sur `closed_event`: `unlock_progress` ( INSERT ... ON CONFLICT (facility_id, buyer_account_id) DO NOTHING ( → `unlock_counts` ( count distinct ( → `qualified_facility` ( `qualifying_sales = least(3, count distinct)`, trust confirmed, `bonus_unlocked_at` si ≥3 ( → `bonus_grant` ( once, reference `facility-bonus:{id}`, 10000 ( → `unlock_object` ( upsert `v2_seller_unlocks`, granted préservé (; `getFacilityBonusStatus` ( authUserId = owner + lecture statut, état zéro locked si absent (; `unlockFacilityBonus` ( WalletPolicyError si !owner/!trust/<3/déjà granté/!wallet, `for update`, grant 10000 + marque `v2_seller_unlocks.status='granted'` + `granted_at` (.
- **HTTP**: `GET /api/v2/seller/facilities/:id/bonus` (statut( + `POST /api/v2/seller/facilities/:id/bonus/unlock` (grant owner(; `WalletPolicyError` → 409 `POLICY_REJECTED`.
- **Client/types**: `getFacilityBonusStatus` + `unlockFacilityBonus` + types `FacilityBonusStatus`/`SellerBonusUnlockState`/`FacilityBonusPersistenceResult`.
- **UI SellerV13**: bandeau « Bonus confiance » ( facilité active (: locked → « X/3 acheteurs distincts · 20 USD verrouillé (ventes QR) »; eligible → « 3/3 … débloqué » + bouton Débloquer; granted → « ✓ 20 USD crédités dans le portefeuille — merci pour votre confiance »; chargé via `loadBonus` au select de facilité.
- **Preuve**: tsc clean; **55 files/380 tests** (+5: repo 2, http 1, api 2; adaptés 2 rating CTE(; build `index-CpV0tuGd.js`; boundary clean; 12 bundles serverless régénérés ( routes bonus présentes (.
- Dossier: `docs/nature-way/omni-nw13e-bonus-trust-evidence-2026-09-13.md`; handoff `HO-OMNI-13`; plan/board/master-plan MAJ. **Prochaine: proof navigateur visuel bandeau bonus (session seller réelle) au prochain spot-check.** Résidus honnêtes: routes bonus réponse 401 owner-only vérifiées en prod (pas d'exercice complet end-to-end sans session); proof navigateur réel non exécuté ( sandbox (; `seller_unlocks` legacy supabase non branché ( le trunk v2 a son objet propre (; Gate 6 CLOSED maintenu.
## NW-13g (P1-D, livré 2026-09-13, branche `omni-v2-rebuild` — D-I « Renouvellement Pro auto »)
- **D-I verrouillée fondateur**: renouvellement Pro **automatique via wallet** + **opt-out explicite** du Seller + **rappel** avant expiration. Baseline scoping: `activateFacilityPro` créait un entitlement 30 j avec `renewal_opt_in=false`, **aucun** job d'auto-renew/toggle/rappel, et `commercial_plan='pro_expired'` **jamais posé** côté serveur (gap fermé).
- **Migration `047_v2_facility_pro_renewal.sql`** → `v2_facility_renewal_runs` (audit: facility_id FK CASCADE, prior/new_entitlement_id, spend_ledger_entry_id, run_at, status CHECK `(succeeded, insufficient_funds, skipped, failed)`, note, created_at, comment D-I, index `(facility_id, run_at desc)`(; additive+idempotent; **preuve branche temp Neon `br-divine-mouse-amlmqnmf` T1-T7** (shape 9 colonnes + comment + index; insert FK `succeeded` OK; CHECK `bogus` rejeté; plan véridique `pro_active`→`pro_expired` (dérivation entitlement actif/expiré); opt-in flip persisté; cleanup 0 trace) — **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 12:15Z ('registre `7d021a17…`')**.
- **Repo**: `getFacilityRenewalStatus` ( owner-only; **plan véridique** dérivé de l'entitlement `facility_pro` actif (state active + ends_at > now → `pro_active`; entitlement expiré → `pro_expired`; sinon `free`; expose `renewalOptIn`/`daysLeft`/`proPriceMinor`/`billingCurrency`/`walletBalanceMinor`/`sufficientFunds` (; `setFacilityRenewalOptIn` ( update dernier entitlement via lateral, `for update of f`, `WalletPolicyError` si aucun Pro activé (; `renewFacilityPro` ( UN guarded statement: wallet spend `facility_pro_spend` référence `facility-pro-renew:{facilityId}:{period}`, entitlement 30 j, `commercial_plan='pro_active'`, audit `succeeded`; sans opt-in/encore actif → audit `skipped`; opt-in + solde < prix → audit `insufficient_funds` (facilité **reste `pro_expired`**, retour `status:'insufficient_funds'`(, aucune trace de spend de rejet (; `getWalletOverview` requête + mapping mis à jour (plan véridique + `proEndsAt`/`renewalOptIn`/`daysLeft`(; bloc stale corrigé (retrait référence hors-scope `effective_spend`(.
- **HTTP** (3 routes owner): `GET /api/v2/seller/facilities/:id/pro/renewal-status`; `POST /api/v2/seller/facilities/:id/pro/renewal-opt-in` (body `{optIn}`(; `POST /api/v2/seller/facilities/:id/pro/renew`; 401/400; `WalletPolicyError` opt-in → 409.
- **Client/types**: `getFacilityRenewalStatus` + `setFacilityRenewalOptIn` + `renewFacilityPro` + types `FacilityRenewalStatus`/`FacilityRenewalOptInResult`/`FacilityRenewalResult`; `WalletFacilitySummary` étendu (+`proEndsAt`, `renewalOptIn`, `daysLeft`(.
- **UI SellerV13**: carte « Pro · renouvellement » ( facilité active ): plan + jours restants + toggle Auto ON/OFF (opt-in( + rappel « Solde insuffisant … » si opt-in et solde < proPrice + bouton « Renouveler Pro maintenant » si `pro_expired`; wallet sheet `Plans par facilité` montre jours restants + tag « renouvellement auto ON ».
- **Preuve**: tsc clean; **55 files/393 tests** (+12: repo 8, http 1, api 3(; build `index-DHes2HoI.js`; boundary clean; 12 bundles serverless régénérés (routes renewal présentes(.
- **Exécution ordre fondateur (2026-09-13)**: migrations **046 + 047 appliquées canonical `br-dawn-hill-am5amy22`** 12:15Z (registre `3c6b08bb…` + `7d021a17…`); push `628c8c4..a6a227a` sur `origin/omni-v2-rebuild`; Vercel auto-deploy → **prod sert `index-DHes2HoI.js` + `index-CXK07M8c.css` === dist local byte-identical (T-07d ✅)**; les 3 routes renewal répondent 401 owner-only (AUTH_REQUIRED propre) en prod; chaînes UI NW-13g présentes dans le bundle prod (`renouvellement auto ON`, `Pro · renouvellement`, `Renouveler Pro maintenant`).
- Dossier: `docs/nature-way/omni-nw13g-facility-pro-renewal-evidence-2026-09-13.md`; handoff `HO-OMNI-13`; plan/board/master-plan à MAJ. **Prochaine: proof navigateur UI visuelle (session seller réelle) au prochain spot-check; puis NW-13h acheteur Pro.** Résidus honnêtes: routes renewal réponse 401 owner-only vérifiées en prod (pas d'exercice complet end-to-end sans session); proof navigateur réel non exécuté ( sandbox (; job de rappel/renouvellement asynchrone automatique **hors scope P1** (le « auto-renew » s'exécute via `POST .../pro/renew` + audit, le rappel est affiché dans la carte Seller( = watch Gate 7; barème D-J Pro vendeur = **10 $ = 5 000 XOF/mois** (repo expose `proPriceMinor`/`billingCurrency`, DEFAULT prix pilote 1 000 XOF); Gate 6 CLOSED maintenu.
## HOTFIX (2026-09-13, commit `4400689`) — crash prod « r.on2 is not a function »
- **Symptôme fondateur**: `Uncaught TypeError: r.on2 is not a function` dans `index-DHes2HoI.js` (bundle NW-13g) — page carte cassée en prod.
- **Cause racine**: `TrunkMap.tsx` enregistrait le handler `style.load` via un alias **`on2`** qui n'existe QUE sur la surface fallback (`createFallbackMapSurface`). La vraie carte MapLibre n'a que `.on()`. Le garde `if (!isFallback)` était INVERSÉ vs la disponibilité : en fallback (qui a `on2`) on ne l'appelait pas, en vrai MapLibre (qui ne l'a pas) on l'appelait → crash au chargement de la carte vectorielle réelle.
- **Fix**: `if (!isFallback) (map as Map).on('style.load', configureStyle)` (guard inchangé), suppression des types morts `StyleEventOnly`/`MapEngineTypedOn`/`MapEngineWithOn2` et de l'alias `on2` mort dans `fallback-map-surface.ts` (decl + impl). tsc clean, **55f/393 tests ✅**, `check:boundary` clean, build `index-CULSrihA.js`.
- **Push + preuve**: poussé `a200af4..4400689` → Vercel auto-deploy → **prod sert `index-CULSrihA.js` === dist local (T-07d ✅)**; grep bundle prod `on2` → 0 occurrence; browser proof prod réelle (canvas + 200+ pins + dock Recherche/QR/Menu/Buyer, HTTP 200).


## NW-13h (P1-E, livre 2026-09-13, branche `omni-v2-rebuild` — D-K « Acheteur Pro »)
- **D-K verrouillee fondateur**: **Acheteur Pro maintenant** — favoris + comparateur first, recommandations ensuite. **Prix CORRIGE 2026-09-13 (base devise USD):** Seller Pro = **$10/mois**, Buyer Pro = **$5/mois**, affiche en devise locale (« 5 $/mois ≈ 2 500 F »). Source de verite `src/domain/pricing.ts` (USD minor sellerPro:1000/buyerPro:500; taux pilote 1 USD = 500 XOF → buyerPro=250000 minor, sellerPro=500000 minor). Les endpoints exposent `baseProPriceUsdMinor`+`baseBillingCurrency='USD'` (sticker) separement du prix charge (`proPriceMinor`/`billingCurrency` = XOF wallet). Wallet/ledger restent **XOF-only** (FedaPay, pilote Lome). Fallbacks `1000`/`250000` rebases sur derivation USD→locale; entilements deja charges gardent leur historique (coalesce). Fondateur greenlit defauts **(a)** pour 4 points d'arbitrage: (1) favoris persistants — table dediee `v2_account_favorites`; (2) activation = paiement wallet veridique — nouveau kind `buyer_pro_spend` + entitlement compte 30 j (`v2_buyer_pro_*`, miroir seller); (3) opt-out/rappel symetriques NW-13g; (4) plan credit bulk bascule `pro` (quota 100 -> `v2_buyer_credit_accounts.monthly_quota`).
- **Migrations**: `048_v2_account_favorites.sql` (favoris) + `049_v2_buyer_pro.sql` (CHECK ledger etendu `buyer_pro_spend`, `v2_buyer_pro_entitlements`, `v2_buyer_pro_renewal_runs`) — additive+idempotent. **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 16:01Z via `scripts/apply-migration.mjs`** (registre `048:1055b32c…` + `049:1819973a…`, re-run `already_applied`). Preuve temp `br-misty-field-amfc4pk8`; live canonical proof T1-T7 (favori/Spend/entitlement/credit/run; CHECK reject `buyer_pro_bogus`; cleanup 0 trace; **trigger append-only ledger disable->delete->enable `O`**).
- **Repo**: `listFavorites`/`addFavorite`/`removeFavorite`; `getBuyerProStatus` (plan veridique active/expired/free + daysLeft/opt-in/balance/sufficientFunds + **baseProPriceUsdMinor/baseBillingCurrency**); `activateBuyerPro` (credit acct pro quota 100 + spend derive USD→XOF `buyer_pro_spend` + entitlement 30 j); `setBuyerProRenewalOptIn`; `renewBuyerPro` (spend+30j+run succeeded/insufficient_funds/skipped; **fix stale accountId fallback**). `getFacilityRenewalStatus`/`activate`/`renew` seller + `getWalletOverview`: fallback `1000`→derive $10→local, exposent aussi base USD. 
- **HTTP** (apres GET /api/v2/buyer/credits): `GET/POST /api/v2/buyer/favorites` + `DELETE /favorites/:id`; `GET /buyer/pro-status`; `POST /buyer/pro-activate`; `POST /buyer/pro/renewal-opt-in`; `POST /buyer/pro/renew`; 401/400 + 409 POLICY_REJECTED. Dispatch catch-all `availability.js`.
- **Client/types**: `getFavorites`/`addFavorite`/`removeFavorite`/`getBuyerProStatus`/`activateBuyerPro`/`getBuyerProRenewalStatus`/`setBuyerProRenewalOptIn`/`renewBuyerPro` + 6 types.
- **UI** (TrunkAppV13): Sheet `favorites` + journeySheets; etoile (Star) facility header; menu buyer « Favoris »; sheet favoris (hcards + retirer); **carte Buyer Pro** wallet + **Plans sheet buyer** (prix USD→local avec etats + toggle opt-in + rappel); **quota comparateur** `openCompare` bloque >1 (free)/>5 (pro) + `compareBlocked` banner + bouton Passer Pro; **Plans seller** « $10/mois ≈ 5 000 F »; SellerV13 carte renouvellement « $10/mois ≈ F » (formatte correctement major→currency, corrige affichage raw minor).
- **Preuve**: tsc clean; **56 files/419 tests** (+14 repo, +1 http, +4 api, +5 pricing.test); build `index-CJVNhZiz.js`; boundary clean; 12 bundles regeneres (availability 7 refs buyer). **Migrations 048+049 APPLIED canonical, live proof 0 trace**.
- **PUSH PROD EXECUTE (ordre fondateur 2026-09-13)**: rebase sur `fb6aec4` (8 commits docs HQ reconcile d'une autre session — zero overlap code) → push `fb6aec4..520afeb` (`d54b4b3` NW-13h + `520afeb` pricing); Vercel auto-deploy ~80s; **prod sert `index-CJVNhZiz.js` + `index-CXK07M8c.css` === dist local byte-identical (SHA-256 `383db0a3…`/`33d04e3c…`, T-07d ✅)**; browser prod: carte + 200+ pins Lomé + dock Recherche/QR/Menu + rolepill Buyer — aucun crash (`r.on2` fix + pricing live). Residus restants: proof navigateur UI des sheets Pro (session rôle réelle) non capturee; recommandations = tranche suivante; packs credits = NW-13i; job async hors P1; source devise utilisateur = `OMNI_DEFAULT_LOCAL_CURRENCY` (XOF pilote, pas encore de champ pays/compte); Gate 6 CLOSED.
- Dossier: `docs/nature-way/omni-nw13h-buyer-pro-evidence-2026-09-13.md`; handoff HO-OMNI-13 MAJ.

## NW-13i (P1, livre 2026-09-13, branche `omni-v2-rebuild` — packs credit bulk via Mobile Money)
- **D-J « prix a fixer » NON verrouille fondateur**: le MECANISME (intent `pack` + webhook → `extra_credits`) est le livrable; la grille `BULK_PACKS` dans `src/domain/pricing.ts` est une **hypothese reversible** (XOF minor: starter 500 F = 10 credits, growth 1 200 F = 30, scale 3 500 F = 100 — ~50 F/credit). Vente du surplus = tunnel FedaPay (wallet/ledger restent XOF-only, pilote Lomé).
- **Migration `050_v2_bulk_credit_packs.sql`** (additive+idempotent, 3 constraints verifiees pg_constraint): recharge intents gagnent `purpose` (`wallet`/`pack` NOT NULL DEFAULT `wallet` CHECK + `pack_credits` int > 0 nullable + constraint `v2_wallet_recharge_intents_pack_purpose_check` = `purpose <> 'pack' OR pack_credits IS NOT NULL`); credit ledger gagne `recharge_intent_id` (uuid nullable) + **UNIQUE** `v2_availability_credit_ledger_recharge_unique` (idempotence rejeu) + FK `ON DELETE SET NULL`. **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 18:50Z via Neon MCP** + registre `omni_schema_migrations` (checksum sha256 `2a33e4f7…7afb`). Preuve temp `br-rough-lake-ame9ho7c` T1-T5 (free nouveau: confirmed + ledger recharge 50000 + pack_credit 10 + extra 10; **rejeu CTE 0 ligne extra reste 10**; UNIQUE rejette doublon; pro existant: merge extra 100 plan/quota intacts; cleanup branche supprimee).
- **Repo**: `getBulkPacks()` (source unique pricing, 0 SQL); `createBulkPackRecharge` (pack inconnu → `WalletPolicyError`, intent `purpose='pack'` + `pack_credits` + checkout FedaPay); `createWalletRecharge` etendu (`purpose`/`packCredits`); **`reconcileWalletRecharge` branche `purpose='pack'`** — confirmation atomique: ledger `recharge` (ref `fedapay:<txn>` on-conflict) + credit ledger `pack_credit` (ON CONFLICT `recharge_intent_id` DO NOTHING) + upsert `extra_credits` (SEULEMENT si pack_credit reellement insere) — replay-safe a 2 niveaux (repo + contrainte UNIQUE).
- **HTTP**: `GET /api/v2/buyer/credit-packs` (catalogue, auth) + `POST /api/v2/buyer/credit-packs` (achat, auth + header `Idempotency-Key`); catch-all `/api/v2/:path*` → `availability.js` (aucune conf vercel.json change); `POLICY_REJECTED` 409.
- **Client/types**: `BulkPack {id, credits, priceMinor, billingCurrency}` + `BulkPackPurchaseResult`; `getBulkPacks` + `purchaseBulkPack` (POST JSON + `Idempotency-Key`).
- **UI TrunkAppV13**: cardbox « Packs credits bulk » dans le Wallet (solde, liste des packs, bouton « Acheter X F » → checkout FedaPay `window.location.assign`, `packBuyingId` anti-doublon); CTA « Rechargez en packs » (textbtn) dans le sheet bulk quand `bulkCost.remaining < 0`; callback `?wallet=recharge` refait `openWallet` + refresh solde + packs + credits.
- **Preuve**: tsc clean; **56 files/424 tests** (+3 repo, +2 pricing); build `index-B1j70RJv.js`; boundary clean; 12 bundles serverless regeneres (`createBulkPackRecharge`/`getBulkPacks` dans availability.js).
- **PUSH PROD EXECUTE (ordre fondateur « go » 2026-09-13)**: rebase sur `fcd0b40` (3 commits docs HQ reconcile, zero overlap code) → push `fcd0b40..53567b8`; Vercel auto-deploy ~60s; **prod sert `index-B1j70RJv.js` + `index-CXK07M8c.css` === dist local (T-07d ✅)**; pack strings presentes dans bundle prod; **`GET /api/v2/buyer/credit-packs` → 401** sans session (route deploiee + garde auth). `r.on2` hotfix (4400689) intact dans le source (0 occurrence, `map.on('style.load')` ligne 775).
- Dossier: `docs/nature-way/omni-nw13i-bulk-credit-packs-evidence-2026-09-13.md`; board MAJ; handoff HO-OMNI-13. **Prochaine: sur ordre fondateur NW-13f analytics / NW-13j publicite (P2/P3) + proof navigateur wallet packs; D-J prix a verrouiller.** Residus honnetes: proof navigateur reel non execute (sandbox sans DB/Auth); achat Mobile Money reel (checkout FedaPay) = proof de confiance final = prochain spot-check fondateur; rejeu webhook reel (double event provider) a confirmer live; Gate 6 CLOSED maintenu, Gate 7 watch.

## NW-13f (tranche #6, livre 2026-09-13, branche `omni-v2-rebuild` — analytics vendeur Pro: conversion + provenance scans)
- **Promesse Pro analytics livree**: repo `getFacilityAnalytics` (owner-only) — **une** requete CTE **read-only** sur `v2_availability_requests/responses` + `v2_transaction_snapshots` + `v2_transaction_events` + `v2_qr_tokens` → entonnoir `requests`/`responsesAvailable`/`transactionsStarted`/`qrScansVerified`/`transactionsClosed` + `grossRevenueMinor` (sum net_amount des `closed`) + `scanToVerifyAvgMs` (avg verified_at−created_at, null si aucun scan); **aucune migration requise** (derivations sur tables existantes). Garde `SellerAuthorizationPolicyError` (join facility→account auth_user, message « Facility not found or not owned » — meme pattern que `getFacilityRenewalStatus`).
- **HTTP**: `GET /api/v2/seller/facilities/:id/analytics` (401 `AUTH_REQUIRED` / 409 `POLICY_REJECTED` non-owner / 200 owner). **Client**: `getFacilityAnalytics` + type `SellerFacilityAnalytics` (exporte serveur + fork client `types.ts`).
- **UI SellerV13**: cardbox **« Performance »** (Demandes recues / Transactions (clôturees) / Revenu `money(...)` / QR verifies + bouton Actualiser) chargee via `loadAnalytics` au `ws.selFacilityId` (auto = premiere facilite de `buildSellerWorkspace`); etats chargement/erreur best-effort (comme bonus/renewal).
- **Preuve**: tsc clean; **56 files/429 tests** (+3 repo: aggregation/zeros/non-owned; +1 http 409 boundary; +1 client endpoint); build `index-bYBBhTs2.js`; boundary clean; 12 bundles serverless regeneres (`getFacilityAnalytics` + `/analytics` dans `availability.js`); smoke navigateur preview boot sans crash + chaines UI presentes dans bundle servi.
- **PUSH PROD EXECUTE (ordre fondateur « go » 2026-09-13)**: commit `46fd0cd` rebase sur 3 commits HQ reconcile (docs-only) → push `e0cae30`; Vercel auto-deploy → **prod sert `index-bYBBhTs2.js` + `index-CXK07M8c.css` === dist local (T-07d ✅)**; `GET /api/v2/seller/facilities/:id/analytics` → **401** sans session; chaines « Performance »/« Demandes reques »/« QR verifies »/« Chargement des indicateurs » presentes dans bundle prod.
- Dossier: `docs/nature-way/omni-nw13f-analytics-evidence-2026-09-13.md`; board + master-plan MAJ. **Prochaine: NW-13j publicite sponsorisee (tranche #8, P3) sur ordre fondateur; proof navigateur UI cardbox Performance (session vendeur reelle) = prochain spot-check; proof 401/409/200 end-to-end en prod a confirmer.** Residus honnetes: analytics **par-facilite** (pas de vue marche globale, pas par-produit, pas d'export, pas de recommandations = tranches ulterieures); proof navigateur UI reelle non capturee (sandbox sans DB/Auth); Gate 6 CLOSED maintenu (« Go with limits »), Gate 7 watch.
## NW-13j (P3 #8, livre 2026-09-13, branche `omni-v2-rebuild` — Publicite sponsorisee IA, campagnes manuelles seules)
- **Promesse publicite livree (campagnes manuelles; IA reste « draft only » non construite)**: Free = campagnes indisponibles; **Pro = campagnes sponsorisees**; **budget pub = reserve depuis le wallet a la creation** (balance wallet != budget pub a l'usage) ; unimplemented → « Bientot disponible ». Migration **`051_v2_ad_campaigns.sql`** (additive+idempotente): table `v2_ad_campaigns` (PK uuid, `facility_id` FK CASCADE, name btrim 1..60, `budget_minor` >0, `spent_minor` 0..budget, `status` CHECK `planifiee/active/terminee/pausee`, `starts_at<ends_at` CHECK, `created_at`); 2 index (`v2_ad_campaigns_active_idx` par facility + partiel `v2_ad_campaigns_sponsored_scan_idx` sur (status, starts, ends) where status='active' pour le scan sponsorise). **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 via Neon MCP** (3 statements) + registre `omni_schema_migrations` (checksum sha256 `4df27a30…4c04`). Preuve temp `br-jolly-thunder-amr30jz7`: 8 constraints pg_constraint verifiees, index presents, scan sponsorship (active → boost, planifiee → non), CHECK bogus/zero/inverted rejetes, rejeu idempotent, cleanup 0 trace.
- **Repo**: `createAdCampaign` (owner-only join auth + `for update`, garde `commercial_plan='pro_active'` → `WalletPolicyError` « require an active Pro plan »; UN guarded statement: balance somme ledger `confirmed` (credit kinds recharge/bonus_grant/reversal/coupon_credit, sinon debit) >= budget, insert ledger `ad_spend` ref `ad-budget:{facilityId}:{startsAt}` ON CONFLICT do nothing (idempotent), insert campagne status live `active` si starts<=now sinon `planifiee`; no-row → probe: non-owner → `SellerAuthorizationPolicyError`, non-Pro/insuffisant → `WalletPolicyError`; `budgetRemainingMinor` = balance − budget). **`listFacilityAdCampaigns`** (owner-only: array_agg json campagnes desc + `budget_remaining_minor` = balance wallet courant).
- **Search**: `listPublicFacilities` LEFT JOIN `v2_ad_campaigns` (active + starts<=now + ends>now) → `min(camp.id) as sponsored_campaign_id` + `order by (count(camp.id)>0)::int desc, trust_state='unclaimed', name` (sponsorises d'abord); `toFacility` → `sponsored: boolean` sur `PublicFacility`.
- **HTTP**: `validateAdCampaignCreate` pur (name 1..60, budgetMinor >0, starts<ends, facilityId uuid, idempotency >=8) ; routes `GET` + `POST /api/v2/seller/facilities/:id/campaigns` (401/400/409 `POLICY_REJECTED`/201/200; `Idempotency-Key` requis).
- **Client/types**: `createFacilityAdCampaign` + `listFacilityAdCampaigns` (fork `types.ts`: `SellerAdCampaign`/`AdCampaignCreateResult`/`AdCampaignListResult`).
- **UI SellerV13**: cardbox **« Campagnes sponsorisees »** apres Performance (Pro-gated; formulaire nom+budget + « Lancer la campagne » + liste statuts planifiee/active/terminee + budget wallet restant; « Bientot disponible » pour fonctions futures). **Buyer TrunkAppV13**: badge **« Sponsorisé »** sur hcards résultats sponsorises.
- **Preuve**: tsc clean; **56 files/443 tests** (+6 repo create/list/errors, +6 validator, +2 api client); build `index-C0Gc3Psp.js`; boundary clean; 13 bundles serverless regeneres (`createAdCampaign`/`listFacilityAdCampaigns`/campaigns dans availability.js + facilities).
- **PUSH PROD EXECUTE (ordre fondateur « go » 2026-09-13)**: remote token rafraichi (GITHUB_TOKEN), rebase sur `d3de027` (HQ docs, zero overlap) → push `5cfb268`; Vercel auto-deploy ~2min → **prod sert `index-C0Gc3Psp.js` + `index-CXK07M8c.css` === dist local (T-07d ✅)**; `GET/POST .../campaigns` → **401** sans session; chaines « Campagnes sponsorisees »/« Sponsorisé » presentes dans bundle prod.
- Dossier: `docs/nature-way/omni-nw13j-ad-campaigns-evidence-2026-09-13.md`; board MAJ. **NW-13j CLOSE = tranche P3 #8 terminee — spec publicite manuelle livree. Prochaines: proof navigateur UI campagnes (session vendeur reelle) au prochain spot-check; verification prix D-J; Gate 7.** Residus honnetes: proof navigateur reel non execute (sandbox sans DB/Auth); aucune campagne creee en prod (attente fondateur); deficit programmatique AI ciblage = « Bientot disponible » par spec; Gate 6 CLOSED maintenu.
- **HOTFIXES NW-13j (2026-09-13, decouverts par preuve live vs prod `demo@seller.omni`)**: (1) **`18c22d5`** — `listFacilityAdCampaigns` 500 sur toute facilite: `array_agg(json_build_object(...))` retourne `json[]` et `coalesce(..., '[]'::json)` → « COALESCE types json[] and json cannot be matched » (pas seulement le cas zero-campagne). Fix: **`json_agg(...) filter (where c.id is not null)`** (json, coalesce propre). (2) **`0de9d8c`** — `listPublicFacilities` 500 sur TOUT appel: `min(camp.id)` n'existe pas (**Postgres n'a pas d'agregat min(uuid)**) → carte vide « service temporairement indisponible » + rail Seller workspace vide. Fix: **`(count(camp.id) > 0) as sponsored`** + `toFacility` lit le boolean (`row.sponsored`, fallback legacy `sponsored_campaign_id`). **Preuves live prod apres deploy**: `prove-nw13j-campaigns.mjs` 7/7 PASS (GET campaigns 200 + `campaigns:[]` + budget 0 XOF, POST 409 Pro-gate, 401 sans session); `GET /api/v2/public/facilities` 200 avec **206 facilites** incl. Omni Demo Seller Hub (`sponsored=false`); suite **56 files/445 tests**, tsc/boundary clean, bundles regenere avec les deux fixes. **Lecons authoring**: jamais d'agregat sur uuid (`min`/`max`); `json_agg` pour une valeur JSON simple (pas `array_agg(json_build_object)`); tester le SQL veritable contre la DB canonical (les stubs repo ne tombent pas sur `COALESCE types json[] and json`/`min(uuid)`) — reproduction directe via Neon MCP = preuve qui compte. Predicate `(count(camp.id) > 0)` remplace la colonne `sponsored_campaign_id` (supprimee).

## NW-15 P2 (P2-A + P2-B, livré 2026-09-14, branche `omni-v2-rebuild` — Gouvernance Admin/Operator/Team)
- **Référentiel équipes (plainte fondateur #4 : « les interfaces pour qu'un admin puisse faire d'un user operator / team »)** : migration **`052_v2_teams.sql`** (additive+idempotente) → `v2_teams` (name btrim 1..60 CHECK, zone, description, created_by_account_id FK SET NULL), `v2_team_members` (team_id FK CASCADE, account_id FK CASCADE, **UNIQUE (team_id, account_id)**, role_in_team CHECK lead|member, status CHECK active|revoked, added_by, revoked_at), `v2_team_invites` (team_id FK CASCADE, email, role_in_team CHECK, status CHECK pending|accepted|revoked, invited_by, accepted_at, revoked_at), **index UNIQUE PARTIAL `(team_id, email) WHERE status='pending'`** (1 pending/équipe/email mais **revoke-then-reinvite possible**, prouvé), 3 index support. **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-14** (7 DDL + 1 index, Neon MCP) + registre `omni_schema_migrations` (checksum `45994f6c…`). Preuve branches temp (`br-withered-glitter…`/`br-wandering-union…`): contraintes vérifiées, duplicate pending refusé, revoke→re-invite OK, accept OK, cascade delete OK.
- **Repo** (`trunk-repository.ts`): `listTeams` (UNE requête admin-guardée, `json_agg(row_to_json(…))` → **objets JSON propres**, `{authorized, data}` même à zéro équipe; jamais de cross-join vide → scoped scalar subqueries), `createTeam` (team_created), `inviteTeamMember` (email normalisé, team_invite_created), `revokeTeamInvite` (team_invite_revoked, motif ≥3), `setTeamMemberStatus` (team_member_active/revoked, role+status+motif validés) — tous CTE admin + audit `v2_audit_events`.
- **HTTP** (5 routes): `GET/POST /api/v2/admin/teams`, `POST .../teams/:teamId/invite`, `POST .../team-invites/:inviteId/revoke`, `POST .../teams/:id/members/:accountId/status` (401/400/403/200/201).
- **Client/api** (`api.ts`): `listTeams` (flatten l'enveloppe `{authorized,data}` → `TeamListResult`), `createTeam`, `inviteTeamMember`, `revokeTeamInvite`, `setTeamMemberStatus` + types `Team`/`TeamMember`/`TeamInvite`/`TeamListResult`.
- **UI AdminV13** (`src/trunk/AdminV13.tsx`): cardbox **« Équipe · Groupes »** — créer groupe (nom/zone/description), inviter par e-mail avec rôle Membre/Responsable, révoquer invitation, retirer/réactiver membre; toasts + reload.
- **Preuve**: tsc clean; **57 files/455 tests** (+10 `team-governance.test.ts`: list autorisé/vide/non-admin/mapping, create+audit, bounds, invite+audit+normalisation, email invalide, revoke+audit, status+audit, role/status invalides); boundary clean; **5 CTE repo exécutés LIVE sur branche temp avec vrai admin** (`7bd0f09d…`/`auth 6dfee45e…`) : create→invite→list (JSON propre)→member-status→revoke, audit écrit; build `index-9io-ht-T.js`.
- **PUSH PROD EXECUTE (suite « go next » 2026-09-14)**: commits `353ea59` (P2-A serveur) + `642205f` (P2-B UI); Vercel auto-deploy → **prod sert `index-9io-ht-T.js` === dist local (T-07d ✅)**; `GET/POST /api/v2/admin/teams` → **401 AUTH_REQUIRED** sans session; chaines « Équipe · Groupes »/« Créer le groupe »/« Inviter un compte » présentes dans bundle prod.
- Dossier: `docs/nature-way/omni-nw15-p2-teams-governance-evidence-2026-09-13.md`; handoff `HO-OMNI-15`. **P2-C restants : assignation zone team→facilities, invite accept flow, exposition « rendre opérateur » par compte.** Résidus honnêtes: cycle complet HTTP authentifié non exécuté (session admin fondateur requise — spot-check navigateur); pas de suppression d'équipe (soft via révocations); Gate 6 CLOSED maintenu.

- **HOTFIX NW-15 P2 (2026-09-14, commit `2a877b3`)**: chips « Équipe · Rôles » dans `AdminV13` rendus **désactivés en permanence** (rien ne se cliquait sur la section). Cause: garde `disabled` inversé dans `roleChip` — `active !== (desired === 'active')` est vrai (disabled) pour **les deux** états (`+Opérateur` sans rôle: false !== true = true; `−Réviseur` avec rôle: true !== false = true) → chaque chip disabled. Fix: `relevant = desired === 'active' ? !active : active` (ternaire explicite). Preuve prod: bundle `index-Dtl_6l8k.js` servi === dist local (sha `21a0fe1e…`, T-07d ✅); logique minifiée relevée dans le bundle prod (`disabled:...!(n===`active`?!r:r)`) — l'ancienne expression a disparu. 455/455 tests, tsc, boundary clean. Leçon authoring: ne jamais coupler `active !== (desired === 'active')` — écrire le ternaire en clair.

- **NW-15 P2-B2 (2026-09-14, commit `2e17594`, fondateur « go » sur la question email)**: exposition **identité réelle (email + name)** dans la section « Équipe · Rôles » de `AdminV13`. `listRoleManagementAccounts` fait maintenant un `left join neon_auth."user" u on u.id::text = candidate.auth_user_id` (id uuid → auth_user_id text) — renvoie `email` + `name` (nullable) dans `RoleManagementAccount` (type dupliqué serveur `trunk-repository.ts` + fork client `types.ts` mis à jour). **Proof SQL LIVE canonical `br-dawn-hill-am5amy22`** (via Neon MCP, requête exacte du repo avec admin `6dfee45e-a86a-4e57-b2fd-ae203aa5e309` = kheirlissi@icloud.com/Kheir Lissi): retourne kheirlissi@icloud.com (admin+reviewer), demo@seller.omni, demo@buyer.omni, juniorkheir@gmail.com (operator), + 5 comptes seed sans email (null) — `group by` étendu avec u.email,u.name OK. **Prod**: bundle client `index-BqG3Pcaj.js` servi === dist (sha `223e1fad…`, T-07d ✅); route `role-management` 401 sans session; 13 bundles serverless régénérés (12 files +~6 lignes chacun = 1 diff de la query dans le repo bundlé). 455/455 tests (test mapping élargi email/name + assertions `left join neon_auth."user"`), tsc, boundary clean. **Leçon authoring**: l'email réel des comptes Omni vit dans `neon_auth."user"` (pas dans `v2_accounts` qui n'a qu'`auth_user_id` — uuid opaque); join clé = `u.id::text = a.auth_user_id`. `emailVerified` est FALSE chez Neon Auth pour ces comptes (pas un signal exploitable). Résidu honnête: si on veut un email d'un compte `complete` (seed), il faut le récupérer ailleurs (le seed ne passe pas par Neon Auth). Spot-check navigateur admin = proof visuel complet.

## NW-15 P2-C (livré 2026-09-14, commit `b5c8868`, branche `omni-v2-rebuild` — accept d'invitation team + mission zones)
- **Acceptation d'invitation par l'invité** : routes `GET /api/v2/team/invites` (mes invitations pending/accepted/revoked) + `POST /api/v2/team/invites/:id/accept` (accepte → `v2_team_members` insert `(team_id, account_id, role_in_team)` + invite → `accepted` + audit `team_invite_accepted`; rejet si non destinataire = FieldPilotPolicyError). Repo `listMyTeamInvites` (owner-only, joint `v2_teams` pour `teamName`/`teamZone`) + `acceptTeamInvite` (UN guarded CTE, idempotent si déjà accepted → `TeamInviteAcceptResult`). Validators purs `validateTeamInviteAccept` dans `http.test.ts`.
- **Mission zone (admin)**: migration **`053_v2_facility_zone.sql`** → `v2_facilities.zone text` nullable + index partiel `v2_facilities_zone_idx ON v2_facilities (zone) WHERE zone IS NOT NULL` + comment (additive+idempotente). **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-14 via Neon MCP** (registre `omni_schema_migrations`, checksum `6793fa83…`), prouvée T1-T4 sur branche temp (colonne, index, ré-exécution idempotente, INSERT/UPDATE/DELETE). Route `POST /api/v2/admin/facilities/:id/zone` (admin-guardée, body `{zone: string|null}`, assigne ou retire, audit `facility_zone_assigned`). Repo `assignFacilityZone` (`for update of f`, trim, audit). Validator `validateFacilityZoneAssignment`.
- **Filtre « mission zone » (reviewer/operator)**: `listReviewQueue` + `listSellerActivationQueue` filtrent par zone quand le reviewer appartient à une équipe zonée — `not exists (équipe zonée) → tout visible`; `existe team.zone and t.zone = f.zone → visible`; équipe zonée + facilité hors zone → masquée. Aligné SQL réel testé dans `team-governance.test.ts` (`t.zone = f.zone` + cas facility zone NULL).
- **UI** : sheet **Compte** (`TrunkAppV13`) — charge `listMyTeamInvites` à l'ouverture (ref mono-shot), carte « Invitations d'équipe » avec bouton « Accepter » (busy par invite); `AdminV13` — champ « Zone de mission » + bouton **Zone** par item de la file de revue, zone actuelle pré-remplie (mapping `f.zone` ajouté à `ReviewQueueItem`).
- **Preuve**: tsc clean; **57 files/466 tests** (+11: filtre zone repo, validators invite-accept/zone, mauvaise autorisation); boundary clean; build `index-BwnpSj7_.js`; 12 bundles serverless régénérés. **PUSH PROD**: remote URL corrigée (`am-n-ra/lome-local-connect`, le token `ghu_…` était mort — remote avait un faux nom); push `e1adac0..b5c8868` → Vercel auto-deploy → **prod sert `index-BwnpSj7_.js` + `index-CXK07M8c.css` === dist local (T-07d ✅)**; routes `GET /api/v2/team/invites`, `POST .../accept`, `POST /api/v2/admin/facilities/:id/zone` → **401** sans session. Migration 053 déjà appliquée canonical avant push.
- Dossier/handoff: à compléter dans `docs/nature-way/` (suite HO-OMNI-15). **P2-C CLOSED → prochaine: P2-D (exposition « rendre opérateur » par compte via UI admin?)** Résidus honnêtes: cycle complet HTTP authentifié non exécuté (session fondateur requise pour proof navigateur); propagation jobs (cron) de renouvellement hors scope P1; Gate 6 CLOSED maintenu.

## NW-15 — Ré-audit cycle V1 + correction itinéraire (2026-09-14, HEAD `8c2133a` pushed, prod `index-B7nhYbvV.js` === local T-07d ✅)
- **Demande fondateur** : « V1 terminée ? » → ré-audit complet du cycle utilisateur réel (arrivée/discovery → recherche → contraintes → auth/onboarding → reprise → résultats/rail → bulk → fiche + panier → confirmation → intention → QR/chat/contact/itinéraire → verrouillage → avis) + **plan concret** avant déclaration V1. FW a aussi demandé une correction: **l'itinéraire vers le vendeur dispo de base sur chaque fiche facilité (sans intention d'achat)**; le contact reste après intention.
- **Audit livré**: `docs/nature-way/omni-v1-cycle-audit-2026-09-14.md` — 18 étapes fondateur, classement code-vérifié FULL/PARTIAL/ABSENT (§2), synthèse dettes (§3), **plan ordonné (§4: RAC-1…PRE-1)**.
  - **16/23 FULL** (discovery pins/nav, recherche, contraintes, auth+reprise, résultats/rail, bulk+crédits, choix fac, confirmation+3 branches, QR+chat, vendeur+verrouillage serveur `payment_declared→payment_confirmed`, avis, plans Pro, flow vendeur+team+admin).
  - **4 PARTIAL**: (1a) recentrage auto initial utilisateur non garanti; (3b/3c) onboarding 3 étapes minimal sans best-practices stats/email+nom; (7b) produit recherché pas mis en avant dans fiche; (7c) panier multi-produits = sélection par fac non persistante.
  - **2 BLOCKED (racine)**: (0b/9c) **contact vendeur ABSENT du modèle** — `v2_facilities` n'a aucun champ phone/whatsapp (001_v2_roots.sql), `claimant_phone` n'est que sur claim_requests (téléphone demandeur); impossible d'exposer le contact après intention tant que la donnée n'existe pas. (9d) itinéraire dans le flux après intention ajouté (voir correction). `BuyerProPlansModal.test.ts` = test legacy decommission, pas un composant manquant (aucune dette).
- **Correction itinéraire (commit `8c2133a`)** : `TrunkAppV13` — état `routeTarget` (RouteTarget type) + câblé à `<TrunkMap routeTarget onRouteClose>` + bouton **« Itinéraire vers ce vendeur »** sur la fiche facilité (carte Localisation, dispo sans intention, `setSheet('none')` pour voir le tracé); `BuyerFlowV13` — prop `onRoute` + bloc « Vendeur · Itinéraire » dans stage `txn` (après intention, chat reste dispo); `setFlowFacility` propage `latitude/longitude` depuis résultats/compare/fiche facilité (le resume onboarding n'a pas de coords — dette mineure acceptée). **Preuve**: tsc ✅, 57 files/**466 tests** ✅, boundary clean, build `index-B7nhYbvV.js`; **push poussé** (remote URL réparée avec `${GITHUB_TOKEN}`, token ghu_ précédent mort) → Vercel auto-deploy → **prod `index-B7nhYbvV.js` === build local (T-07d ✅)**; chaînes « Itinéraire vers ce vendeur »/« Itinéraire vers le vendeur » présentes dans le bundle prod.
- **Plan ordonné** (avant déclarer V1 terminée): **RAC-1** contact vendeur (migration `v2_facilities.contact_*` + API + UI fiche désactivée avant/active après intention) → **COR-0a/9d** itinéraire fiche/**flow** (fait) → **COR-1a** recentrage auto → **COR-3b** onboarding enrichi → **COR-7b** produit recherché mis en avant → **COR-7c** panier vendeur → **TEC-1** audit technique transversal → **PRE-1** preuve navigateur cycle complet 4 largeurs. **Aucun passage Gate 7 avant RAC-1 + PRE-1.**

## RAC-1 (livré 2026-09-16, commit `4f6d55f`, branche `omni-v2-rebuild` — contact vendeur après intention d'achat)
- **Demande fondateur** (Q): « fix this too — Uncaught TypeError: r.on2 is not a function » remonté sur le bundle `index-DHes2HoI.js:852` → root-cause traitée avec le livrable RAC-1 complet : le modèle omniait le contact vendeur (`v2_facilities` n'avait aucun champ phone/whatsapp), impossible à exposer côté transaction. Livré en intégralité (migration + repo + HTTP + API + UI + tests).
- **Migration `054_v2_facility_contact.sql`** : `v2_facilities` ADD `contact_phone text`/`contact_whatsapp text` nullable + CHECK `char_length(btrim(col)) between 5 and 40` chacun (additive+idempotente). **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-16 via Neon MCP** (registre `omni_schema_migrations` checksum `d6e4f7a1c2b3`), 207 facilités pré-existantes rétro-compatibles (NULL). Preuve T1-T5 sur branche temp `br-quiet-cell-amfzmzly` (colonnes, CHECK accepte/rejette, idempotence, UPDATE round-trip, rétro-compat) puis branche supprimée.
- **Repo** : `updateSellerFacilityContact` (owner-bound via `v2_accounts.auth_user_id`, trim, validé 5-40, `FORBIDDEN_OR_FACILITY_NOT_FOUND` sinon) ; `createSellerFacility` accepte `contactPhone`/`contactWhatsapp` (optionnels, INSERT colonnes) ; `getTransaction` joint `v2_facilities f` → `sellerContactPhone`/`sellerContactWhatsapp` exposés **uniquement dans le snapshot transactionnel** (membre-scoped — pas de fuite sur les fiches publiques) ; `listSellerCatalogue` expose `contactPhone`/`contactWhatsapp` au vendeur propriétaire.
- **HTTP** : validator étendu (contact optionnel 5-40) + route `PATCH /api/v2/seller/facilities/:id/contact` (401 sans session, 400 format invalide, 200 owner).
- **API client** : `createSellerFacility` (payload étendu) + `updateSellerFacilityContact`.
- **UI** : `BuyerFlowV13` — bloc « Vendeur · Itinéraire » du stage `txn` affiche désormais le **contact (tél + WhatsApp) après intention d'achat** ; `SellerV13` — formulaire de création avec Téléphone/WhatsApp optionnels + note « visible par les acheteurs qu'après une intention » + carte **« Contact vendeur »** éditable sur facilité active (Modifier → Enregistrer).
- **Preuve** : tsc clean; **57 files/474 tests** (+8 : repo 5 — getTransaction expose/null+contact, update owner-bound, rejet 4 chars, create accepte contact ; http validator 3) ; build `index-NE5OyC0H.js`; boundary clean; 12 bundles serverless régénérés. **Push** `9271b74..4f6d55f` → Vercel auto-deploy → **prod sert `index-NE5OyC0H.js` + `index-CXK07M8c.css` === dist local (T-07d ✅)**; route PATCH contact répond **401** sans session en prod ; chaînes « Contact vendeur » présentes dans le bundle prod.
- Dossier/handoff : à compléter (suite HO-OMNI-16). **RAC-1 CLOSED → prochain du plan ordonné : PRE-1 (preuve navigateur cycle complet 4 largeurs), puis COR-1a/3b/7b/7c.** Résidus honnêtes: proof navigateur réel non exécuté (sandbox sans DB/Auth — session fondateur requise) ; l'« erreur on2 » remontée est le symptôme du bundle intermédiaire `index-DHes2HoI.js` en cours de déploiement (le prod final ne la contient plus) ; Gate 6 CLOSED maintenu.
## COR-1a (livré 2026-09-16, commit `837e28c`, branche `omni-v2-rebuild` — recentrage auto initial garanti)
- **Gap code-vérifié (audit §2 1a PARTIAL)**: `TrunkMap` — `userPositionRef.current = userPosition` n'était assigné qu'au **render**; le callback géoloc posait `geolocationResolvedRef.current = true` **synchronement**, et `beginArrival` (poll 100 ms) lit `userPositionRef.current` dès ce flag → si React n'avait pas encore re-rendu, la ref était encore `null` → l'arrival `flyTo` visait la **position par défaut Lomé (1.22, 6.13)** au lieu de la position réelle. C'est exactement « recentrage auto non garanti ».
- **Fix** : `userPositionRef.current = nextPosition` écrit **avant** `setUserPosition(nextPosition)` dans le callback `getCurrentPosition` (réf. synchrone ⇒ la poll 100 ms lit la vraie position). Comportement conservé : si géoloc refusée/indispo → fallback Lomé (inchangé, sandbox = denied).
- **Seam testable** : `map-reveal.ts` — `arrivalTargetFor(user)` + `DEFAULT_ARRIVAL_TARGET` (helper pur, sans DOM) ; `TrunkMap` l'utilise pour calculer la cible d'arrivée (plus de `?? 1.22` inline).
- **Preuve** : tsc clean ; **57 files/477 tests** (+3 : position réelle récente ; fallback Lomé si null/NaN ; `(0,0)` traité comme position réelle) ; boundary clean ; build `index-6iG6vG06.js` ; preview local shell OK (pas de régression). **Push** `e4423a6..837e28c` → Vercel auto-deploy → **prod sert `index-6iG6vG06.js` + `index-CXK07M8c.css` === dist local (T-07d ✅)**.
- Dossier/handoff : à compléter (suite HO-OMNI-16). **COR-1a CLOSED → prochain du plan ordonné (§4) : COR-3b (onboarding enrichi) → COR-7b (produit recherché mis en avant) → COR-7c (panier vendeur) → TEC-1 → PRE-1.** Résidus honnêtes : la preuve navigateur *avec géoloc autorisée* n'est pas exécutable dans le sandbox (permission denied) — le contrat de cible est prouvé unitairement (`arrivalTargetFor`) et la preuve visuelle réelle reste pour PRE-1 (session fondateur) ; Gate 6 CLOSED maintenu.
## TEC-1 (livré 2026-09-16, commit `a601089`) — audit technique transversal
- **Objet** : lister les dettes **interface/technique/logique/métier** restantes (plan ordonné §4, juste avant PRE-1). Dossier `docs/nature-way/omni-tec1-technical-audit-2026-09-16.md`.
- **Constaté (code-vérité, 487/487 + tsc + boundary + build)** : **aucune dette bloquante** pour PRE-1. Les deux plus visibles — **T-1** arbre de routes TanStack **mort** (`src/routes/` 14 fichiers + `router.tsx` + `routeTree.gen.ts` + `start.ts`, **0 importeur vivant**; `main.tsx` monte `TrunkAppV13` directement) ; **T-2** **6 tests placeholder** qui n'assertent que `toBeDefined()` sur des composants **inexistants** (`BuyerProPlansModal`, `CompanyFacilityOnboardingModal`, `DirectInStoreScanSheet`, `OmniWalletModal`, `OnboardingModal`, `SellerScannerModal`) = fausse couverture. Autres : `src/core`/`src/integrations` 0 importeur, bundle mono 2,2 Mo, `seller_unlocks` legacy non branché, job Pro async hors scope P1.
- **Bug `r.on2 is not a function`** : **non reproductible** au HEAD courant — aucun pattern d'appel fautif dans le code vivant (vraisemblablement artefact d'un ancien bundle / extension). Résolu.
- **Recommandation** : **PRE-1** (preuve navigateur cycle 4 largeurs — session fondateur) → **V-9** nettoyage (supprimer routes mortes + tests placeholder, commit dédié) → **Gate 7 (terrain)**. Décision fondateur requise pour V-9.

## COR-3b (livré 2026-09-16, commit `275cc7c`) — onboarding enrichi
- **Gap (audit §2 3b/3c PARTIAL)** : `OnboardV13` = 3 étapes dont 1 formulaire, contenu éducatif minimal, nom non demandé.
- **Livré** : 4 étapes — (1) **étape éducative** (boucle V1 autoritative du maquette : Découvrir / Interroger / Vérifier & transiger + note confiance « la disponibilité n'apparaît qu'une fois confirmée »), (2) contact + **Prénom (optionnel)**, (3) code OTP, (4) plans (accroche personnalisée si prénom). Classes `docs/design.md` uniquement (`.cardbox`/`.kv`/`.btn.ok`) — aucun nouveau pattern d'interaction. OTP reste une **preview maquette** (pas d'auth réelle) : aucune fausse persistance ajoutée.
- **Preuve** : test **DOM réel** (`react-dom/client` + jsdom, `src/trunk/onboard.test.tsx`) — étape 1 éducative rendue, formulaire d'accès absent du 1er écran, reprise de la recherche + champ prénom présents. **58 files/479 tests**, tsc clean, boundary clean, build `index-D7cgtOkM.js`; push `46de7a2..275cc7c` → prod `index-D7cgtOkM.js` === local (T-07d ✅).

## COR-7b (livré 2026-09-16, commit `8d24c63`) — produit recherché mis en avant
- **Gap (audit §2 7b PARTIAL)** : après recherche, la fiche facilité était générique, le produit demandé noyé dans la liste.
- **Livré** : `searchedTerm` capturé à chaque `runSearch`; helper pur **`highlightSearchedProduct`** (`ui-helpers.ts`) — match exact (1000) > nom contient la requête (100) > mot significatif ≥3 lettres (10, stopwords FR exclus), tie-break sur nom le plus court; la fiche affiche le **badge « Produit recherché »** et **trie le produit matché en tête** (reste ensuite « en stock d'abord »). Style **monochrome** (`.status.ink` + inset `--ink-faint`) — l'accent `#2E8B6F` reste réservé à la confiance (règle design.md #3).
- **Preuve** : +3 tests contrat (match exact/token multi-mots, null si aucun match ou requête vide) — le cas « bureau » remonte bien `Bureau` et non `Chaise de bureau`. **482/482 tests**, tsc clean, boundary clean, build `index-zgFQEqso.js`; push `275cc7c..8d24c63` → prod `index-zgFQEqso.js` === local (T-07d ✅).
- **Reste du plan ordonné §4** : COR-7c (panier vendeur multi-produits) → TEC-1 → PRE-1 → **alors seulement Gate 7 (terrain)**. Résidu honnête : preuve navigateur réelle de ces 3 tranches groupée dans **PRE-1** (session fondateur, 4 largeurs) ; Gate 6 CLOSED maintenu.

## COR-7c (livré 2026-09-16, commit `eb508fa`) — panier par vendeur persistant
- **Gap (audit §2 7c PARTIAL)** : la sélection multi-produits `facProductSel` était **éphémère** (perdue dès qu'on quittait la fiche, reset à chaque `handlePinSelect`).
- **Livré** : module pur **`facility-cart.ts`** (`FacilityCarts = Record<facilityId, productId[]>`) — toggle/clear/prune/parse/serialize ; **sessionStorage** (survit aux sheets + rechargement d'onglet, oublié à la fermeture de session). `TrunkAppV13` : `facProductSel` dérivé du panier du vendeur courant, `handlePinSelect` **conserve** la sélection et **élague** les produits retirés (`pruneCart`), bouton **« Vider le panier »**, panier vidé après envoi bulk. Un panier **par vendeur** — jamais mélangé entre facilités.
- **Preuve** : +5 tests contrat (`facility-cart.test.ts`) — isolation inter-vendeurs, dernier produit retire l'entrée, clear ciblé, prune, round-trip + rejet de junk. **487/487 tests**, tsc clean, boundary clean, build `index-Coa4DsCu.js`; push `19ce2e2..eb508fa` → prod `index-Coa4DsCu.js` === local (T-07d ✅).
- **Reste du plan ordonné §4** : TEC-1 (audit technique transversal) → PRE-1 (preuve navigateur cycle complet) → **alors seulement Gate 7 (terrain)**. Résidu honnête : preuve navigateur réelle de COR-1a/3b/7b/7c groupée dans **PRE-1** (session fondateur, 4 largeurs) ; Gate 6 CLOSED maintenu.

## COR-1a (livré 2026-09-16, commit `837e28c`, branche `omni-v2-rebuild` — recentrage auto initial garanti)

## Domaine transaction — contrat + conception exhaustive (2026-09-16, HEAD `af806a1`)
- **Question fondateur** : « pas d'annulation d'une transaction en cours, verrouillage d'intention qui oblige à aller au bout, UI qui permet juste de sortir puis revenir — + timer/temps estimé par étape — et zéro dette logique/technique ».
- **Vérité code (sondée)** : la machine transactionnelle à 10 états n'a **aucun** état `cancelled/expired` — **l'annulation réelle n'existe pas** (conforme à l'intention). Solide : `UNIQUE(transaction_id, state)` (1 événement/état), append-only réel (triggers 003/041), **matrice de transitions appliquée serveur acteur-par-acteur** (scan QR = verrou, seller→`qr_verified` ; …), QR 10 min, ré-émission = remplacement.
- **Mais 10 dettes réelles trouvées** : **DT-1** stock `quantity_allocated_omni` **jamais réservé/décrémenté** (survente possible) · **DT-2** `v2_purchase_intents.state` **mort** (jamais muté) · **DT-3** aucune annulation de demande de dispo acheteur · **DT-4** QR 10 min hardcodé, pas de ré-émission/révocation · **DT-5** **aucun endpoint « mes transactions »** → transaction en cours **irrécupérable** · **DT-6** bouton de flux **« Annuler »** = `target:'none'` (sort vers la carte) + branche `handleDock('cancel')` **code mort** · **DT-7** aucun planificateur d'expiration · **DT-8** aucune notification de transaction (infra push existe) · **DT-9** litige schéma-only · **DT-10** aucun minuteur/estimation.
- **Modèle retenu** : Phase A **souple** (discovery→dispo→intention, sortie libre, cancel autorisé) / Phase B **dure** (verrou au `qr_verified`) ; notions **Suspendre / Reprendre / Expirer** — **jamais « Annuler »** après verrou. Types (fixe/mobile/digital/service) = variations de **champs de dispo + libellés**, **un seul Trunk** (pas de 2e machine à états).
- **Temps** : chaque étape porte **estimation + échéance + responsable** ; le temps **relance puis expire le non-verrouillé**, ne libère **jamais** le verrouillé (sauf auto-réception à confirmer). Compte à rebours dérivé des événements — **aucune nouvelle source de vérité temporelle**.
- **Décisions D-TXN-1…10 à confirmer** ; plan correctif **FF-1…FF-9** ordonné (FF-1 renommage trompeur → FF-2 « Transactions en cours » → FF-4 cancel Phase A → FF-3 expiration → FF-5 QR → FF-6 timer → FF-7 notifications → FF-8 réservation stock → FF-9 litige watch).
- Dossiers : `docs/nature-way/omni-transaction-flow-state-contract-2026-09-16.md` + `omni-transaction-domain-design-2026-09-16.md` + plan local `intra-skill-plan-NW-PROD-OMNI-TXN-01.md`. **Aucun code** tant que les décisions ne sont pas rendues (Root avant pixels).

## FF-1 / FF-2 / FF-6 (livrés 2026-09-16, commit `2408add`, branche `omni-v2-rebuild` — ordre fondateur « GO D-TXN-1…10 / FF-1…9 »)
- **Contexte** : fondateur a validé « oui go » sur le contrat transactionnel + plan correctif. GO exécuté dans l'ordre recommandé.
- **FF-1 (DT-6)** : la cible de dock `'cancel'` était **morte** (`target:'none'` + branche `handleDock('cancel')` jamais atteinte) et **mentait** (suggérait une annulation inexistante). Retirée du type `DockItem`, de `handleDock` et du call-site ; la pastille devient **« Quitter »** (icône `back`, sort vers la carte) **sans jamais annuler** la transaction. Le commentaire d'en-tête documente la règle.
- **FF-2 (DT-5)** : « sortir et revenir ». Nouvelle lecture **`GET /api/v2/buyer/transactions`** → `repository.listOpenTransactions` (member-scoped via `v2_transaction_members`, états **non terminaux** `current_state <> 'closed'`, plus récent d'abord, limit 50 ; jointures `v2_products`/`v2_facilities` pour le nom). Type client `OpenTransactionSummary`/`OpenTransactionsResult`, `api.ts listOpenTransactions`. UI : section **« Transactions en cours »** dans l'espace Buyer (reprise en un tap → `getTransaction` → stage `txn`, **sans repasser par la dispo**), entrée menu dédiée. Helper `startFlow` purge l'id de reprise résiduel à **tout** démarrage de flux neuf (5 sites).
- **FF-6 (DT-10)** : timer/estimation par étape. Module pur **`transaction-time.ts`** : libellé d'état FR, **responsable** (buyer/seller/system), âge relatif, **échéance par étape** (`STAGE_DEADLINE_MINUTES`) + `deadlineLabel`. Bandeau dans le suivi `BuyerFlowV13` : étape · qui agit · échéance + note « transaction verrouillée, non annulable, reprenable ». **Le temps relance, il n'annule jamais.**
- **Preuve** : **497/497 tests** (+10 : 8 `transaction-time.test.ts`, 2 repo `listOpenTransactions`), tsc clean, boundary clean, build `index-uGJ5kBBY.js`. **Réponse à l'erreur signalée** : `r.on2 is not a function` (bundle `index-DHes2HoI.js`, NW-13g) est **déjà corrigée** par `4400689` (ancêtre de HEAD + origin, identifiant `on2` absent de `src/` et du bundle courant) — vérifié en navigateur sur prod : chargement propre, carte rendue, aucune erreur console.
- **PUSH BLOQUÉ** : jeton GitHub du remote (`ghu_…`) **et** `GITHUB_TOKEN` rejetés (« Password authentication is not supported » / « Invalid username or token »). Commit `2408add` **local, ahead-1**, prêt à pousser dès qu'un jeton `repo` valide est fourni. Guardrail T-07d non franchi.
- **Reste du plan FF** : FF-9 (litige, watch Gate 7) ; **FF-8 réservation stock livré** (voir ci-dessous).
- **FF-8 réservation stock livré et poussé (2026-09-17, `b2ce397`→`65d81cb`)** — D-TXN-7. **Problème** : `quantity_allocated_omni` n'était qu'un filtre de recherche, jamais décrémenté → deux acheteurs pouvaient sur-vendre la même unité. **Modèle** : *réserver au verrou d'intention, libérer à l'expiration, décrémenter à la clôture*. Nouveau compteur `v2_products.quantity_reserved_omni` (int not null default 0, `check >= 0`) ; disponibilité réelle = `greatest(allocated - reserved, 0)` (plancher 0, jamais négatif). **Migration `db/migrations/055_v2_stock_reservation.sql`** (additive+idempotente : colonne + index partiel `v2_products_reserved_idx` sur les lignes >0) — **APPLIQUÉE sur la branche canonique `br-dawn-hill-am5amy22`** 2026-09-17 (⚠️ le tool MCP `prepare_database_migration` parente **toujours sur la branche par défaut** `br-bitter-math-amrlbym6`, PAS sur la canonique → appliquer à la main sur la canonique) + registre `omni_schema_migrations` (checksum `75640c2d…`), 16/16 produits à 0. **Code** : `createPurchaseIntent` garde anti-survente (`available >= quantité`, sautée en replay idempotent) + `product_reserve` ancré sur `fresh_intent` ; `sweepExpiredIntents` `stock_release` ancré sur `intent_expired` ; `submitTransactionRating` `stock_settle` ancré sur `closed_event` ; disponibilité projetée dans `toProduct`, catalogue vendeur, recherche par quantité, réponse vendeur. **508/508 tests**, tsc/boundary clean, prod `65d81cb` déployé (client JS inchangé = serveur pur).
- **BUG réel trouvé par la preuve E2E FF-8 (2026-09-17, `65d81cb`)** : dans `submitTransactionRating`, les CTE `rated_event`/`closed_event` relisaient `v2_ratings` **dans la même instruction** que l'INSERT de notation → sémantique de snapshot Postgres : la ligne insérée n'est pas visible → le **premier** appel de notation levait `TransactionPolicyError('Rating is available only after the Buyer confirms receipt.')` **tout en ayant persisté la notation** (l'acheteur devait réessayer). Correctif : CTE `rating_present` alimentée par le **RETURNING** de `inserted_rating` (+ fallback relecture pour les replays) ; `rated_event`/`closed_event` et le SELECT final la consomment. C'était la « race de visibilité → retry » notée en T-08, non diagnostiquée à l'époque.
- **BUG réel trouvé par la preuve E2E du cycle (2026-09-17, `e4948a5`)** : dans `createPurchaseIntent`, le bloc QR était **mort** — `qr_eligible` relisait `v2_transaction_snapshots` puis `v2_transaction_members`, lignes **insérées dans la même instruction** donc invisibles (même sémantique de snapshot) → l'INSERT `v2_qr_tokens` ne s'exécutait jamais, l'événement `qr_ready` non plus, et le retour `qrToken`/`qrExpiresAt` était **toujours null** (contrat mensonger ; l'UI prod contournait via un appel séparé `issueBuyerQrToken`). Correctif : `qr_eligible` consomme le **RETURNING** d'`intent_upsert` (qui porte déjà `transaction_id`+`buyer_account_id`). Le QR est désormais émis dans la même instruction que l'intention.
- **BUG réel — entrée Seller (2026-09-17, `471ef98`) — D-TXN-12** : le switch Seller (compte connecté **sans** facilité) menait à la **coquille d'un vendeur déjà installé** (barre demandes/facilité, bascule ON/OFF, boutons Catalogue/Offres, note portefeuille — tous vides/inutiles) au lieu de l'**entrée revendiquer/créer**. **Cause racine** : `getSellerCatalogue` renvoie un objet **toujours truthy** (`{facilities:[],products:[]}`) pour un compte sans facilité ; `SellerV13` décidait d'afficher l'espace de travail sur `hasData = catalogue !== null` → le bloc d'entrée (gardé par `!hasData`) n'était **jamais** rendu dès lors que le serveur avait répondu, et le rendu tombait d'emblée dans la coquille installée. La garde du switch (`!sellerAvailable`, dérivée de `onboarding_state === 'seller_ready'`) était **aussi le mauvais critère** (l'entrée vendeur doit s'ouvrir à tout compte connecté ; la garde Facility-before-Offer ne porte que sur publication/disponibilité). **Correctif** : `seller-workspace.ts` expose `hasFacility` (`facilities.length > 0`) et `claimable` (facilités publiques `unclaimed` non possédées) ; `SellerV13` affiche l'entrée sur `hasData && !hasFacility` (pas seulement au premier chargement) et **liste les facilités à revendiquer** avec passage direct au brouillon (`onClaim` → `startClaim`) ; la coquille est gardée par `hasFacility` ; le switch ouvre toujours la sheet seller. **Aussi corrigé** : « La facilité n'est pas sur la carte ? Créer » (fiche facilité) déclenchait un `alert()` **mort** → il ouvre désormais l'espace vendeur **directement sur le formulaire de création** (`startInCreate`). **Preuves** : `SellerV13.test.tsx` (rendu jsdom du composant réel) — **3/5 cas ÉCHOUENT si l'on rétablit l'ancienne garde `hasData`**, la régression est donc réellement capturée ; `seller-workspace.test.ts` couvre `hasFacility` + filtre `claimable` ; **515/515 tests**, tsc/boundary clean, prod `index-CZVMytUb.js` === local.
- **Preuve E2E du cycle transactionnel (2026-09-17) — `scripts/prove-v2-transaction-lifecycle.mjs`** (`npm run proof:transaction-lifecycle`) : pilote le **code livré** sur branche jetable à travers **tout le flux verrouillé**, jamais exercé de bout en bout ailleurs. **T1–T9 PASS** : intention + QR émis ; scan vendeur (`qr_verified`) ; déclaration puis confirmation de paiement ; mise en œuvre (`fulfilled`) **+ notification de la contrepartie (FF-7)** ; réception acheteur puis notation qui **clôture** (`closed`, intention `completed`) ; refus d'une transition hors séquence ; **refus d'annulation** de la demande une fois le flux engagé (D-TXN « pas d'annulation ») ; transaction clôturée non « reprenable » (`listOpenTransactions` vide). ⚠️ `v2_transaction_snapshots`/`v2_transaction_events` append-only → branche jetable.
- **BUG réel trouvé par la preuve E2E du cycle (2026-09-17, `2b33433`) — D-TXN-11** : « l'état courant d'une transaction » était relu par `order by created_at desc, id desc`. Or `v2_transaction_events.id` est un **uuid aléatoire** (`gen_random_uuid()`) et `created_at` = `now()` **fermé sur l'horloge de transaction** → deux états écrits dans la **même instruction** (ex. désormais `qr_ready`+`intent_created`, `rated`+`closed`) ont **des `created_at` identiques**, donc le tri par `id` est **non déterministe**. Conséquence réelle : après une notation, `intent_created` pouvait être relu comme état courant → **flux verrouillé rouvert** (déclaration de paiement acceptée avant le scan vendeur, transitions hors séquence). **Correctif** : migration **`056_v2_transaction_event_state_rank.sql`** — colonne **GÉNÉRÉE (stored)** `state_rank smallint` (intent_created→closed = 1→10 ; valeur inconnue = 0) + index `(transaction_id, created_at desc, state_rank desc)`. Colonne générée ⇒ toujours calculée, jamais fournie par un INSERT, **aucune écriture à modifier**. Les **11 lectures** d'état courant dans `trunk-repository.ts` départagent sur `state_rank` ; les lectures non transactionnelles (audit, notifications, entitlements) gardent le tri par `id`. **APPLIQUÉE sur la branche canonique `br-dawn-hill-am5amy22`** 2026-09-17 + registre `omni_schema_migrations` (checksum `660b5a9f…`) ; **migration validée sur branche jetable** (mapping 1→10, ré-exécution idempotente, relecture déterministe). **Leçon** : ne jamais départager un état par une clé aléatoire — utiliser un rang métier. Classe de bug apparentée à la sémantique de snapshot Postgres (lire dans la même instruction ce qu'on vient d'écrire), qui a frappé 4 fois cette session : notation (`65d81cb`), QR (`e4948a5`), et ce tri non déterministe.
- **Preuve E2E FF-8 (2026-09-17) — `scripts/prove-v2-stock-reservation.mjs`** : pilote le **code livré** (`createTrunkRepository`) sur une **branche jetable** créée depuis la canonique, avec fixtures réelles (2 comptes, 1 facilité, 1 offre publiée allouée 10) et nettoyage best-effort. **T1–T6 PASS** : réserve au verrou (10→reserved 2/dispo 8) ; replay même clé → même intention, **pas** de double réservation ; survente 9>8 **refusée** avec le message honnête « plus disponible, stock réservé » ; décrément à la clôture (allocated 10→8, reserved 2→0) ; replay de clôture idempotent ; libération à l'expiration (reserved 3→0, allocated intact 8). ⚠️ à retenir : `v2_transaction_snapshots`/`v2_transaction_events` sont **append-only** (trigger `v2_reject_append_only_mutation`) → impossible de backdater/supprimer : on pilote l'expiration par l'horloge du sweep (`now` futur) et on jette la branche. Commande : `FF8_PROOF_ALLOW_DISPOSABLE_BRANCH=1 FF8_PROOF_DATABASE_URL=<branche jetable> npx tsx scripts/prove-v2-stock-reservation.mjs`.
- **FF-3/FF-4/FF-5/FF-7 livrés et poussés (2026-09-17, `bb906a3`→`8f5258b`)** : FF-4 `12de520` (annulation acheteur d'une demande de dispo, POST `/api/v2/buyer/availability-requests/:id/cancel`, 401/409) ; correctif prod `cf1d07d` (soudure SQL `s.transaction_idand m.role` cassait `createPurchaseIntent`) ; FF-3 `5329bdf` (planificateur d'expiration `sweepExpiredIntents` + `submitTransactionRating` mute `intents.state='completed'`) ; FF-5 `2ecb8f9` (TTL QR borné 1–60 min via `resolveQrTtlMinutes`/`qrExpiryFrom`, ré-émission, révocation `revokeQrToken` + POST `/api/v2/qr-revocations` + UI « Ré-émettre/Révoquer ») ; FF-7 `f46330f` (`transitionTransaction` notifie la contrepartie : événement `transaction_turn` dédupliqué + livraison in_app + web_push si abonnement actif). **507/507 tests**, tsc/boundary clean.
- **BUG DÉPLOIEMENT CRITIQUE (2026-09-17, `8f5258b`) — à retenir** : `vercel.json` (FF-3) déclarait un cron **horaire** `0 * * * *`. Le plan Vercel Hobby n'autorise que des crons **quotidiens** → Vercel a **silencieusement cessé de déployer** à partir de `cf1d07d`. Conséquence : prod est restée ~50 min sur le bundle FF-4 `index-BXAIiF6A.js` alors que FF-3/5/7 étaient poussés (guardrail T-07d **non franchi**, détecté en pariant sur `GET /api/v2/cron/expire-intents` = 404, `qr-revocations` = 404, et `api.github.com/.../deployments` = dernier `cf1d07d`). **Correctif** : cron quotidien `0 3 * * *` + balayage opportuniste des intentions échues au moment de `GET /api/v2/buyer/transactions` (idempotent, non bloquant) pour ne plus dépendre du seul cron. **Après le correctif prod === local (`index-eWrnjSxm.js`)** ; routes vérifiées en prod : `qr-revocations` 401, `cron/expire-intents` 401, `buyer/transactions` 401. **Règle** : après chaque push, **toujours** comparer hash prod === build local ET vérifier que GitHub `deployments` a bien une entrée pour le commit (un cron refusé gèle la prod sans rouge).

## PRE-1 — preuve navigateur du cycle V1, 4 largeurs (2026-09-17, commit `c771a13`) — 80/80
- **Dernier item du plan ordonné §4 de l'audit cycle V1** (`docs/nature-way/omni-v1-cycle-audit-2026-09-14.md`) : **le plan §4 est désormais intégralement exécuté** — RAC-1 → COR-1a → COR-3b → COR-7b → COR-7c → TEC-1 → **PRE-1**.
- **Résultat : 20/20 PASS à 360 / 768 / 1280 / 1920 — soit 80/80**, console propre à chaque largeur. Preuve exécutée **sur prod** (`omni.sparkafrika.online`) avec un vrai navigateur.
- **Le harnais précédent rapportait 21 faux négatifs — dus à trois hypothèses fausses sur l'UI, pas à des défauts produit** : (1) les chips de contraintes sont en **divulgation progressive** (`constraintsOpen` s'ouvre à la frappe — les assertir sur la sheet vierge échouait **toujours**) ; (2) le libellé réel est « Itinéraire vers **ce** vendeur » ; (3) les résultats sont des `button.hcard` dans `section[data-sheet=results]`, et **un pin de carte porte le même nom** — cliquer le pin faisait **intercepter le clic par le canvas MapLibre**.
- **Requête de preuve passée de « coffee » (0 résultat) à « boulangerie »** (1 facilité confirmée, 3 produits) : une recherche vide prouvait la coquille de résultats vides, pas le cycle. **Ne jamais calibrer une preuve de cycle sur un jeu vide.**
- **Deux assertions d'honnêteté ajoutées** : `.chip.soon` porte bien `aria-disabled="true"` ; l'itinéraire **sans géoloc** est étiqueté `data-state="unavailable"` (« Position indisponible… ») au lieu d'un faux tracé — **c'est le contrat, pas un échec**.
- **Périmètre honnête** : le segment authentifié (intention → QR → paiement → verrouillage → avis) reste **BLOCKED** — il exige une session fondateur et est **attesté, jamais simulé**.
- Artefacts : `scripts/prove-v1-cycle-browser.mjs` + `docs/nature-way/pre1-proof/` (JSON + 8 captures). Handoff `HO-OMNI-17`.

## MapLibre glyphes — clusters en chiffres bruts (2026-09-17, commit `131d23a`) — en prod
- **Défaut réel en prod.** `tiles.basemaps.cartocdn.com` (style CARTO Positron) sert **toutes** les familles **sauf `Noto Sans Bold`** : cette plage renvoie **404 sans en-tête CORS** → MapLibre journalise « Rendering codepoint locally instead » et la couche `omni-cluster-count` **s'affiche en chiffres non formés**.
- **Piège n°1 — un helper présent mais jamais appelé** : `rewriteGlyphUrl` existait dans `TrunkMap.tsx` mais **n'était jamais invoqué** (0 référence dans le bundle prod), d'où l'impression trompeuse que « c'était déjà corrigé ». **Vérifier la présence dans le bundle servi, pas seulement dans le source.**
- **Piège n°2 — une réécriture par hôte est trop large** : elle aurait déplacé les glyphes OpenFreeMap, que cet hôte sert **correctement**.
- **Piège n°3 — dans la preuve, pas dans le produit** : matcher les glyphes sur `/fonts/.*\.pbf` **rate précisément toutes les requêtes réécrites**, car l'hôte de repli sert les glyphes **à la racine** (`fonts.openmaptiles.org/Noto Sans Bold/0-255.pbf`). La preuve passait **à vide** (`glyphRequests: 0`). Corrigé en matchant la **plage Unicode** (`/\d+-\d+\.pbf/`) — c'est ce qui a rendu la preuve **décisive**.
- **Correctif** : `createGlyphTransformRequest()` (`src/lib/maplibre.ts`), câblé dans `new Map({ ..., transformRequest })` (`src/trunk/TrunkMap.tsx`) ; ne réécrit **que** les requêtes glyphes quittant `cartocdn.com` → `fonts.openmaptiles.org`. **MapLibre passe `ResourceType.Glyphs === "Glyphs"` (G majuscule).**
- **Preuve A/B** (`scripts/proof-map-glyphs.mjs`) : prod **d'avant** échoue explicitement (CORS `Noto Sans Bold`) ; build corrigé → **5 requêtes glyphes toutes 200**, `boldServedFromWorkingHost: 1`, **0 erreur glyphes/CORS** ; **rejoué sur prod après déploiement → PASS**. Prod `index-Bjl18UB3.js` === build local (T-07d ✅).

## Règles de preuve acquises (2026-09-17) — à ne pas réapprendre
- **Un push ne prouve pas un déploiement.** Après chaque push : comparer **hash prod === build local** *et* vérifier l'entrée GitHub `deployments`. Un cron refusé par le plan Vercel **gèle la prod silencieusement** (cf. §BUG DÉPLOIEMENT CRITIQUE).
- **Une preuve qui ne peut pas échouer n'est pas une preuve.** Vérifier qu'elle **échoue** sur l'état d'avant le correctif (d'où l'A/B prod vs build corrigé) ; un compteur à `0` est souvent le signe d'un matcher faux, pas d'un succès.
- **Un helper présent mais jamais appelé ressemble à un correctif déjà en place** : vérifier la **référence dans le bundle servi**, pas la définition dans le source.

## Deux bugs live fondateur corrigés à la racine (2026-09-17, commit `5a31ebc`, en prod `index-DUw7RWKU.js`)
- **Signaux fondateur** : `Uncaught Cannot stop, scanner is not running or paused.` et `/api/v2/buyer/pro-status` → **409**.
- **Scanner — piège de classe : un `catch` n'attrape pas un `throw` synchrone.** `html5-qrcode` signale « pas en cours de scan » en **levant une chaîne** (`throw \`Cannot stop…\``) et **de façon synchrone** ; ce n'est **pas** une promesse rejetée. La chaîne `scanner.stop().then(...).catch(...)` de `SellerQrScannerSheet.tsx` **ne pouvait donc pas l'intercepter** : démonter l'écran avant que `start()` ne se résolve, ou un onglet masqué pendant le démarrage, s'échappait en **erreur non capturée**. `clear()` lève de la même façon (« Cannot clear while scan is ongoing ») et l'ancien `.catch()` appelait `clear()` **justement dans ce cas** → second throw. Correctif : `teardownScanner()` (export dédié) garde `stop()` **et** `clear()` indépendamment, tolère un `stop()` qui ne renvoie pas de promesse, et ne présume pas que `clear()` réussira.
- **pro-status — un code de statut qui accusait l'utilisateur à tort.** `getBuyerProStatus` levait `BuyerSearchPolicyError('ACCOUNT_UNAVAILABLE')` quand l'identité authentifiée n'avait **pas encore** de ligne compte. La couche HTTP mappait cette classe en **409 POLICY_REJECTED** — un code de **violation de politique** pour ce qui est une **précondition de provisionnement**. La lecture sœur `/api/v2/account/context` répondait déjà **403 ACCOUNT_UNAVAILABLE** pour la même condition : deux endpoints, même cause, deux codes. Correctif : le dépôt renvoie `null`, HTTP répond **403 ACCOUNT_UNAVAILABLE**, sur `pro-status` **et** `pro/renewal-status`. **Règle : séparer « l'utilisateur n'a pas le droit » de « l'état n'existe pas encore » — le premier est 403, le second ne doit pas être 409.**
- **Preuve** : 518 → **523 tests** ; les **5 nouveaux tests ont été vérifiés comme échouant contre le code d'origine** avant correction (un test qui ne peut pas échouer ne garantit rien). `tsc` 0 ; prod `index-DUw7RWKU.js` === build local (sha256 `f3727dce…`, T-07d ✅) ; garde-fou défensif confirmé **présent dans le bundle servi**.

## Itinéraire — diagnostic mesuré, gate sur décision fondateur (2026-09-17, handoff `HO-OMNI-18`)
- **Demande fondateur** : « notre façon de faire les itinéraires doit être aussi parfaite que ce que Google Maps propose et tout ». **Aucune ligne de code d'itinéraire n'a été écrite** — le gate est un **choix de fournisseur et de périmètre**.
- **Ce qui existe vraiment** : l'itinéraire actuel **n'est pas un itinéraire, c'est une ligne droite**. `TrunkMap.tsx` construit une `LineString` à **2 coordonnées** ; `routeDistanceLabel` calcule une **haversine** (vol d'oiseau) ; **aucun moteur de routage** dans `package.json`. À décharge : l'UI **l'annonce** (`(tracé direct)`) et passe en `data-state="unavailable"` (« Position indisponible… ») sans géolocalisation — **il n'y a pas de mensonge à corriger, il y a une source de vérité géométrique à remplacer.**
- **Écart mesuré sur le vrai réseau de Lomé** (OSRM démo) : Marché d'Adawlato → nord de Lomé = **4,91 km** en ligne droite contre **6,19 km** en route réelle, soit **+26 %**. La valeur actuelle **sous-estime systématiquement** distance, temps et coût de déplacement.
- **La couverture n'est pas le problème** : OSM couvre bien Lomé — rues nommées réelles remontées (Rue de L'Avenir, Rue Khra, Rue Kamé, Rue Slt Gnémégnah, Avenue Maman N'Danida, Boulevard de la Paix), turn-by-turn exploitable **en français**.
- **Le nœud est le fournisseur** : la politique d'usage du serveur de démonstration OSRM (FOSSGIS / `router.project-osrm.org`) **restreint l'usage aux cas raisonnables non commerciaux et à ≤ 1 requête/seconde**, sans garantie d'uptime ni de fraîcheur — une application commerciale où chaque utilisateur appelle le service **sort de ce cadre**. Voies réelles : fournisseur hébergé avec clé (OpenRouteService / GraphHopper / Mapbox) ou **OSRM auto-hébergé** pour le Togo. Dans **tous** les cas : **proxy serveur obligatoire** (la clé ne doit **jamais** partir dans le bundle client) + **cache** pour ne pas payer deux fois le même trajet.
- **Contradiction de spec bloquante, non résolue** : la maquette Species (S11/S22/S23/S25/S33) dit itinéraire **verrouillé jusqu'à l'intention** ; la correction fondateur du 2026-09-14 (`HO-OMNI-16`) dit itinéraire **ouvert sur chaque fiche facilité**, sans intention. **Le code suit le fondateur** — cohérent — mais **la maquette n'a jamais été réconciliée**, donc un futur contributeur peut « corriger » le code à l'envers. À trancher : D-ROUTE-3, puis amender les 5 écrans.
- **Hypothèses vérifiées du code** : `RevealPoint = Pick<PublicFacility, 'latitude' | 'longitude'>` — les entrées sont en `{ latitude, longitude }`, et c'est `routeFeatureCollection` qui **inverse** en `[longitude, latitude]` pour la `LineString` GeoJSON (`ROUTE_SOURCE = 'omni-v2-route'`) ; `routeDistanceLabel` est bien une haversine (rayon 6 371 km), formatée `m` sous 1 km sinon `km` à 1 décimale virgule ; `fallback-map-surface.ts` a `addSource`/`addLayer` en **no-op** → le repli DOM **ne sait pas afficher un tracé**.
- **Ce qui n'est pas prouvé** : aucune mesure de latence ni de coût du fournisseur retenu ; aucun test du réseau routier **via le code Omni** (uniquement par requête directe) ; aucune validation du tracé aux 4 largeurs avec une vraie géométrie.
- **Périmètre honnête à assumer** : « aussi parfait que Google Maps » **n'est pas atteignable** en open data (trafic temps réel, reroutage dynamique, qualité de géocodage ne sont pas fournis). Cible recommandée, écrite : **« itinéraire routier fiable et honnête à Lomé »**.
- **Point ouvert indépendant du fournisseur — vérifié en base (branche canonique `br-dawn-hill-am5amy22`, 2026-09-17)** : sur **206 facilités**, **6 seulement ont une adresse** = **2,9 %**. Même le meilleur moteur de routage produit un mauvais itinéraire sur un point mal géocodé — c'est une **dette de données Omni**.
- **Découverte en vérifiant** : **17 facilités sont à longitude négative** (`lng −1,0013 → −0,0041`) — c'est-à-dire **à l'ouest de Greenwich, au Ghana**, hors de la zone Lomé — toutes en `source_kind = 'public_import'`, **sans adresse**, et beaucoup nommées par des placeholders (« Unnamed public place », 8 sur 17). Extrêmes réels de la supply : lat **5,9172–6,4282** / lng **−1,0013–2,3912**. Ces points fantômes produiraient des itinéraires absurdes ; à nettoyer avant de juger la qualité du routage.
- **Artefacts** : `docs/nature-way/omni-route-directions-contract-2026-09-17.md` (contrat + D-ROUTE-1…5) ; `docs/nature-way/intra-skill-plan-NW-PROD-OMNI-ROUTE-01.md` (plan + RT-01…RT-09) ; `docs/founder-hq/handoff-receipt-HO-OMNI-18.md`.

## Itinéraires routiers réels — LIVRÉS (2026-09-17, directive « fix », commit `fa524f3`)
- **Le diagnostic ci-dessus était incomplet : le routage réel existait déjà et avait été orpheliné.** `src/components/omni/CartePage.tsx` et `src/routes/fiche.$id.tsx` appelaient OSRM avec `overview=full&geometries=geojson&steps=true` — donc un **vrai turn-by-turn**, avec `translateManeuver` en français. Mais `src/main.tsx` ne monte que `TrunkAppV13` : ces deux fichiers sont **du code mort**, et `src/routes/*` + `src/components/omni/*` avec eux. La ligne droite du tronc était donc une **régression de la reconstruction**, pas un manque initial. **Leçon : avant de conclure qu'une capacité n'existe pas, chercher un ancien chemin orphelin — « grep l'appel réseau » avant « concevoir » .**
- **`api/v2/**/*.js` sont des artefacts de build suivis par git** (`build:vercel-functions` via esbuild). Toute modification serveur doit être **rebuild + committée** avec le source, sinon la prod sert l'ancien bundle.
- **Aucune 13e fonction Vercel n'est nécessaire** : `vercel.json` a une réécriture **catch-all** `/api/v2/:path*` → `api/v2/availability`, qui redispatch sur le pathname (`__path`). Preuve qu'un endpoint non épinglé passe bien par là : `/api/v2/buyer/pro-status` répond 401, pas un 404 Vercel. Utile car le plan Hobby plafonne à **12 fonctions**.
- **Le routage ne doit pas dépendre de la base.** L'endpoint est servi **avant** `createTrunkRepository()`. Sans ça, une panne DB transformait un itinéraire valide en **500** — découvert en exerçant le handler, pas en lisant le code.
- **Piège JavaScript réel : `Number(null)` vaut `0`, pas `NaN`.** Une requête omettant ses coordonnées partait donc de **0,0** (Null Island) au lieu d'être rejetée. `coordinateParam` distingue l'absence d'une valeur ; l'absence renvoie **400**. **Règle : pour un paramètre numérique dont l'absence est invalide, ne jamais utiliser `Number(get(...))` avec un fallback `NaN`.**
- **Ne jamais router un point hors zone.** 17 facilités sont au Ghana (lng < 0) : router vers elles produit un résultat absurde. Garde de périmètre **au routage ET à l'import** (batch + unitaire), avec compteur `skippedOutOfZone` — un import qui écarte de la donnée **doit le dire**.
- **Le fournisseur ne doit jamais être appelé depuis le navigateur.** Vérifié sur le bundle : `router.project-osrm.org` **0**, `OSRM_BASE_URL` **0**, `route/v1` **0** dans le bundle client ; l'adaptateur n'existe que dans le bundle serverless. Le fournisseur se choisit par **`OSRM_BASE_URL`**, donc un OSRM auto-hébergé s'active **sans changement de code**. **Aucun repli sur la démo publique** (usage non commercial ≤ 1 req/s).
- **Preuve de bout en bout exigée, pas seulement des mocks** : handler complet → fournisseur réel = **5,4 km / 6 min, 181 points, 8 instructions** nommant de vraies rues de Lomé (« Boulevard du 13 Janvier », « Avenue Maman N'Danida »).
- **Preuve prod** : coordonnées absentes → **400** ; non configuré → **200 `PROVIDER_NOT_CONFIGURED`** ; Ghana réel → **200 `OUT_OF_ZONE`**. **545 tests** (523 avant), tsc + boundary clean.
- **Falsifications faites** : garde de zone/configuration neutralisée → **2 échecs** ; zone élargie au monde → **2 échecs** ; retour à `numberParam` → **3 échecs**.
- **RT-D1 TRANCHÉE par le fondateur le 2026-09-17 : « prenons mapbox comme fournisseur d'itinéraire » (commit `afffe29`).** Mapbox Directions est implémenté et poussé : `MAPBOX_ACCESS_TOKEN` sélectionne Mapbox, `OSRM_BASE_URL` reste supporté (auto-hébergement Togo) et **Mapbox gagne si les deux sont posés**. Il reste une action de **configuration** : poser `MAPBOX_ACCESS_TOKEN` dans l'environnement Vercel. Sans elle, la prod répond honnêtement `PROVIDER_NOT_CONFIGURED` et l'UI garde le repli **étiqueté `tracé direct`**.
- **Piège Mapbox à ne pas réapprendre : Mapbox renvoie « pas d'itinéraire » avec un HTTP 200** (`code: 'NoRoute'` / `'NoSegment'`). Se fier au statut HTTP seul ferait passer un échec pour un succès. Le `code` est donc vérifié séparément, et `TokenExpired`/401 remontent en `PROVIDER_ERROR` — jamais en ligne droite silencieuse.
- **Le trafic n'est pas activé par défaut, volontairement** : `mapbox/driving-traffic` facture sur un palier supérieur. Il faut l'opt-in explicite `MAPBOX_DRIVING_PROFILE=driving-traffic`. `language=fr` fait produire par Mapbox ses propres instructions localisées, meilleures que celles composées à partir des codes de manœuvre bruts (ce formateur reste le repli).
- **`access_token` ne doit jamais atteindre le client.** Vérifié sur le bundle construit : `MAPBOX_ACCESS_TOKEN`, `MAPBOX_DRIVING_PROFILE`, `api.mapbox.com`, `OSRM_BASE_URL` et `access_token` = **0 occurrence** dans le JS client, présents seulement dans le bundle serverless. **Le cache est clé par fournisseur** : sinon une bascule OSRM→Mapbox servirait une route de l'ancien moteur.
- **Quel jeton Mapbox, et pourquoi pas le « default public token » :** créer un **jeton public dédié (`pk.`)**, **pas** le jeton public par défaut du compte et **pas** un jeton secret (`sk.`). Mapbox documente que le jeton par défaut « is intended for use in development, and should be replaced with a new public token once your app is ready for deployment », et on ne peut y gérer ni scopes ni restrictions d'URL. Le jeton secret sert à créer/modifier des ressources du compte : inutile ici, Directions est une API en lecture. Scopes : garder les scopes publics standards (`styles:tiles`, `styles:read`, `fonts:read`) ; Directions fonctionne avec un jeton public standard et **je n'ai pas pu vérifier qu'un scope dédié « directions » existe** — ne pas partir à sa recherche. Piège si on édite les scopes : un jeton créé **sans** scope ne peut plus en recevoir ensuite.
- **NE PAS mettre de restriction d'URL sur ce jeton.** Les restrictions d'URL s'appuient sur l'en-tête **Referer/Origin du navigateur** ; or nos appels partent d'une fonction serverless Vercel **sans Referer de navigateur** → risque de **403 sur chaque requête**. (J'avais écrit l'inverse dans une première version de `.env.example` : c'était faux, corrigé.) La restriction d'URL n'est de toute façon pas notre défense : le jeton n'atteint **jamais** le navigateur, il ne peut donc pas être aspiré depuis le client.
- **RISQUE RÉEL TRAITÉ (2026-09-17) : `/api/v2/public/routing` était public, sans authentification ni limitation, et le fournisseur est facturé.** Le jeton ne fuit pas, mais n'importe qui pouvait appeler **notre** endpoint pour consommer **notre** quota Mapbox (le cache 5 min n'aide que sur des paires identiques). Ce n'est pas de l'exfiltration, c'est une facture : Mapbox facture à la requête. **Correctif livré** : migration `057_v2_route_requests` + `src/server/route-quota.ts` + `src/server/routing-gate.ts` — budget **60/heure et 500/jour par acheteur**, sur Mapbox uniquement. Restent à faire **côté compte Mapbox** : l'**alerte de budget** (Mapbox n'a pas de plafond de dépense).
- **`ALLOWED_ORIGIN` NE PROTÈGE PAS cet endpoint — ne pas s'y fier.** `Access-Control-Allow-Origin` est appliqué par le **navigateur**, pas par le serveur (`http.ts:276`, et `?? '*'` par défaut). Un appelant avec `curl`/script ignore le CORS et reçoit la réponse. Le CORS empêche un *site tiers* de lire la réponse depuis le navigateur d'un utilisateur ; il n'empêche personne de consommer notre quota. Dit autrement : notre propre endpoint est le proxy qui cache le jeton, mais rien n'y limite l'appelant.
- **Une limitation en mémoire NE SUFFIT PAS sur Vercel.** Chaque instance serverless a son propre compteur ; sans état partagé elle se contourne en parallélisant. Il n'y a **aucun** Redis/KV dans les dépendances. La route est servie **avant** toute base (volontaire, cf. `http.ts:279`). **Résolu sans nouvelle dépense** en réutilisant **Neon, déjà facturé**, comme état partagé (`057_v2_route_requests` + `route-quota.ts`) : la table ne contient que `(auth_user_id, horodatage)` — aucun itinéraire, aucune coordonnée — et est purgée à 48 h par le cron existant. Un compteur en mémoire aurait été **faux** (il aurait laissé passer N× la limite, N = nombre d'instances).
- **La règle d'accès est dérivée du coût, pas d'une politique uniforme.** `src/server/routing-gate.ts` : **Mapbox (facturé) ⇒ identité exigée + budget** ; **OSRM auto-hébergé (coût zéro) ⇒ aperçus anonymes conservés**. Le verrou d'intention est **opt-in** (`ROUTING_REQUIRE_INTENT=1`), **éteint par défaut** : il supprime tout aperçu d'itinéraire avant décision, ce qui peut coûter des conversions — or le budget borne déjà la facture. `ROUTING_ACCESS_MODE=never` rouvre l'accès anonyme en incident. Conséquence utile : **le jour où `MAPBOX_ACCESS_TOKEN` est posé, la protection s'active seule** — l'oubli de configuration dangereux devient impossible.
- **Panne de base ⇒ l'itinéraire PASSE (échec ouvert), volontairement.** L'itinéraire n'est pas un chemin monétaire et a été conçu pour survivre à une panne DB (servi avant `createTrunkRepository()`). Le compteur protège la facture, il ne doit pas devenir la raison qu'un acheteur ne voit pas son trajet. Un refus n'est écrit que sur **« pas d'intention vivante »**, jamais sur une erreur d'infrastructure. Le budget n'est débité **qu'après** une route réellement servie.
- **Piège de clé attrapé avant livraison : la route ne détient PAS d'`account_id`.** Elle n'a que le `sub` du JWT (`auth_user_id`, cf. `getAuthUserId`). Un schéma indexé sur `v2_accounts.id` avec clé étrangère aurait **cassé l'insertion** pour un acheteur sans ligne compte. `v2_route_requests.auth_user_id` est donc du **texte, sans FK** — un test verrouille ce point.
- **Tarif Directions (à confirmer sur le compte) : ~100 000 requêtes/mois offertes, puis ~2,00 $/1 000** (dégressif au volume). Ordre de grandeur : le quota gratuit absorbe ~3 300 itinéraires/jour. Au-delà, 100 000 requêtes coûtent ~200 $. Mapbox **n'a pas de plafond de dépense** : à surveiller par alerte de budget.
- **« Débloquer l'itinéraire après intent » : l'intuition est bonne, mais la protection réelle vient d'AILLEURS.** Aujourd'hui l'itinéraire s'ouvre à **deux** endroits : un bouton direct sur la fiche facilité (`TrunkAppV13.tsx:1597`, **sans** intent) et le callback du flux d'achat (`:1685`, après intent). Déplacer le bouton supprime le **chemin occasionnel**, mais **ne protège pas** l'endpoint : `routeTarget` est un **état client**, donc n'importe qui peut appeler `/api/v2/public/routing` directement avec des coordonnées de la zone pilote. Le vrai gain de « après intent » est **indirect et décisif** : exiger un intent force à **exiger une authentification**, et c'est l'authentification qui donne ce qui manque — **attribution et limitation par compte**, au lieu d'un endpoint anonyme. Donc : **le levier est `auth`, pas le bouton.** Le bouton reste utile (UX, honnêteté, moins d'appels), mais il ne faut pas le confondre avec la protection.
- **T-07d : pour vérifier la prod, viser `https://omni.sparkafrika.online`.** Les URL `*.vercel.app` citées dans d'anciens documents sont **mortes ou privées** (`omniview-6h67iweyd-…`/`omniview-j1rzpufmb-…` → **410 Gone** ; l'alias de branche `omniview-git-omni-v2-rebuild-…` est derrière le **SSO Vercel** et répond 302). L'hôte public qui sert réellement l'app et dont les routes `/api/v2/*` fonctionnent est **`omni.sparkafrika.online`**. Un board qui cite un hash de bundle ou un hôte sans date se périme silencieusement : `index-DUw7RWKU.js` était enregistré, la prod sert `index-BUMFRcnb.js`.
- **Un « bug live » peut être un écart de déploiement, pas de code.** Toujours trancher par un appel réel avant d'écrire du correctif : `git merge-base --is-ancestor <sha> HEAD` + `git branch -r --contains <sha>` pour savoir si le correctif est dans la branche **et** poussé, puis l'API en prod pour savoir s'il est **déployé**. Les deux bugs « live » du 2026-09-17 (`Cann…` scanner, 409 pro-status) étaient déjà corrigés **et** déployés (`5a31ebc`) — la vérification a évité de refaire un travail déjà fait.
- **`omni_schema_migrations` peut être en retard sur le schéma réel.** Sur le canonique, le registre s'arrête à `053` alors que les objets de `052`/`054`/`055` **existent**. Ne jamais conclure « migration non appliquée » depuis le registre seul : **vérifier l'objet** (`information_schema.tables`/`.columns`). Vérifié 2026-09-20 : `v2_route_requests` (`057`) **absente** du canonique.
- **Migration `057` prouvée sur branche jetable, puis annulée.** `create table v2_route_requests` = 3 instructions, 0 erreur ; 60 lignes pour A → `A_hourly=60`, `B_hourly=0` (comptage **par utilisateur**) ; purge > 48 h → 1 ligne supprimée et **compteur horaire vivant intact** (purger ne change jamais une décision de quota). **Piège Postgres :** un CTE qui écrit n'est **pas visible** du reste de la même instruction — lire un compteur dans la requête qui purge renvoie une valeur périmée. Sans effet sur le code (il ne fait jamais ça), mais ne pas s'y laisser piéger en vérifiant.
- **Variables d'environnement Vercel : trois pièges, et deux faux signaux à ne jamais utiliser.** (1) Ajouter une variable **ne suffit pas**, il faut redéployer : « *Any change you make to environment variables are not applied to previous deployments, only to new deployments* » — un déploiement est un artefact immuable. (2) **FAUX SIGNAL :** `last-modified` de `index.html` **n'est pas** une heure de build — il a suivi l'heure de ma requête (10:56:58 → 11:47:29), donc il ne prouve rien. (3) **FAUX SIGNAL :** le hash du bundle **reste identique** et c'est **normal** quand la variable est lue **côté serveur** — elle ne modifie jamais le bundle client, donc « hash inchangé » n'accuse pas un redéploiement manquant. **Le seul signal fiable est comportemental : comparer deux variables sur le MÊME déploiement.** Méthode qui a tranché le 2026-09-20 : `DATABASE_URL` (via `/api/v2/public/facilities` → **206 lignes** ⇒ lue) vs `MAPBOX_ACCESS_TOKEN` (via `/api/v2/public/routing` ⇒ **vide**). Conclusion : **le mécanisme Vercel fonctionne, seule cette variable n'arrive pas** ⇒ ni « pas redéployé », ni « variables cassées ». Restent (a) variable posée sur un **autre projet**, (b) **valeur vide** (une valeur blanche compte pour absente à cause du `?.trim()`), (c) **nom faux** (espace final / insécable, invisible à l'œil). Corollaire : **`reason` qui change de valeur est la seule preuve** que la variable est lue — préférable à tout hash ou horodatage.
- **Séparer « le secret est-il valide ? » de « la variable arrive-t-elle ? » — sans échanger le secret.** Un incident de configuration d'environnement se décompose en deux moitiés indépendantes : (1) le **secret** est-il accepté par le fournisseur, (2) la **variable** atteint-elle le processus. On tranche (1) en appelant le fournisseur **directement** avec le secret (`curl https://api.mapbox.com/directions/v5/...?access_token=…`) : une réponse valide ⇒ le secret est bon et **tout le défaut est côté plateforme** ; un 401/403 ⇒ le secret est en cause. On tranche (2) par un **test comportemental en prod** (voir la note sur les variables Vercel). Cela évite de demander au fondateur de coller un secret dans la conversation — **ne jamais accepter un secret dans le chat** : il est journalisé. Le repo fournit déjà les deux oracles côté serveur : le test `http-routing.test.ts` « requires sign-in once a billed provider is configured » (jeton posé ⇒ **401 `AUTH_REQUIRED`** sur appel anonyme) et `PROVIDER_NOT_CONFIGURED` (émis **uniquement** si jeton **et** OSRM sont vides, quel que soit `ROUTING_ACCESS_MODE`).
- **Ne pas suivre un tutoriel générique contre une architecture délibérée.** Le fondateur a transmis un guide `mapbox-gl-directions` plaçant `accessToken` dans la page. L'appliquer **casserait** une décision d'architecture : le jeton passerait **dans le bundle client** (aujourd'hui 0 occurrence), l'endpoint facturé serait exposé au navigateur, et le quota par acheteur (`RT-D1`/`057`) deviendrait **contournable**. Les tutoriels officiels sont corrects **en général** ; ici le proxy serveur + gate + quota est le choix retenu. Réflexe : quand une source externe propose une intégration directe, vérifier d'abord ce qu'elle **retire** (ici : le contrôle du coût).
- **Vérifier son propre raisonnement avant d'imposer une action au fondateur.** J'ai écrit « cause confirmée par les horodatages : il manque un redéploiement » — **c'était faux**, le fondateur a redéployé et rien n'a changé. Les horodatages HTTP n'étaient **pas** un signal valable. Leçon : quand un diagnostic n'a qu'**une seule** preuve indirecte, le présenter comme hypothèse, pas comme cause confirmée ; et chercher le test **comportemental** qui départage.
- **⚠️ Vercel déploie le COMMIT POUSSÉ, pas le HEAD local — vérifier `meta.githubCommitSha` avant tout diagnostic.** Cause racine du 2026-09-20, après avoir écarté à tort le jeton, le projet et le redéploiement : les déploiements de prod portaient `d492f77`, un **ancêtre** de HEAD mais **antérieur** à `afffe29`/`c4c8bef`. Le code déployé ne lisait **que** `OSRM_BASE_URL` (**0 occurrence** de `MAPBOX_ACCESS_TOKEN`). ✅ **Méthode qui tranche en 2 commandes :** `curl -H "Authorization: Bearer $VTOK" "https://api.vercel.com/v6/deployments?projectId=<P>&target=production&limit=5"` → lire `meta.githubCommitSha` ; puis, dans le dépôt, `git show <sha>:<fichier>` et `git merge-base --is-ancestor <sha-du-fix> <sha-déployé>`. **Corollaire :** une fonctionnalité peut être « en prod » d'après `git log` et **absente de l'artefact** si le commit n'est pas poussé. Toujours comparer **le sha déployé** au sha du correctif, jamais au HEAD local.
- **Diagnostiquer par élimination avec un accès plateforme, mais ne pas confondre « preuve d'un symptôme » et « liste de causes ».** Le 2026-09-20, ma preuve prod était **juste** (« la variable n'atteint pas le chemin d'exécution ») et ma **liste de causes était fausse** (mauvais projet / valeur blanche / nom caché). L'inspection Vercel a **écarté les trois** : clé de **19 caractères ASCII**, `target=["production"]`, bonne variable unique, bon projet. Tester chaque hypothèse **avant** de la présenter évite de faire chercher le fondateur au mauvais endroit. Le bon réflexe : d'abord établir **ce que l'artefact déployé contient**, ensuite seulement discuter la configuration.
- **Ne jamais réclamer un secret dans la conversation ; si un accès plateforme est nécessaire, demander un jeton d'API à portée limitée, et recommander sa révocation après usage.** Un jeton Vercel collé dans le chat y reste journalisé → le révoquer/régénérer. Et Vercel **ne déchiffre pas** la valeur des variables `sensitive` via l'API (`decrypted:false`) : inutile d'insister, et c'est une protection à connaître.
- **Lire `reason` en prod est un diagnostic, pas seulement un succès/échec.** `PROVIDER_NOT_CONFIGURED` est émis si le **fournisseur** résolu est nul. Selon le **code déployé**, cela peut vouloir dire « variable absente » (bundle récent) **ou** « ce code ne connaît même pas cette variable » (bundle ancien) — deux mondes différents. C'est pourquoi `reason` seul ne suffit pas : **confronter le message au code réellement déployé** (voir la note sur `meta.githubCommitSha`).
- **Pousser = déclencher un build Vercel ; vérifier par un changement de `reason`, pas par le sha.** Le 2026-09-20, l'ordre fondateur a été donné, 13 commits poussés (`d492f77..d4feddd`), Vercel a reconstruit en ~1 min, et la prod est passée de `200 PROVIDER_NOT_CONFIGURED` à **`401 AUTH_REQUIRED`**. C'est la **signature exacte** prédite par le test du dépôt quand le jeton est configuré : le remède **confirme** la cause racine. Boucle de vérification : `git push` → `GET /v6/deployments?projectId=…&limit=1` jusqu'à `READY` → **retester le comportement** (jamais se contenter du sha). **Piège d'auth rencontré :** une URL de remote `https://<token>@github.com/…` **périmée** déclenche une invite de mot de passe interactive qui **bloque** ; y remédier par `export GIT_TERMINAL_PROMPT=0` + `git remote set-url origin "https://${GITHUB_TOKEN}@github.com/<owner>/<repo>.git"` pour reprendre le jeton frais. **Le gate d'identité précède le contrôle de zone** : hors zone, un appel anonyme renvoie **401**, pas `OUT_OF_ZONE` — ne pas y voir une anomalie.
- **`RT-D1` : le jeton configuré n'implémente PAS le gate d'intention.** `routingGate()` renvoie `identity` par défaut dès que le fournisseur est `mapbox` ; **sans `ROUTING_REQUIRE_INTENT=1`, tout acheteur connecté peut demander un itinéraire**, en contradiction avec la décision « gate behind real purchase intent ». Et la migration `057` (`v2_route_requests`) étant absente, le quota par acheteur **no-ope silencieusement** (`recordRouteRequest` best-effort, `routeQuotaExceeded` fail-open). **Conséquence à ne pas manquer : la protection du coût n'est pas active** — donc « le routage marche » ne signifie pas « le coût est borné ».
- **La base « production » n'est pas forcément la base de l'application — identifier une base par ses DONNÉES, jamais par son nom.** Le 2026-09-20, la branche Neon nommée `production` portait **4** facilités / 3 comptes, tandis que la prod en servait **206** / 9 : l'application lisait `omni-v2-rebuild` (`br-dawn-hill-am5amy22`). L'outil de migration MCP visait la branche **par défaut** et aurait donc appliqué `057` au mauvais endroit **en silence**. Réflexe : compter une table connue (`v2_facilities`) sur chaque branche candidate et comparer au chiffre observé en prod avant toute écriture.
- **Un déploiement créé par l'API Vercel ne reprend pas l'alias canonique.** `POST /v13/deployments?forceNew=1` construit bien un déploiement `target=production`, mais `omni.sparkafrika.online` continuait de servir un build **antérieur** à la variable ; réassigner l'alias par API échoue (`not_found`). **Seul un push de commit** fait promouvoir le build par l'intégration GitHub. Vérifier par `GET /v4/aliases/<domaine>` → quel `deploymentId` sert réellement, au lieu de supposer que « REDY = actif ».
- **Ne pas activer un verrou produit « en connaissance de cause » : le code du dépôt peut déjà contenir l'avertissement.** `.env.example` disait noir sur blanc que `ROUTING_REQUIRE_INTENT` « supprime tout aperçu d'itinéraire avant engagement et peut coûter des conversions ». Une mesure en base a montré que le gate exige un **jeton QR vivant** alors que le TTL QR est de **10 minutes** (12 jetons, **0 vivant**) : le verrou refuserait des acheteurs **déjà engagés**. Lire l'avertissement du dépôt **avant** de poser la variable, pas après.
- **Reste hors autorité OpenHands :** **RT-D2** sort des 17 facilités hors zone.
- **Dette de données distincte, non réparée :** **2,9 %** des facilités (6/206) ont une adresse. Aucun moteur ne produit un bon itinéraire sur un point mal géocodé.

## Session 2026-09-24 — Species : monde peuplé, revendication, pin déplaçable, RT-D2 (✅ TOUT POUSSÉ)

- **Jeton GitHub : invalide en début de session, puis rafraîchi** (`401` → `200`). Les 4 commits ont finalement été **poussés** (`7e5e62c..4b8410d` sur `origin/omni-v2-rebuild`, vérifiés via l'API GitHub). **Leçon : le secret `GITHUB_TOKEN` peut expirer en cours de session ; retenter plus tard le fait fonctionner à nouveau** — ne pas conclure à une faute de configuration avant d'avoir retenté.
- **SP-7/SP-8 livrés (`7e5e62c`, POUSSÉ)** — constat honnête : la maquette avait **déjà** le pin niveau 0, la fiche `seller-claim` avec preuve + arbitrage (S-18), le choix créer/revendiquer et la couche d'intel (`admin-signal`). Deux écarts d'**exécution** cassaient la promesse fondateur : (1) **« le monde peuplé » n'était pas peuplé** → 3 lieux seulement, portés à **12** ; (2) **un lieu niveau 0 était une impasse** (toast mort, aucun chemin vers la revendication) → nouvel écran **`lieu-connaitre`** (statut Niv.0, « Géré par : aucune entité », **ce qu'Omni ne peut PAS dire** = dispo/prix/horaires, et les **deux gestes** claim (preuve + arbitrage) / create (immédiat)). Règle posée : *un lieu n'est jamais une impasse — c'est une invitation à revendiquer*.
- **SP-10 (pin déplaçable, `49f7ca5`)** — l'écran de création affichait `Lieu : Lomé · Adawlato` **en dur** : aucune carte, aucun pin, aucune géoloc. Livré : le pin **part de la position utilisateur** et **se déplace librement** (souris + tactile). **Un seul geste pour les trois formes** — Fixe (se corrige) / Mobile (se déplace) / Digital (dit *d'où l'on parle*) : le **sens** change (`Où vous êtes` ↔ `D'où vous parlez (origine)`), pas le geste. Cohérent Seed (« une seule offre, sept caractéristiques »), aucun type séparé. Limite honnête : un lieu déplacé par son propriétaire est **déclaré**, pas **vérifié** (badge S-31).
- **`b653253` — recherche de repère + S-15 :** (a) champ *« Chercher un lieu proche »* avec de **vrais noms relevés dans les tuiles de Lomé** (Cathédrale du Sacré Cœur, Grand Marché d'Adawlato, Église Saint-Antoine, Collège Notre-Dame, Tokoin-Hôpital, Amoutivé, Tokoin-Forever) — choisir un repère rapproche le pin (`Tokoin-Hôpital · à affiner`), puis on affine au doigt. (b) **BUG RÉEL attrapé par la preuve** : `marche` → **0 résultat** (comparaison sensible à l'accent) — pour un utilisateur togolais qui tape sans accent c'est un défaut réel ; corrigé par **normalisation NFD** des deux côtés (`marche`→Grand Marché, `eglise`→Église, `cathedrale`→Cathédrale, `tokoin`→2 quartiers, `ecole`→Collège, **5/5**). **Leçon : la preuve par le rendu trouve ce que la lecture du code ne montre pas.** (c) **S-15 — « les cartes ont la route par défaut ? » NON, vérifié** : OpenFreeMap = tuiles seules ; CARTO = routage = **produit séparé** (partenaires TomTom/Mapbox/HERE) et ses conditions **interdisent** l'usage *turn-by-turn navigation*. Un fond de carte est une **image** du monde ; un routage est un **calcul sur un graphe**.
- **`30bee51` — RT-D2 (décision fondateur) :** *« pas de vol d'oiseau — vrai itinéraire avec guidage vocal, accessible uniquement après intention réelle d'achat »*. **Clôt `RT-D1` et la contradiction `D-ROUTE-3`** : la maquette Species (S11/S22/S23/S25/S33) **avait raison** (verrouillé jusqu'à l'intention) ; la correction 2026-09-14 (itinéraire ouvert) est **remplacée**.
  - **Le routage réel EXISTE déjà en code** (`routing-adapter.ts` : Mapbox `language=fr` + OSRM) — il est éteint par **configuration**, pas par manque de code. Trois variables réalisent la décision : `MAPBOX_ACCESS_TOKEN`, `MAPBOX_DRIVING_PROFILE=driving-traffic` (trafic = palier facturé, opt-in), `ROUTING_REQUIRE_INTENT=1`.
  - **⚠️ PIÈGE CRITIQUE À CORRIGER AVANT D'ACTIVER LE VERROU :** `hasLivePurchaseIntent` (`trunk-repository.ts:5066`) exige un **jeton QR VIVANT** (`q.expires_at > now()`), **TTL 10 minutes**. Mesure en base : **12 jetons QR, 0 vivant**. Le verrou, activé tel quel, **dirait non à tout le monde** — y compris un acheteur **déjà payé et scanné**. **L'intention (décision d'achat, ~24 h) ≠ le QR (geste de vérification, 10 min)** ; les confondre ferme la fonction au moment utile. **ORDRE NON NÉGOCIABLE : corriger d'abord, activer ensuite.**
  - **Guidage vocal** : instructions du fournisseur (déjà exposées dans `steps`) + voix **client** `speechSynthesis` (**coût 0**). **Limite honnête : disponibilité de la voix FR sur Android réel NON prouvée** → essai appareil avant de promettre ; sinon itinéraire **consultable avant de partir**, jamais un guidage en mouvement.
  - **Coût** : ~100 k/mois gratuits (~3 300/j), puis ~2 $/1 000 ; **Mapbox n'a pas de plafond** → **alerte de budget obligatoire**. Le **verrou d'intention est aussi un garde de coût** — `S-15` satisfait **par le comportement**. Quota par acheteur = migration `057` (vérifié **absente** du canonique).
  - **Ordre** : RT-1 (corriger le verrou, **code**) → RT-2/RT-3 (config) → RT-4 (voix + essai Android) → RT-5 (budget + `057`) → RT-6 (maquette : itinéraire verrouillé + guidage vocal).
## Session 2026-09-24 (suite) — JETON MAPBOX ARRIVÉ + `RT-1` CORRIGÉ ET DÉPLOYÉ

- **Le jeton Mapbox posé dans Vercel ARRIVE bien** — prouvé **par le comportement**, pas par un hash : `GET /api/v2/public/routing` anonyme est passé de **`200 PROVIDER_NOT_CONFIGURED`** à **`401 AUTH_REQUIRED`**, la **signature exacte** prédite par le test du dépôt quand le jeton est configuré. **Le routage réel est ARMÉ en prod.** (`DATABASE_URL` sur le même déploiement répond 206 lignes ⇒ mécanisme Vercel sain.)
- **`RT-1` CORRIGÉ (`3cbd9e2`, POUSSÉ, DÉPLOYÉ)** — le piège annoncé s'est bien activé avec le jeton :
  - `hasLivePurchaseIntent` exigeait un **jeton QR vivant** (`q.expires_at > now()`, TTL **10 min**) **en plus** de `state='active'` → un acheteur **déjà payé et scanné** était refusé dès que le QR avait 10 minutes, c'est-à-dire **presque toujours**. Mesure : **12 jetons QR, 0 vivant** → le verrou aurait dit **non à tout le monde**.
  - **Modèle réel, vérifié — corrige une erreur de ma propre doc :** l'intention n'a **pas** d'`expires_at` ; sa durée de vie **EST** son `state` → **60 min** d'inactivité puis `expired`, et **une intention vérifiée par QR n'expire jamais** (reste `active` jusqu'à la clôture). La doc RT-D2 disait « ~24 h » : **faux**, corrigé.
  - **Correctif :** critère = **`pi.state = 'active'`** seul. On penche du côté de l'acheteur — même philosophie que « une panne de base ne ferme pas un itinéraire ».
  - **Preuve falsifiée :** le nouveau test **échoue sur l'ancien code** (1 échec, `not.toContain('v2_qr_tokens')`) et **passe sur le corrigé** (146/146, suite **588/588**). Bundle serverless régénéré — `hasLivePurchaseIntent` ne joint plus `v2_qr_tokens` (vérifié dans `api/v2/availability.js`).
  - **Prod :** `index-CYyAv6ci.js` === build local + **entrée de déploiement GitHub pour `3cbd9e2`** ⇒ **T-07d ✅** (un push ne prouve pas un déploiement : les deux vérifiés).
- **Jeton vérifié NON FUITE** : **0** occurrence de `MAPBOX_ACCESS_TOKEN` / `MAPBOX_DRIVING_PROFILE` / `api.mapbox.com` / `OSRM_BASE_URL` / `access_token` dans `dist/assets/*.js` ; présent **seulement** côté serverless (7 fichiers).
- **`RT-5` — CLOS vérifié le 2026-09-24 : `057_v2_route_requests` est DÉJÀ appliquée.** La note précédente (« migation `057` absente du canonique ») était **fausse/périmée** — vérifié par l'objet et le registre sur `br-dawn-hill-am5amy22` (la canonique, identifiée **par ses données** : 206 facilités / 9 comptes = exactement ce que sert la prod). `v2_route_requests` existe avec ses 3 colonnes (`id`/`auth_user_id` text/`occurred_at`), son index `v2_route_requests_user_time_idx`, et `omni_schema_migrations` la liste. **Registre complet `044`→`059`, aucune dérive** (les entrées legacy 001–0xx sont hors registre par design). `058`/`059` présents : `v2_entities` (3 lignes), `v2_facilities.entity_id` + index + **3 facilités liées** (backfill fait). **Aucune migration à appliquer.**
- **Quota d'itinéraires PROUVÉ ACTIF sur la canonique (2026-09-24)** — la crainte « no-ope silencieusement » est **levée** : les requêtes réelles de `route-quota.ts` tournent sur la vraie table, et la sémantique **par utilisateur** est prouvée (60 lignes pour un seul identifiant → `hourly_used=60` ⇒ `QUOTA_HOURLY` ; un autre identifiant → `0`). **Sondes nettoyées, 0 ligne résiduelle.** (Piège évité : un premier essai avec 60 identifiants *distincts* ne prouvait pas l'isolement par utilisateur — le refaire avec **un seul** identifiant est ce qui rend la preuve décisive.)
- **`RT-3` — SUPERSEDED par la section « Session 2026-09-24 (suite) » ci-dessous :** le fondateur a **ajouté** `ROUTING_REQUIRE_INTENT=1` dans Vercel. ⚠️ **Ajouter la variable ne prouve pas qu'elle est lue** (déploiement immuable + l'appel anonyme ne discrimine pas). Vérification en attente d'une session authentifiée. La protection de coût, elle, est **déjà réelle** (057 + quota par acheteur, prouvés). `.env.example` corrigé (il décrivait encore le **comportement fautif** « QR non expiré »).
- **Reste avant clôture Species : `SP-9`** (intel 3 formes), **`S-08`/`S-12`** (transport comme offre), **`RT-3`** (config fondateur), **`RT-4`** (voix + essai Android réel), **`RT-6`** (maquette), puis **`SP-VALIDATION` fondateur**.
- **Résidu honnête :** le chemin **authentifié** (acheteur avec intention vivante → itinéraire réel + voix) **n'a pas été exercé** en prod faute de session ; tout ce qui est prouvé ici est **anonyme** (401) et **unitaire** (gate SQL). **Ne pas confondre « le gate est correct » et « le guidage vocal marche sur un téléphone ».**

## Session 2026-09-24 (suite) — `RT-D2` OPTION (b) : ITINÉRAIRE VERROUILLÉ JUSQU'À L'INTENTION

- **Décision fondateur : option (b)** — le bouton itinéraire reste **visible mais désactivé** sur la fiche facilité (l'acheteur doit **savoir** que la capacité existe et être incité à s'engager), plutôt que disparaître.
- **Le fondateur a raison sur le fond, et le diagnostic est précis :** l'itinéraire était ouvert à **deux** endroits **qui se contredisaient** :
  - **fiche facilité** (`TrunkAppV13`) : **OUVERT**, avec la phrase *« Disponible sans générer d'intention d'achat »* ;
  - **flux d'achat** (`BuyerFlowV13`) : **déjà** derrière l'intention, **au même endroit que le contact**.
  → Le flux d'achat était **déjà conforme** ; c'est la **fiche facilité** qui divergeait. « Ranger l'itinéraire là où est le contact » = **retirer l'ouverture précoce de la fiche**, pas déplacer le contact.
- **Livré :** bouton `disabled` + `aria-disabled` + `title` explicatif ; phrase → *« Requis : votre intention d'achat — comme le contact vendeur, l'itinéraire se débloque après avoir choisi cette offre »* ; bouton **qui mène à l'intention** (« Choisir cette offre — débloquer l'itinéraire ») au lieu de tracer. **Plus aucun `setRouteTarget` depuis la fiche.**
- **Maquette de référence corrigée** : 3 endroits portaient encore *« Itinéraire (tracé direct) »* = **vol d'oiseau + accès libre**, contraire à RT-D2 → alignés. **Le seul bouton d'itinéraire encore actif est celui de la SALLE DE TRANSACTION** (« point de retrait ») : correct, il est **déjà après** l'intention (vérifié par contexte, pas par regex — mon premier détecteur l'avait signalé à tort).
- **Garde `route-intent-lock.test.ts` (+5)** verrouille le contrat **côté source ET côté maquette**. **FALSIFIÉ** : ancien comportement restauré → **5/5 ÉCHOUENT** ; correctif restauré → **5/5 passent**. **593/593**, tsc/boundary/maquette verts.
- **⚠️ PIÈGE À NE PAS MANQUER — déplacer le bouton NE PROTÈGE PAS l'endpoint.** `routeTarget` est un **état CLIENT** : n'importe qui peut appeler `/api/v2/public/routing` **directement**. Le bouton est l'**honnêteté** de l'interface ; **la serrure est `ROUTING_REQUIRE_INTENT` côté serveur**. Les deux gestes, pas l'un ou l'autre.
- **Modèle temporel du gate, vérifié (`pi.state = 'active'` seul)** : l'itinéraire est ouvert **pendant toute la remise** (l'intention vérifiée par QR **n'expire jamais** avant clôture) — donc **exactement quand il sert**. **Après clôture** l'intention passe `completed` → l'itinéraire **se reverrouille**. Acceptable (le retrait a eu lieu), mais si le fondateur veut **consulter un itinéraire dans l'historique après la transaction**, c'est une **fonctionnalité neuve**, pas RT-D2.
- **`RT-3` — variable `ROUTING_REQUIRE_INTENT=1` AJOUTÉE par le fondateur dans Vercel (2026-09-24).** **NON ENCORE VÉRIFIÉE** : l'ajouter ne suffit pas (un déploiement est un artefact immuable), et **l'appel anonyme ne discrimine pas** — `401` dans les **deux** modes. Pour trancher il faut un appel **authentifié** (`identity` → route servie ; `intent` → `200 INTENT_REQUIRED` sans intention vivante) : nécessite une **session acheteur** (`OMNI_PROOF_*`, non disponibles au sandbox). **Ne pas déclarer RT-3 fait sur la foi de la variable posée.**
- **Blocage poussée :** jeton GitHub `401` **persistant** cette session (contrairement au `401` transitoire de la veille). **2 commits locaux en attente** (`0c1fcbe` RT-5/.env.example + `55c6ece` RT-D2(b)). Tout le reste est **poussé et déployé**.

