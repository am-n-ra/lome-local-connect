node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example

.env.local

# Local-only proof fixtures (regenerate from prod API; do not commit)
.tmp-*

# Agent scratch space (plans, temp observations) — never product code
.agents_tmp/
