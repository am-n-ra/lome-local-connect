# Founder HQ Board — Omni

**As of:** **2026-09-26 (UTC)** — **PORTE COURANTE = ROOT (ouverte)** — Species V2 **CLOSE `founder-confirmed` 2026-09-25**.

## HQ RECONCILIATION — « on a raté tout le process depuis Species » (2026-09-26)

> Réponse mesurée au verdict fondateur : `docs/founder-hq/hq-reconciliation-2026-09-26.md`.
> **Verdict : le fond est juste ; la cause n'est pas le process, c'est sa SYNCHRONISATION.**
> Seed V2 (`S-01…S-34`) et Species V2 (74 écrans, 27/27, `NON MESURÉ = 0`) sont **réels et validés**.
> Ce qui a été raté : **Root exécuté sous une Species non conforme** (incidents 2026-09-23 **et** 2026-09-24,
> tous deux enregistrés), puis une **mémoire produit en retard** sur le socle réel — d'où un rond-point
> **documentaire**, pas produit.
>
> **Trois défauts BLOQUANTS ont été corrigés et vérifiés EN PROD le 2026-09-26 :**
> - **`RB-PROD-3`** — la carte acheteur renvoyait **HTTP 500 pour tout appel** (`listPublicFacilities`
>   sélectionnait `e.commercial_plan` sans le grouper) → **500 → 200, 206 facilités**. Prod `85c1669`.
> - **`RB-PROD-1`/`RB-PROD-2`** — aucun vendeur ne pouvait créer de facilité, aucune offre ne pouvait être
>   créée → corrigés **et déployés** (vérifiés dans l'artefact servi).
> - **`T-07d` fermé** — prod `index-D48HqbeO.js` === build local.
>
> **Pourquoi 607 tests verts ne l'ont pas vu** : les tests utilisent un **`sql` stubbé** — ils n'exécutent
> **jamais** SQL, donc une requête que Postgres ne peut pas **compiler** passe tout. **Angle mort de méthode.**
> Deux gardes ajoutées, **toutes deux falsifiées** : preuve SQL réelle (`27/27`, exit 1 si on remet le bug)
> + garde statique GROUP BY (une violation exacte sur le code cassé, 0 faux positif). **609 tests.**
>
> **Nouvelle découverte `SP-V2-01` (Haute)** — **l'impasse de l'offre sans propriétaire** : 2 lieux
> **visibles acheteur** sans compte mais **portant une offre publiée** ; l'acheteur **peut demander** la
> disponibilité (le chemin d'écriture ne teste pas la propriété) et **aucun vendeur ne peut répondre**
> (la file vendeur joint `f.account_id`) → **expire en 15 min, 0 crédit payé**. Cause = **dette de
> données** (fixtures `public_import` du 2026-08-22). **Décision fondateur : retirer ou rattacher.**
>
> **Le « fond qui manque » est chiffré :** les **caractéristiques d'offre** (cœur S-01/S-02 — « tout est
> offre, le type est une caractéristique ») sont **déclarées en schéma, jamais écrites** :
> `uniqueness_kind`/`handover_kind`/`price_kind`/`condition_kind` = **0/16**. La maquette le montre,
> l'app ne le peut pas.
>
> **Prochaine porte (Root)** — ordre recommandé : **`R-B` d'abord** (caractéristiques d'offre : écriture →
> lecture → recherche), puis **alignement app ↔ maquette** (seuils `seuil` éditable + devise `D-LOC`),
> `R-D` (chemin `individu`), `R-C`/`SP-V2-01`, `R-E` (S-11). **Ne pas rouvrir Seed/Species ni élargir.**

---

## ⛔ DÉCISION BLOQUANTE — cohérence Seed ↔ socle (2026-09-23)

> **⚠️ CE BLOC EST PÉRIMÉ — conservé pour l'historique, corrigé le 2026-09-25.**
> Il annonçait « **7 incohérences, une seule racine** » et « **la maquette suit le Seed ; le socle ne suit pas** ».
> **La section R-1→R-4b, plus bas dans ce même fichier, le contredit : le socle A suivi.** La décision
> `D-C1` a été **tranchée par le fondateur (2026-09-24)** **et exécutée** (`058`→`061`, vérifié live sur
> `br-dawn-hill-am5amy22` : `v2_products.facility_id` → `is_nullable = YES`, `v2_entities` présente,
> `entity_id` sur offres/lieux/entitlements, 13/16 offres liées).
> **Un en-tête qui contredit son propre corps est exactement le défaut qui a produit le rond-point.**

Le fondateur : *« tout le fond et la logique qui doit faire de omni omni n'est pas là et même il y a beaucoup d'incohérence »*.

**Mesuré** (`docs/nature-way/omni-seed-vs-code-coherence-register-2026-09-23.md`) : ~~**7 incohérences, une seule racine**~~ → **corrigé : 0 incohérence ouverte. `C-1`/`C-2`/`C-3`/`C-7` fermées (`058`→`061`) ; `C-4`/`C-5`/`C-6` corrigées (`R-4a`).**

- ~~**Racine :** `db/migrations/001_v2_roots.sql:82` — `v2_products.facility_id uuid not null`~~ **FAUX** :
  `001:71` est `v2_facility_entitlements.facility_id` ; et `058:33` a fait `drop not null` sur `v2_products`.
- **Autres :** ~~Pro par **facilité**~~ (**fermé `R-4b`**) · ~~plafond **5**~~ (**corrigé `R-4a`**) ·
  ~~bulk `ceil(N/100)`~~ (**corrigé `R-4a`**) · ~~seuil **3 uniforme**~~ (**corrigé `R-4a`**).
- **Verdict corrigé :** **la maquette ET le socle suivent le Seed.** Ce qui n'avait pas suivi, c'est
  **ce document** — et c'est ce qui a fait croire à un rond-point.

**Décision — fondateur seul :** **D-C1 RETENUE (fondateur, 2026-09-24)** — reconstruire le socle au modèle Seed.
**D-C2 / D-C3 : écartées.** (D-C2 contredisait S-01/S-02/S-25 que le fondateur a écrits ; D-C3 réduisait sans guérir.)

**D-C1 EST DÉJÀ PARTIELLEMENT EXÉCUTÉE — vérifié en base le 2026-09-24 :**
- **R-1 (`058_v2_entity_layer_r1`)** : `v2_entities` créée · `v2_products.entity_id` (13/16 offres liées) ·
  `facility_id` **nullable** (`is_nullable=YES`) · 5 caractéristiques d'offre (`position_kind`, `uniqueness_kind`,
  `handover_kind`, `price_kind`, `condition_kind`) — **C-1/C-2 (racine) traités au niveau schéma.**
- **R-2a (`059_v2_facility_entity_link`)** : lien explicite facilité → entité · **3/3** facilités réelles liées.
- **R-2 (code entité-aware)** : écriture (`createSellerProductDraft`), lectures (catalogue, transition, disponibilité,
  stock-events), UI `CompanyV13` groupe par **entité**. **Preuve A/B** : une offre sans lieu est **invisible** par
  l'ancienne requête, **visible** par la nouvelle.

**Ce qui reste :** **R-3c** (gel des colonnes de confiance du lieu) → ~~R-4b~~ (**fait 2026-09-25**, voir ci-dessous) → **R-5** (découverte 2 niveaux, S-11). Contrat : `omni-root-v2-entity-layer-contract-2026-09-23.md` §6/§12.

**R-4a EXÉCUTÉ (`019d97f`)** : C-4 (`FREE_OFFER_LIMIT = 20`), C-5 (1 crédit par besoin bulk, plus de `ceil(N/100)`), C-6 (seuil individu 1 / organisation 3). **Le registre C-1…C-7 est donc périmé sur C-4/C-5/C-6** — vérifié dans `src/domain/invariants.ts` et `trunk-repository.ts`.

**R-4b EXÉCUTÉ (2026-09-25, commit `6b88907`)** — **C-3 fermé** : le Pro vit sur l'**entité**.
- **Migration `061_v2_entitlement_entity.sql`** (additive+idempotente) : `v2_facility_entitlements.entity_id` + FK cascade + backfill + index partiel. **Prouvée sur branche jetable puis APPLIQUÉE canonical `br-dawn-hill-am5amy22`** + registre (`53e3347b…`).
- **Code** : activation/renouvellement écrivent `entity_id` ; toutes les lectures d'entitlement couvrent **lieu OU entité**.
- **Preuve A/B** : un 2e lieu d'une même entité, **sans** entitlement propre → ancienne porte **refusait** (`0`), nouvelle **accorde** (`1`).
- **3 bugs réels trouvés EN LE FAISANT** (chacun prouvé A/B sur Postgres) : (1) la porte de publication lisait `v2_entities.commercial_plan`, **colonne jamais écrite** → un vendeur Pro **payant** ne pouvait pas dépasser le plafond gratuit ; (2) deux gates testaient `state='active'` **sans** `ends_at > now()` et **aucun balayage d'expiration n'existe** → un Pro expiré depuis 60 j passait encore ; (3) l'affichage de découverte montrait la colonne brute, **jamais remise à `free`** → un Pro échu s'affichait `pro_active` pour toujours.
- **596 → 600/600 tests**, tsc/boundary clean, build + 12 bundles serverless régénérés.

**R-3 EXÉCUTÉ (R-3a + R-3b, 2026-09-25, commits `4495edc` + `9c3f5d8` + `3222c03`) :**
- **Découverte qui a changé le plan :** **aucune** écriture n'allait vers `v2_entities` — les **8 sites** d'écriture écrivaient tous sur `v2_facilities`, les colonnes d'entité ne venaient que du **backfill R-1**. Passer les lectures à l'entité d'abord **aurait servi une confiance périmée** : prouvé en base, entité `confirmed` + lieu `rejected` → **8 produits affichés pour une facilité rejetée** (l'ancienne porte : 0). **Régression de sécurité, pas détail de style.** L'ordre du plan a donc été **inversé** : miroir d'écriture **d'abord**.
- **R-3b (miroir)** : `submitTransactionRating`, `reviewVerificationRequest`, `correctFacilitySalesCounter` alimentent l'entité **en plus** du lieu. Claim (3 écritures = imports `entity_id` NULL) et Pro (2 écritures = R-4b) **non mirés volontairement**.
- **R-3a (lecture)** : entité d'abord, repli lieu, sur les 5 portes décisionnelles (découverte carte, détail facilité, dispo simple, dispo bulk).
- **Migration `060_v2_entity_trust_backfill.sql`** : filet de sécurité, **appliquée + enregistrée sur la canonique** (`e7c41b90a2f8`).
- **Deux pièges évités avant la prod** : `commercial_plan` **ne doit pas** être lu depuis l'entité (colonne `free` par défaut, aucune écriture → entitlements cassés) ; et les `api/v2/**.js` **sont des artefacts committés** — le 1er push les avait **oubliés**, la prod aurait servi l'ancien comportement (détecté, corrigé par `9c3f5d8`).
- **Preuves** : miroir → entité 1/lieu 1/`still_stale=false` ; découverte **202→202 lignes** (aucun fan-out) ; `060` répare puis 0 ligne au rejeu ; **596/596**, tsc/boundary clean ; **prod sert la porte entité** (5 occurrences dans le bundle déployé), client `index-DeBqIxQ1.js` === local.

**Rien d'aval (Trunk, Venture Lifecycle) ne s'ouvre avant la couche entité.**

---

**As of (V1 historique):** 2026-09-23 (UTC) — **PORTE COURANTE = Seed reconciliation + Species reconciliation (RÉOUVERTE par le fondateur 2026-09-23).** Voir la section « RÉOUVERTURE 2026-09-23 » en tête des lignes d'état et `founder-hq-master-plan.md` §« HQ RECONCILIATION — 2026-09-23 ». Les lignes ci-dessous datées 2026-09-17 décrivent l'état **V1 antérieur** (`omni-v2-rebuild` @ `c4c8bef`+) ; elles restent vraies historiquement mais **ne sont plus l'état de porte**.

**As of (historique V1):** 2026-09-17 (UTC) — reconciled to repo truth : correctifs livrés par `5a31ebc`, prod `index-DUw7RWKU.js` === build local (sha256 `f3727dce…`, T-07d ✅) ; documents de dispatch ajoutés ensuite (`HO-OMNI-18`), bundle servi inchangé  
**Plan:** `HQ-OMNI-2026-09-02` (`docs/founder-hq/founder-hq-master-plan.md`)  
**Supersedes:** `v2-founder-hq.md` (2026-08-25) as the operating index. That file is kept as history.  
**Reconciliation note (2026-09-17):** la board était figée au 2026-09-13/14 et **omettait 9 tranches livrées depuis** : l'audit cycle V1 + son plan ordonné §4, RAC-1, COR-1a/3b/7b/7c, TEC-1, FF-1…FF-8, les bugs racines E2E (D-TXN-11), le bug de déploiement critique, le correctif glyphes MapLibre et **PRE-1**. Ces lignes sont désormais présentes. **Le plan ordonné §4 de l'audit cycle V1 est intégralement exécuté.**


---

## Species V2 CLOSE 2026-09-25 — état de porte COURANT : **Root**

> Marker de contrôle : **Species V2 CLOSE** · source de vérité : `docs/founder-hq/current-state.md`.

| Area | Current truth | Authority / record | Gate or status | Owner | Next action | Capacity / risk |
|---|---|---|---|---|---|---|
| **Active gate (COURANT)** | **Root** — **Species V2 CLOSE `founder-confirmed` 2026-09-25** (fondateur : « **Validé** »), validation portant sur **`SP-1…SP-10`**. Mesure à la clôture : `check:species-t12` **26 rendues / 32, `NON MESURÉ = 0`, 0 non conforme** ; `check:maquette` 74 écrans ; **600/600 tests**. Volet contraintes (D-CON-1…5 + D-LOC-1…5) livré et falsifié. | `current-state.md` §Gate ; `omni-species-v2-decision-registry-2026-09-23.md` ; `omni-search-constraints-decisions-D-CON-2026-09-25.md` ; `omni-currency-localization-contract-2026-09-25.md` | **Species CLOSE · Root OUVERT** | Fondateur (validation donnée) · Nature Way (exécution) | **⏭️ ACTION COURANTE = finir le socle `R-B`…`R-E`**, puis aligner l'app sur la maquette | ⚠️ **L'app N'EST PAS alignée** (chips figés, devise en dur) — **maintenant débloqué** puisque Root est ouvert. **`public.markets` absente** du canonique v2 ; **dette data** (9 produits `USD` tarifés en francs) ; **bug serveur** : filtre budget aveugle à la devise |
| **Seed V2** | `omni-intent-brief-v2-2026-09-23.md` — 34 décisions **S-01…S-34**, `founder-confirmed`. Mot du fondateur : **« c'est ça »**. Supersede l'Intent Brief du 2026-09-02. | `docs/nature-way/omni-intent-brief-v2-2026-09-23.md` | **CLOS** | Fondateur | Sert de référence unique à T-11/T-12 | Toute décision S-xx non couverte par une surface = travail Species |
| **Species V2** | Maquette `docs/maquette/omni-species-v2-interactive.html` — **74 écrans**. `SP-1…SP-10` **livrés ET ACCEPTÉS**. **2026-09-25 : volet CONTRAINTES** — D-CON-1…5 + D-LOC-1…5 (devise par localisation) ; seuils réglables, fiche lisant la contrainte active, distance non dupliquée, 3 groupes explicites ; gardes falsifiés. | maquette V2 + registre + `omni-search-constraints-decisions-D-CON-2026-09-25.md` + `omni-currency-localization-contract-2026-09-25.md` | **CLOSE `founder-confirmed` 2026-09-25** | **Fondateur (donné)** | — (clos) | `T-12` **refait et clos** — audit par **rendu navigateur**. ⚠️ **Dénominateur corrigé 2026-09-25 (`COH-V2-18`)** : l'audit imprimait « 16/16 » avec un dénominateur **choisi par lui-même** (15 décisions sur 34). Désormais il **lit le Seed** et **classe chaque décision** → **27/27 conforme, 26 rendues, `NON MESURÉ = 0`**, exit ≠ 0 si un non-conforme survit. **1 écart réel corrigé** (`S-04`). Les 5 dernières `règle écrite` (`S-09`, `S-13`, `S-16`, `S-17`, `S-28`) sont **désormais mesurées au rendu** — 2 prédicats faux corrigés (`S-17` sur 3 surfaces ; `S-28` casse `innerText`). |
| **Décisions V2** | `D-1a` fusion maquette+app (**appliqué**) ; `D-2a` **l'offre appartient à l'ENTITÉ** (**appliqué**). | Intent Brief V2 + registre de cohérence | `done` | Fondateur | — | — |
| **Gate 6 (V1)** | Verdict fondateur「 Go with limits 」 2026-09-11 — **reste fermé pour la V1**, mais **superseded comme porte courante** par la réouverture Seed/Species. | Master Plan | `closed` (V1) · **non courant** | Fondateur | Ne pas confondre avec la porte courante | — |
| **T-07d (prod)** | Prod `omni.sparkafrika.online` sert **`index-BUMFRcnb.js`** ≠ local → **T-07d OUVERT** (re-vérifié 2026-09-20). `MAPBOX_ACCESS_TOKEN` jamais posé → `PROVIDER_NOT_CONFIGURED`. | board 2026-09-20 | `open` | Fondateur | **Ne pas pousser** tant que la porte Seed/Species est ouverte | Pousser maintenant = rejouer le pattern précipité |

| Area | Current truth | Authority / record | Gate or status | Owner | Next action | Capacity / risk |
|---|---|---|---|---|---|---|
| Active milestone | M-01: pilot-ready V1 loop derived from `OMNI — MASTER V1`. Seed confirme 2026-09-02. Rebuild order fixe par le fondateur: **Admin/team ops → Seller → Buyer**, puis seller+buyer+team operator prouvent la boucle. | Confirmed Intent Brief; HQ Master Plan | **⚠️ SUPERSEDED — voir « RÉOUVERTURE 2026-09-23 » : Gates 1-2 (Seed/Species) sont RÉOUVERTS ; la porte courante est Seed+Species reconciliation.** (État V1 historique :) Gates 1-6 `closed` (Gate 6 = verdict fondateur「 Go with limits 」 2026-09-11); **P1+P2+P3 (ordre NW-14 P0→P1→P2) COMPLET + EN PROD**: NW-13c→j (migrations 044→051 APPLIED canonical), NW-15 P2 Gouvernance CLOSED 2026-09-14 (`052`+`053` APPLIED); **puis cycle V1 bouclé 2026-09-16/17**: audit 18 étapes, **plan ordonné §4 RAC-1→COR-1a/3b/7b/7c→TEC-1→PRE-1 intégralement exécuté**, domaine transactionnel FF-1…FF-8 livré + 4 bugs racines E2E corrigés, correctif glyphes MapLibre en prod, **PRE-1 80/80 aux 4 largeurs**; HEAD `c4c8bef` (+5 commits non poussés), **576 tests**, tsc clean, boundary clean ; prod canonique **`omni.sparkafrika.online`** sert **`index-BUMFRcnb.js`** ≠ local **`index-Dq15D13D.js`** donc **T-07d OUVERT** (re-vérifié 2026-09-20 ; le board indiquait `index-DUw7RWKU.js` = **périmé**) ; **prod → `PROVIDER_NOT_CONFIGURED` : `MAPBOX_ACCESS_TOKEN` jamais posé** | Founder + Nature Way | **Prochaine porte à confirmer fondateur : (a) V-9 (nettoyage routes mortes + 6 tests factices, décision requise) OU (b) exercer le cycle authentifié en session fondateur (intention→QR→paiement→verrouillage→avis, seul segment encore BLOCKED) OU (c) Gate 7 Venture Lifecycle** (dispatch `/nature-way-venture-lifecycle` — stage scorecard + CAC split) | Ne pas elargir le perimetre au-dela de M-01 (pas de PR 2-6; Gate 7 = watch jusqu'à proof demande) |
| NW-12 Search intent + team roles | Chips contraintes réelles ( Quantité/≤15k/≤10km/Ouvert(, bientôt honnête; recherche = cœur: pins de la carte = résultats après réussite; fallback raster si style vectoriel planté; admin: Équipe · Rôles ( grant/revoke operator/reviewer motif audité( prouvé 4 tests | NW-12 dossier + AGENTS.md | done, push 7b20da0, prod hash en cours | Nature Way | P1 ( NW-13c→d→e→g(; Gate 7 watch | Recherche = cœur: les affordances doivent rester vraies; surface roles = staff limité |
| NW-13a/b + NW-14 | Entrée Seller universelle livrée( `68b9107`(; spec Plans Free/Pro **verrouillée fondateur** ( D-E…D-K résolues, `docs/nature-way/omni-nw-13b-plans-free-pro-spec-2026-09-10.md`(; NW-14 audit flows+recherche( `aab6966`( docs-only: 4 plaintes fondateur traduites en constats code-vérifiés + **priorisation P0 recherche → P1 vendeur → P2 gouvernance team/operator** — ordre **P0→P1→P2 validé fondateur 2026-09-11**; code débloqué sur P0/P1/P2( NW-14 §4( | `docs/nature-way/omni-nw-13b-*` + `omni-nw-14-*` + AGENTS.md | NW-14 = **validé fondateur 2026-09-11**; P0/P1/P2 verrouillés, code débloqué(; Gate   ​6 reste `ready` | Nature Way | **P0 livré 2026-09-11** ( `952df28`+`5cd175c`( — fallback map surface, countmark, chips rayon; prochaine: P1 ( NW-13c→d→e→g( selon ordre validé, puis verdict Gate 6 | Périmètre M-01, une porte un spécialiste; ne pas élargir |
| NW-14 P0 (2026-09-11: recherche | **P0 livré sur omni-v2-rebuild**: P0-A fallback map surface (`952df28`, surface MapLibre-compatible sur fallback DOM(, P0-B countmark V1.3 + cinématique sur fallback (`5cd175c`(, P0-C chips portée rayon 1/5/10/25/100/Monde câblées `rayon_km` + rangée buyer (`5cd175c`(. Preuves: tsc clean, 55 files/347 tests, build vert, boundary clean | `docs/nature-way/omni-nw14-p0-evidence-register-2026-09-11.md` + `intra-skill-plan-NW-PROD-OMNI-01.md` § slice P0 | P0 3/3 `done`; **Gate 6 CLOSED — verdict fondateur「 Go with limits 」 2026-09-11** | Nature Way | P1 ( NW-13c→d→e→g( next; clôture honnête Gate 6 avec limites assumées ( P0-C « Monde » no-op serveur( | Ne pas élargir au-delà M-01 |
| Product proof | Root applique: `038_v2_g03_root_gaps.sql` + `039_v2_operational_state.sql` sur Neon (fondateur, manuel; Trunk: slices Admin/Seller/Buyer prouvees en prod (9/9, 8/8, 8/8), **preuve integree 16/16** (correlation `65488663-91f4-4347-ba46-4fd9d600991d`, commit `ebd9d80`) -> Gate  4 CLOSED 2026-09-03. Gate 5 (Branches/UI( CLOSED (HO-OMNI-06 contextualisation nature-way pushed `113e278`; T-08 done `b681954`(. **Gate 6 — V-7 COMPLET (T1–T9 `done`**: T4 retrait/livraison+note ( `773bc4f`+migration 043 appliquée+backfill; T5 seller map-first (`ea71025`+`a7f8e5a`), T6 QR auto+scanner (`3f1a43a`), T7 onboarding gating (`6fdfb8a`), T8 sim+fraîcheur (`b681954`), T9 D-5 bundle 5/5 (`af5bc82`/`75393bd`/`d802a32`/`9d4762f`/`638ad33`) — **52 files/329 tests**, builds vérifiés, **T-07d ✅ chaque slice: prod hash === build local** (dernier `index-BJwt4Bc9.js`) 2026-09-09. **Post-V-8 (2026-09-09:** COH-06 toggle vendeur réel (`7aa4319`+`a845d53`) livré end-to-end: SellerV13 ON/OFF→`setSellerFacilityOperationalState` (src/server+repo+api), 12 bundles serverless portés (`/api/v2/seller/facilities/:id/operational-state`), auth-guarded 401 vif sur prod, audit-logged, états ouvert/ferme/temporairement_indisponible; scanner QR public+vendeur réparé (`.start(` constraints `{ facingMode:{ ideal:'environment' } } }`, orphan lignes retirées, visibility teardown( = COH-04 partiel; **suite 331/331 tests, tsc clean, boundary clean, build `index-DrdqlqWh.js`, prod sert `index-DrdqlqWh.js` === build local (T-07d ✅.** | `db/migrations/038_*`/`039_*`/`043_*`, `src/server/*`, `src/trunk/*`, git `omni-v2-rebuild` | Gate 5 `done`; **Gate 6 Canopy/launch-readiness = `closed`** — verdict fondateur「 Go with limits 」 2026-09-11; spot-check 4 largeurs prod RESOLVED 2026-09-08; 4/4 OK + re-vérifié T-07d ✅ post-V-8; **Gate 7 Venture Lifecycle = watch** | Nature Way | (spot-check fondateur résolue via automate sandbox + confirmation fondateur) | **Prochaine action:** Gate 6 CLOSED; dispatch **P1** ( NW-13c→d→e→g( puis Gate 7 (Lomé pilot demand/proof = watch)  |
| NW-13c P1-A (2026-09-11: création facilité serveur+UI | **Livré sur omni-v2-rebuild**: migration `044_v2_facility_type_rayon.sql` ( `facility_type` fixe/mobile/digital, `rayon_km` mobile, lat/lng nullable=digital sans point ( ; repo `createSellerFacility` étendu (type strict, provis. slot free D-J, trust `unconfirmed` D-B, garde `seller_ready` retirée = vendeur universel D-A; HTTP validator pur `validateSellerFacilityCreate`; client `createSellerFacility` + types; **UI SellerV13**: hint « la création arrive avec la spec Free/Pro » remplacé par formulaire minimal (3 types, coords si fixe/mobile + « Me localiser », rayon si mobile, aucun point si digital ( → POST → refresh → P3. **Preuves**: tsc clean, **55 files/360 tests** (+13: repo 5/http 6/client 2(, build `index-CBKUqllX.js`, boundary clean; bundles serverless régénérés. | `docs/nature-way/omni-nw13c-facility-creation-evidence-2026-09-11.md` + `handoff-receipt-HO-OMNI-13.md` + `db/migrations/044_*.sql` | P1-A `done` ( branch-only; **migration 044 APPLIED canonical 2026-09-13 (Neon MCP)** + registre `omni_schema_migrations` (checksum `454c66b5…`( + preuve INSERT digital/mobile; prod non poussé (guardrail T-07d( | Nature Way | Sur **ordre fondateur push prod** → proof navigateur + prod-hash; puis **NW-13d (crédits bulk)** | Limites assumées: proof navigateur réel non exécuté (sandbox sans DB/Auth(; route HTTP create non exercée end-to-end en prod ( pas de push (; Gate 6 reste CLOSED, Gate 7 watch  |
| NW-13d P1-B (2026-09-13: crédits bulk serveur | **Livré sur omni-v2-rebuild**: migration `045_v2_buyer_credit_accounts.sql` → `v2_buyer_credit_accounts` (compteur mensuel: plan free/pro, monthly_quota 3/≈100, period_month, credits_used, extra_credits packs; UNIQUE buyer( + `v2_availability_credit_ledger` (audit kind/amount signé/request_id(; repo `getOrCreateCreditStanding`+`getBuyerCreditSummary`+`createAvailabilityRequest` débite 1/facilité (`cardinality(facility_scope)`( dans UN guarded statement, idempotence sans re-débit, `InsufficientCreditsError` si épuisé; HTTP `GET /api/v2/buyer/credits` + POST avail 403 `INSUFFICIENT_CREDITS` + validator pur `validateAvailabilityRequestCreate`; client `getBuyerCreditSummary` + `AvailabilityResult` étendu; UI BuyerFlowV13 compteur « Crédits bulk — X/quota » + état épuisé « rechargez en packs ». **Preuves**: tsc clean, **55 files/367 tests** (+7: repo 3/http 3/api 1(, build `index-1lygK87G.js`, boundary clean; bundles serverless régénérés. | `docs/nature-way/omni-nw13d-bulk-credit-evidence-2026-09-13.md` + `handoff-receipt-HO-OMNI-13.md` + `db/migrations/045_*.sql` | P1-B `done` ( branch-only; **migration 045 APPLIED canonical 2026-09-13 (Neon MCP) 12 statements** + registre `omni_schema_migrations` (checksum `f8917577…`( + preuve live ( reset mensuel/period_month → credits_used 0; pack extra 20 → remaining 23; débit → 22; 0 trace (; prod non poussé (guardrail T-07d( | Nature Way | Sur **ordre fondateur push prod** → proof navigateur; puis **NW-13e** | Limites assumées: achat packs réel = **NW-13i** ( `extra_credits` capacité prête, pas de vente (; plan buyer Pro ≈100 = **NW-13h** ( quota 3 free appliqué (; Gate 6 reste CLOSED, Gate 7 watch  |
| NW-13d-2 P1-B2 (2026-09-13: facturation bulk `ceil(N/100)` | **Livré sur omni-v2-rebuild** (amendement fondateur D-G « oui approuve go » 2026-09-13(: vérif **manuelle (1 facilité) = 0 crédit** (createAvailabilityRequest sans debit/credit_spend); **demande bulk = `ceil(N/100)` credits SANS plafond**: repo `createBulkAvailabilityRequest` (`facilityIds[]` ≥2 distincts, `facility_scope`=array complet, débit `Math.ceil(N/100)` guarded + ledger, paramétrisation `({{ids}})::text[]::uuid[]` sans concaténation, refus `InsufficientCreditsError` **« Missing M » chiffré**); HTTP validator pur `validateBulkAvailabilityRequestCreate` + route `POST /api/v2/bulk-availability`; client `requestBulkAvailability` + type `BulkAvailabilityResult`; **UI TrunkAppV13**: sheet bulk refondu — encart coût live (N facil = K credits, solde, après-envoi, « il manque M » si insuffisant), filtre rayon 5/10/25/50 km, cases à cocher + tout/aucune, **UN** bulk (`requestBulkAvailability`) + poll groupé; BuyerFlowV13 « Vérification manuelle gratuite » sans blocage. **Preuves**: tsc clean, **55 files/375 tests** (+8: repo 5/http 4/api 1/adaptés 4(, build `index-5yeSIqD5.js`(, boundary clean; **preuve canonique live Neon MCP T1-T5** (manuel 0, bulk 2→1, refus 250→3 « Missing 1 », cleanup 0 trace(. | `docs/nature-way/omni-nw13d2-bulk-cost-evidence-2026-09-13.md` + `intra-skill-plan-NW-PROD-OMNI-01.md` § NW-13d-2 | P1-B2 `done` ( branch-only; aucune migration requise ( 045 déjà appliquée supporte le scope array (; prod non poussé (guardrail T-07d( | Nature Way | Sur **ordre fondateur push prod** → proof navigateur (encart coût bulk(; puis **NW-13e** | Limites assumées: proof navigateur réel non exécuté (sandbox sans DB/Auth(; route bulk non exercée end-to-end en prod ( pas de push (; achat packs = **NW-13i**, plan buyer Pro ≈100 = **NW-13h**; Gate 6 reste CLOSED, Gate 7 watch  |
| NW-13e P1-C (2026-09-13: bonus confiance D-H | **Livré sur omni-v2-rebuild** (D-H verrouillé fondateur: 20 USD/10 000 XOF minor débloqué après **3 ventes à users distincts**): migration `046_v2_seller_bonus_trust.sql` → `v2_seller_unlock_progress` (PK (facility_id, buyer_account_id) = « users distincts », FK CASCADE) + `v2_seller_unlocks` (UNIQUE (facility_id, unlock_type), unlock_type `pro_test_credit_20_usd`, distinct_buyer_count, required_count 3, status locked/eligible/granted, amount_minor 10000); repo: `submitTransactionRating` CTE réécrit — sur `closed_event` insertion buyer distinct (`ON CONFLICT DO NOTHING`), `qualifying_sales = least(3, count distinct)`, upsert `v2_seller_unlocks` (granted jamais rétrogradé), `getFacilityBonusStatus` + `unlockFacilityBonus` (owner + trust + ≥3 + bonus null + wallet, `for update`) marque granted; HTTP 2 routes owner (`GET/POST /api/v2/seller/facilities/:id/bonus[/unlock]`); client/types `getFacilityBonusStatus`/`unlockFacilityBonus`/`FacilityBonusStatus`; **UI SellerV13** bandeau « Bonus confiance » (X/3 acheteurs distincts · 20 USD, éligible → « Débloquer », granted → « ✓ 20 USD crédités »). **Preuves**: tsc clean, **55 files/380 tests** (+5: repo 2/http 1/api 2; adaptés 2 rating(, build `index-CpV0tuGd.js`, boundary clean; bundles serverless (12( régénérés; **preuve DB branche temp Neon T1-T5** (`br-odd-credit-amrpqgev`: distinct=3 malgré rejeu, eligible, granted préservé, CHECK reject, cleanup 0 trace(. | `docs/nature-way/omni-nw13e-bonus-trust-evidence-2026-09-13.md` + `intra-skill-plan-NW-PROD-OMNI-01.md` § NW-13e + `db/migrations/046_*.sql` | P1-C `done` ( code + preuve branch temp (; **migration 046 APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 12:15:17Z (registre `3c6b08bb…`) + push prod exécuté**; prod sert `index-DHes2HoI.js` === local (T-07d ✅); routes bonus 401 owner-only vérifiées | Nature Way | **NW-13g** livré+en prod (04/047 APPLIED); **prochaine: NW-13h (acheteur Pro D-K)** | Limites assumées: proof navigateur réel non exécuté (sandbox sans DB/Auth( (chaînes UI vérifiées dans bundle prod); `seller_unlocks` legacy supabase non branché (le trunk v2 a son objet propre(; Gate 6 reste CLOSED, Gate 7 watch  |
| NW-13g P1-D (2026-09-13: renouvellement Pro auto D-I | **Livré + en prod**: migration `047_v2_facility_pro_renewal.sql` → `v2_facility_renewal_runs` (audit status `succeeded`/`insufficient_funds`/`skipped`/`failed`, FK CASCADE, prior/new/spend entitlement ids, index (facility_id, run_at desc)); repo `getFacilityRenewalStatus` (plan **véridique** pro_active/pro_expired/free dérivé de l'entitlement + renewalOptIn/daysLeft/proPriceMinor/solde/sufficientFunds), `setFacilityRenewalOptIn` (refuse sans activation, 409 WalletPolicyError), `renewFacilityPro` (spend wallet ref `facility-pro-renew:{id}:{period}` + entitlement 30 j + pro_active + audit; **insufficient_funds sans spend → reste pro_expired**, skipped si pas dû); HTTP 3 routes owner (renewal-status/opt-in/renew); client/types + UI carte « Pro · renouvellement » (toggle opt-in, rappel solde, bouton renouveler) + wallet sheet. **Preuves**: tsc clean, **55 files/393 tests** (+12), build `index-DHes2HoI.js`, boundary clean; preuve branche temp Neon T1-T7; **047 APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 12:15:28Z (registre `7d021a17…`)**; prod sert `index-DHes2HoI.js`+`index-CXK07M8c.css` === dist local byte-identical (T-07d ✅, cmp). | `docs/nature-way/omni-nw13g-facility-pro-renewal-evidence-2026-09-13.md` + `db/migrations/047_*.sql` + `AGENTS.md` | P1-D `done (code+DB+prod)`; **P1 c→d→d2→e→g COMPLET**; migrations 042→047 complètes au registre canonical | Nature Way | **NW-13h (acheteur Pro D-K)** = prochaine tranche (sur ordre fondateur); job rappel/renouvellement asynchrone réel = watch Gate 7 | Limites assumées: proof navigateur UI visuelle (session seller réelle) non capturée (sandbox — chaînes UI `renouvellement auto ON`/`Pro · renouvellement` vérifiées dans bundle prod); job de rappel asynchrone automatique = hors scope P1 (watch Gate 7); barème D-J (Pro vendeur 5 000 XOF) à confirmer; Gate 6 CLOSED maintenu, Gate 7 watch  |
| NW-13i P1 (2026-09-13: packs crédits bulk via Mobile Money, D-J « prix à fixer » non verrouillé | **Mécanisme livré (D-J prix = hypothèse réversible code-only)**: migration `050_v2_bulk_credit_packs.sql` → recharge intents pilotables (`purpose` `wallet`/`pack` CHECK + `pack_credits` int > 0 + contrainte pack-purpose) + credit ledger (`recharge_intent_id` + **UNIQUE** idempotence rejeu + FK SET NULL) — additive, idempotente; repo `getBulkPacks` (source unique pricing, 0 SQL) + `createBulkPackRecharge` (pack inconnu → `WalletPolicyError`, intent `purpose='pack'` + checkout FedaPay) + `createWalletRecharge` étendu (`purpose`/`packCredits`); webhook `reconcileWalletRecharge` branche `purpose='pack'` → wallet ledger `recharge` + credit ledger `pack_credit` (ON CONFLICT `recharge_intent_id` DO NOTHING) + upsert `extra_credits` (seulement si pack_credit inséré) — **atomique + replay-safe**; HTTP `GET/POST /api/v2/buyer/credit-packs` (auth, POST `Idempotency-Key`); client/types `BulkPack`/`BulkPackPurchaseResult` + `getBulkPacks`/`purchaseBulkPack`; **UI Wallet** cardbox « Packs crédits bulk » (solde, liste, bouton Acheter → checkout FedaPay `window.location.assign`) + CTA « Rechargez en packs » dans le sheet bulk + callback `?wallet=recharge` rafraîchit wallet/packs/crédits. **Preuves**: tsc clean, **56 files/424 tests** (+3 repo, +2 pricing), build `index-B1j70RJv.js`, boundary clean; bundles serverless (12) régénérés (`createBulkPackRecharge`/`getBulkPacks` dans `availability.js`); **preuve canonique live Neon T1-T5** (branche temp `br-rough-lake-ame9ho7c`: migration 050 intégrale; buyer Free nouveau → intent confirmed + ledger recharge 50000 + pack_credit 10 + extra 10; **rejeu CTE 0 ligne, extra reste 10**; **UNIQUE rejette doublon**; buyer Pro existant → merge extra 100, plan/quota intacts; cleanup branche supprimée); **050 APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 18:50Z (registre `2a33e4f7…`)**. | `docs/nature-way/omni-nw13i-bulk-credit-packs-evidence-2026-09-13.md` + `db/migrations/050_v2_bulk_credit_packs.sql` + `AGENTS.md` | P1 (NW-13i) `done (code+DB+push prod)`: prod sert **`index-B1j70RJv.js` === local (T-07d ✅)**, route credit-packs 401 sans session | Nature Way | **P1 COMPLET**; **NW-13f analytics livré (2026-09-13)**; NW-13j publicité = prochaine P2/P3 (sur ordre fondateur) | Limites assumées: proof navigateur UI visuelle + achat Mobile Money réel (checkout FedaPay) = prochain spot-check fondateur; rejeu webhook réel à confirmer live; Gate 6 CLOSED maintenu, Gate 7 watch  |
| NW-13f P2 (2026-09-13: analytics vendeur — conversion, provenance scans | **Livré sur omni-v2-rebuild**: repo `getFacilityAnalytics` (owner-only, **une** requête CTE read-only sur `v2_availability_requests/responses` + `v2_transaction_snapshots` + `v2_transaction_events` + `v2_qr_tokens` — aucune migration): entonnoir requests/responsesAvailable/transactionsStarted/qrScansVerified/transactionsClosed + `grossRevenueMinor` (net_amount des closed) + `scanToVerifyAvgMs` (avg verified_at−created_at, null si aucun scan); garde `SellerAuthorizationPolicyError` « Facility not found or not owned »; HTTP `GET /api/v2/seller/facilities/:id/analytics` (401/409/200); client `getFacilityAnalytics`/`SellerFacilityAnalytics`; **UI SellerV13** cardbox « Performance » (Demandes reçues / Transactions (clôturées) / Revenu / QR vérifiés + bouton Actualiser) chargée au fac sélectionnée. **Preuves**: tsc clean, **56 files/429 tests** (+3 repo, +1 http, +1 api), build `index-bYBBhTs2.js`, boundary clean; bundles serverless (12) régénérés (`getFacilityAnalytics`/`/analytics` dans `availability.js`); smoke navigateur preview: boot sans crash, chaînes « Performance »/« Demandes reçues »/« QR vérifiés » dans le bundle servi. | `docs/nature-way/omni-nw13f-analytics-evidence-2026-09-13.md` + `src/server/trunk-repository.ts` + `AGENTS.md` | P2 (NW-13f) `done (code + tests + build)`; push prod = étape suivante (guardrail T-07d) | Nature Way | Sur **ordre fondateur push prod** → proof navigateur (cardbox Performance) + 401/409/200 real in prod; **puis NW-13j publicité** | Limites assumées: analytics **par-facilité** (pas de vue marché globale, pas par-produit, pas d'export, pas de recommandations); proof navigateur UI réelle non capturée (sandbox sans DB/Auth — chaînes UI vérifiées dans bundle local); route non exercée end-to-end en prod (pas de push); Gate 6 CLOSED maintenu, Gate 7 watch  |
| NW-13j P3 #8 (2026-09-13: publicité sponsorisée manuelle — dernière tranche de l'ordre | **Livré + EN PROD**: migration `051_v2_ad_campaigns.sql` → table `v2_ad_campaigns` (facility FK CASCADE, name 1..60, budget>0, spent 0..budget, status `planifiee/active/terminee/pausee`, starts<ends, partial index sponsored_scan) — **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 + registre sha256 `4df27a30…4c04`**; repo `createAdCampaign` (owner + Pro-only, **budget réservé depuis wallet à la création** — ledger `ad_spend` ref `ad-budget:{id}:{startsAt}` ON CONFLICT idempotent, `WalletPolicyError` non-Pro/insuffisant) + `listFacilityAdCampaigns` (campagnes + budget wallet restant); **search `listPublicFacilities` LEFT JOIN campagnes `active` fenêtre → `sponsored` + boost**; HTTP `POST/GET /api/v2/seller/facilities/:id/campaigns` (401/400/409/201/200, `Idempotency-Key`); client/types; **UI SellerV13** cardbox « Campagnes sponsorisées » (Pro-gated: nom + budget + « Lancer », liste statuts, budget wallet restant, message "Activez Omni Pro…") + **buyer badge « Sponsorisé »** sur hcards sponsorisés. **Preuves**: tsc clean, **56 files/443 tests** (+6 repo, +6 http, +2 api), build `index-C0Gc3Psp.js`, boundary clean; preuve temp `br-jolly-thunder-amr30jz7` (constraints/index/scans sponsorisés/CHECK/idempotence/0 trace); smoke preview boot + chaînes UI dans bundle; **push `5cfb268`+`cabf271` → prod `index-C0Gc3Psp.js` === local (T-07d ✅)**, routes campaigns 401 sans session. | `docs/nature-way/omni-nw13j-ad-campaigns-evidence-2026-09-13.md` + `db/migrations/051_v2_ad_campaigns.sql` + `AGENTS.md` | **P3 #8 `done (code+DB+prod)` — ordre NW-13 c→d→d2→e→g→h→f→i→j COMPLET; P1+P2+P3 livrés** | Nature Way | Proof navigateur UI campagnes (session vendeur Pro réelle) au prochain spot-check fondateur; tarif pub réseau à verrouiller; Gate 7 watch | Limites assumées: aucune campagne créée en prod (attente vendeur Pro réel); IA ciblage = draft only « Bientôt disponible » (spec); budget pub = réservé wallet à la création (pas de budget séparé); Gate 6 CLOSED maintenu, Gate 7 watch |
| NW-15 P2 Gouvernance Admin/Operator/Team (2026-09-13/14 — P2-A→P2-C) | **CLOSED 2026-09-14 — plainte fondateur #4 soldée** : **P2-A** migration `052_v2_teams.sql` (APPLIED canonical `45994f6c…`; `v2_teams`/`v2_team_members`/`v2_team_invites` + index partial pending unique); repo 5 méthodes admin-guardées + audit; routes `/api/v2/admin/teams…`; **P2-B** UI « Équipe · Groupes » dans `AdminV13` (créer/inviter/révoquer/retirer); **P2-B2** identité réelle email/name (join `neon_auth."user"`; `2e17594`); **P2-C** migration `053_v2_facility_zone.sql` (APPLIED canonical `6793fa83…`) + invite-accept (`GET /api/v2/team/invites` + `POST .../accept` + UI sheet Compte « Invitations d'équipe ») + mission zone (`POST /api/v2/admin/facilities/:id/zone` + champ/bouton Zone dans la file de revue) + **filtre zone reviewer/operator** (review queue + seller activation queue, règle `t.zone = f.zone`, pas d'équipe zonée = tout visible). **Preuves**: tsc clean, **57 files/466 tests**, boundary clean, build `index-BwnpSj7_.js`; **push `b5c8868`+`cea8809` → prod === local (T-07d ✅)**; routes 401 sans session en prod. Dossiers `omni-nw15-p2-teams-governance-evidence-2026-09-13.md` + `omni-nw15-p2c-invite-accept-zones-evidence-2026-09-14.md`. | `docs/founder-hq/handoff-receipt-HO-OMNI-15.md` + `docs/nature-way/omni-nw15-p2*` + `db/migrations/052_*.sql`+`053_*.sql` + `AGENTS.md` | **P2 complet `done (code+DB+prod)`**; Gate 6 CLOSED maintenu; Gate 7 watch | Nature Way | Spot-check navigateur fondateur (créer groupe + zone + accepter invitation en session réel); **prochaine porte à confirmer = Gate 7 Venture Lifecycle (preuve demande Lomé) ou tranche product suivante** | Limites assumées: cycle HTTP complet authentifié non exécuté (session fondateur); proof navigateur UI non capturé (sandbox sans DB/Auth); remote git réparée en P2-C (`am-n-ra/lome-local-connect` — ancien token mort) |

| Cycle V1 — audit + plan ordonné (2026-09-14) | Audit des 18 étapes du cycle fondateur, code-vérifié : **16/23 FULL, 4 PARTIAL, 2 BLOCKED, 1 corrigée**. Verdict : « V1 terminée » **non déclarable** avant RAC-1 + PRE-1. Plan ordonné §4 : **RAC-1 → COR-0a/9d (fait) → COR-1a → COR-3b → COR-7b → COR-7c → TEC-1 → PRE-1**. | `docs/nature-way/omni-v1-cycle-audit-2026-09-14.md` + handoff `ho-omni-16` | audit `done`; **plan §4 intégralement exécuté au 2026-09-17** | Nature Way | Clore la boucle cycle V1 et rapporter au fondateur | Aucun passage Gate 7 avant RAC-1 + PRE-1 (respecté) |
| RAC-1 (2026-09-16, `4f6d55f`) — contact vendeur après intention | **Racine bloquante du cycle, soldée.** `v2_facilities` n'avait aucun champ contact (le modèle *omettait* le contact vendeur) → migration `054_v2_facility_contact.sql` (contact_phone/contact_whatsapp, additive) + repo + HTTP + API + UI : fiche facilité « Contact » **désactivée avant intention, active après**. | `db/migrations/054_v2_facility_contact.sql` + AGENTS.md §RAC-1 | `done` — **CLOSED** | Nature Way | Lever le seul blocage racine identifié par l'audit | Résidu honnête : contact révélé seulement après intention réelle (session fondateur) → couvert par PRE-1 |
| COR-1a/3b/7b/7c (2026-09-16) — recentrage, onboarding, produit recherché, panier | **Quatre corrections PARTIAL→FULL** : **COR-1a** `837e28c` recentrage auto initial garanti sur la position utilisateur (`arrivalTargetFor`, contrat de cible prouvé unitairement) ; **COR-3b** `275cc7c` onboarding enrichi (étape éducative + prénom optionnel) ; **COR-7b** `8d24c63` produit recherché mis en avant dans la fiche (badge + tri, monochrome — l'accent `#2E8B6F` reste réservé à la confiance) ; **COR-7c** `eb508fa` panier de demande par vendeur, persistant pour la session. | AGENTS.md §COR-1a/3b/7b/7c + `omni-v1-cycle-audit` §4 | `done` (4/4) | Nature Way | Preuve navigateur groupée dans PRE-1 | COR-1a : la preuve *avec géoloc autorisée* est impossible en sandbox (permission denied) — contrat prouvé unitairement, visuel réel en PRE-1 |
| TEC-1 (2026-09-16, `a601089`) — audit technique transversal | Dettes interface/technique/logique/métier restantes (juste avant PRE-1), code-vérité **487/487 + tsc + boundary + build**. **Aucune dette bloquante pour PRE-1.** Deux plus visibles : **T-1** arbre de routes TanStack **mort** (`src/routes/` 14 fichiers + `router.tsx` + `routeTree.gen.ts` + `start.ts`, **0 importeur vivant** — `main.tsx` monte `TrunkAppV13` directement) ; **T-2** **6 tests placeholder** qui n'assertent que `toBeDefined()` sur des composants **inexistants** = fausse couverture. | `docs/nature-way/omni-tec1-technical-audit-2026-09-16.md` | `done` — **V-9 (nettoyage routes mortes + tests factices) en `todo`, décision fondateur requise** | Nature Way | Ne rien supprimer avant décision fondateur sur V-9 | Dette *réelle* mais non bloquante : à ne pas confondre avec un défaut produit |
| FF-1…FF-8 (2026-09-16/17) — contrat + domaine transactionnel | Suite de 8 correctifs sur le flux verrouillé, livrée et poussée : **FF-1/2/6** `2408add` (retrait du dock « annuler » **mort et menteur** → « Quitter » ; « Transactions en cours » reprenable ; timer/échéance par étape) ; **FF-4** `12de520` annulation acheteur d'une demande de dispo (Phase A, sans effet monétaire) ; **FF-3** `5329bdf` planificateur d'expiration serveur ; **FF-5** `2ecb8f9` cycle QR (TTL 1–60 min, ré-émission, révocation) ; **FF-7** `f46330f` notifications de transaction ; **FF-8** `b2ce397`→`65d81cb` réservation de stock (migration `055`). Contrat + conception : `omni-transaction-flow-state-contract` + `omni-transaction-domain-design` (2026-09-16). | AGENTS.md §FF-1/2/6, §FF-3/4/5/7, §FF-8 + plan local `intra-skill-plan-NW-PROD-OMNI-TXN-01.md` | **FF-1…FF-8 `done` + en prod** ; **FF-9 (litige) = `watch` Gate 7** | Nature Way | FF-9 seulement si le terrain le justifie | Décisions D-TXN-1…12 confirmées fondateur (« GO ») |
| Bugs racines trouvés par les preuves E2E (2026-09-17) | Quatre bugs réels, invisibles aux tests unitaires, révélés en pilotant le **code livré** : **D-TXN-11** `2b33433` (+ migration `056`) — l'état courant était départagé par `id` **uuid aléatoire** sur des `created_at` **identiques** (horloge de transaction) → tri **non déterministe**, un flux verrouillé se rouvrait après notation ; correctif = colonne **GÉNÉRÉE** `state_rank` + index. **QR mort** `e4948a5` — `qr_eligible` relisait des lignes insérées dans la **même instruction** (invisible) → `qrToken` **toujours null** alors que le contrat le promettait. **Notation** `65d81cb` — même sémantique de snapshot : le 1er appel levait une erreur **tout en persistant** la note. **Soudure SQL** `cf1d07d`. | AGENTS.md §D-TXN-11 + `scripts/prove-v2-transaction-lifecycle.mjs` (T1–T9) + `scripts/prove-v2-stock-reservation.mjs` (T1–T6) | `done` — **leçon consignée** : ne jamais départager un état par une clé aléatoire ; ne pas relire dans la même instruction ce qu'on vient d'écrire | Nature Way | Preuves E2E rejouables (`npm run proof:transaction-lifecycle`) | Classe de bug ayant frappé **4 fois** — surveillée explicitement désormais |
| BUG déploiement critique (2026-09-17, `8f5258b`) | `vercel.json` (FF-3) déclarait un cron **horaire** ; le plan Vercel Hobby n'autorise que des crons **quotidiens** → Vercel a **silencieusement cessé de déployer** dès `cf1d07d`. Prod est restée ~50 min sur un bundle périmé **sans aucun rouge**. Détecté en pariant sur des routes 404 + `api.github.com/.../deployments`. Correctif : cron quotidien `0 3 * * *` + balayage opportuniste des intentions échues. | AGENTS.md §BUG DÉPLOIEMENT CRITIQUE | `done` | Nature Way | **Règle acquise : après chaque push, comparer hash prod === build local ET vérifier l'entrée GitHub `deployments`** | Un cron refusé gèle la prod **silencieusement** — le guardrail T-07d « hash === local » ne suffit pas seul |
| MapLibre glyphes — libellés de clusters en chiffres bruts (2026-09-17, `131d23a`) | **Défaut réel en prod.** `tiles.basemaps.cartocdn.com` sert toutes les familles du style Positron **sauf `Noto Sans Bold`** : cette plage renvoie **404 sans en-tête CORS** → MapLibre journalise « Rendering codepoint locally instead » et la couche `omni-cluster-count` (et tout libellé gras) **se dégrade en chiffres non formés**. Un helper `rewriteGlyphUrl` **existait mais n'était jamais appelé** (0 référence dans le bundle prod) et sa réécriture par hôte était **trop large** (elle aurait déplacé OpenFreeMap, que cet hôte sert correctement). Remplacé par `createGlyphTransformRequest()`, câblé dans `new Map(...)` : ne réécrit que les glyphes quittant `cartocdn.com`. | `src/lib/maplibre.ts`, `src/trunk/TrunkMap.tsx`, tests `maplibre.unit.test.ts`, preuve `scripts/proof-map-glyphs.mjs` | `done` — **en prod** (`index-Bjl18UB3.js` === build local, T-07d ✅) ; preuve A/B : prod d'avant **échoue** (CORS `Noto Sans Bold`), build corrigé **5 requêtes glyphes toutes 200**, `boldServedFromWorkingHost: 1`, **0 erreur** | Nature Way | Surveiller toute régression glyphes via `no-console-errors` dans PRE-1 | Correctif **ciblé** : les hôtes sains et OpenFreeMap gardent leurs URLs déclarées |
| PRE-1 — preuve navigateur du cycle V1 (2026-09-17) | **Dernier item du plan ordonné §4, exécuté.** Preuve navigateur réelle sur **prod**, **4 largeurs** (360/768/1280/1920) : arrivée → carte réelle → fourniture (206 pins) → recherche → contraintes → résultats → fiche → confiance → itinéraire → contact gardé. **20/20 PASS à chaque largeur (80/80), console propre partout.** Le harnais précédent produisait **21 faux négatifs** dus à 3 hypothèses fausses sur l'UI (chips en divulgation progressive ; libellé réel « Itinéraire vers ce vendeur » ; les résultats sont des `button.hcard` dans `section[data-sheet=results]`, un pin portant le même nom intercepte le clic), **pas** à des défauts produit. Requête de preuve « boulangerie » (1 facilité confirmée, 3 produits) au lieu de « coffee » (0 résultat). | `scripts/prove-v1-cycle-browser.mjs` + `docs/nature-way/pre1-proof/` (JSON + 8 captures) | **`done` (80/80)** — périmètre couvert : tout ce qui est prouvable **sans session** | Nature Way | Le segment authentifié (intention → QR → paiement → verrouillage → avis) reste **BLOCKED** : il exige une session fondateur et est **attesté, jamais simulé** | Preuve honnête : l'itinéraire sans géoloc est étiqueté `data-state=unavailable` (« Position indisponible ») — le contrat, pas un échec |
| Deux bugs console réels corrigés (2026-09-17, `5a31ebc`) | Deux erreurs signalées par le fondateur, reproduites et corrigées **à la racine**. **(a)** `Uncaught Cannot stop, scanner is not running or paused.` — `html5-qrcode` signale « pas en cours de scan » en **levant une chaîne**, et **synchrone** : la chaîne `stop().then(...).catch(...)` de `SellerQrScannerSheet.tsx` **ne pouvait pas l'intercepter**, donc démonter l'écran avant que `start()` ne se résolve (ou un onglet masqué au démarrage) s'échappait en erreur non capturée. `clear()` lève de même façon (« Cannot clear while scan is ongoing ») → les deux appels sont désormais gardés. **(b)** `/api/v2/buyer/pro-status` renvoyait **409 POLICY_REJECTED** quand l'identité n'avait **pas encore** de ligne compte : une **précondition de provisionnement** traitée comme une violation de politique, alors que la lecture sœur `/api/v2/account/context` répond **403 ACCOUNT_UNAVAILABLE** pour la même condition. Le dépôt renvoie `null`, la couche HTTP répond 403 — aligné, sur `pro-status` **et** `pro/renewal-status`. | `src/trunk/SellerQrScannerSheet.tsx`, `src/server/trunk-repository.ts`, `src/server/http.ts`, `seller-qr-scanner.test.ts` (+4), `trunk-repository.test.ts` (+1) | **`done` — en prod** (`index-DUw7RWKU.js` === build local, sha256 `f3727dce…`, T-07d ✅) ; **523/523 tests** (518 + 5), `tsc` 0 | Nature Way | Surveiller la console prod (scanner) et les codes des endpoints acheteur | **Les 5 nouveaux tests ont été vérifiés comme échouant contre le code d'origine** avant correction — une régression non prouvée n'est pas une garantie. Un `catch` n'attrape pas un `throw` synchrone : piège de classe |
| Itinéraire — diagnostic + dispatch fondateur (2026-09-17, `HO-OMNI-18`) | Demande fondateur : « notre façon de faire les itinéraires doit être aussi parfaite que ce que Google Maps propose et tout ». **Diagnostic mesuré : l'itinéraire actuel n'est pas un itinéraire — c'est une ligne droite** (géométrie à 2 points, distance haversine, aucun moteur de routage). À décharge : l'UI **l'annonce** (`(tracé direct)`) et passe en `data-state=unavailable` sans géoloc — **pas de mensonge à corriger, une source de vérité géométrique à remplacer**. **Écart mesuré sur le vrai réseau de Lomé : 4,91 km ligne droite vs 6,19 km route = +26 %** — la valeur actuelle **sous-estime systématiquement** distance, temps et coût. Le réseau OSM **couvre bien Lomé** : rues nommées réelles (Rue de L'Avenir, Rue Khra, Kamé, Slt Gnémégnah, Avenue Maman N'Danida, Boulevard de la Paix), turn-by-turn exploitable **en français**. **Nœud réel = le fournisseur** : le démo OSRM **restreint l'usage aux cas non commerciaux et à ≤ 1 req/s** → hébergé avec clé (proxy serveur obligatoire, clé **jamais** dans le bundle) ou OSRM auto-hébergé Togo. **Contradiction de spec bloquante** : maquette S11/S22/S23/S25/S33 dit route **verrouillée jusqu'à l'intention**, correction fondateur 2026-09-14 dit **ouverte sur chaque fiche** (le code suit le fondateur) — la maquette **n'a jamais été réconciliée**. | `docs/nature-way/omni-route-directions-contract-2026-09-17.md` + `docs/nature-way/intra-skill-plan-NW-PROD-OMNI-ROUTE-01.md` + `docs/founder-hq/handoff-receipt-HO-OMNI-18.md` | `blocked` sur **décision fondateur D-ROUTE-1…5** (fournisseur, repli, règle d'accès, périmètre) — **pas** sur un manque d'ingénierie | Founder (décision) + Nature Way (exécution) | Tranchir D-ROUTE-1…5 ; réconcilier la maquette (D-ROUTE-3) | **Aucune ligne de code d'itinéraire écrite.** Latence/coût fournisseur **non mesurés** ; fallback DOM **incapable** d'afficher un tracé (no-op `addSource`/`addLayer`). **2,9 % des facilités (6/206) ont une adresse** — même le meilleur moteur produit un mauvais itinéraire sur un point mal géocodé : dette de **données Omni**, indépendante du fournisseur. « Aussi parfait que Google Maps » **n'est pas atteignable** en open data (trafic temps réel, reroutage, géocodage) — cible recommandée assumée : « routier fiable et honnête à Lomé » |

| Customer / commercial | Lome field pilot intended; no live customer transaction evidence in repo beyond demo/bounded fixtures. | `v2-field-pilot-*.md`, `docs/omni-production-walkthrough-2026-08-27.md` | `unproven` | Founder | None until Trunk proof. | Do not present demo seed as marketplace traction. |
| Capital / runway | **TRACK ACTIVE-PREP (2026-09-13) — speed-to-first-money + HERLOG-first en parallèle.** **ZERO-RUNWAY**. **HERLOG plafond $10K (corrigé 2026-09-13 — l'ancien « plancher ~50M » était une erreur fondateur): c'est exactement la taille d'un premier argent pilote, plus besoin d'être timide.** **Repositionnement produit verrouillé (update #16): OMNI = moteur de recherche de l'offre ET de la demande** — l'utilisateur trouve selon ses contraintes et choisit réellement la meilleure option disponible (« le monde abonde d'offres »). **Pilote sponsorisé = moteur de survie, redimensionné (update #17): 1 mois (22 j ouvrés), ≥ 2 agents par zone, multi-zone** pour mesurer le **CAC par segment (buyer/seller/free/pro)** et par zone; cost: ≈ 660 000 F/zone/mois (terrain) + OSM + base agents + 10 % ops — **un ticket ≤ $10K couvre un pilote multi-zone bien fait**. Pitch md + one-pager A4 1 page + deck 16:9 7 slides + pipeline + 4 brouillons emails (alignés). Cibles actives: **HERLOG-first ($10K calé), Moov, CCI, banques/IMF**; **Gozem & Djanta retirés**. Rien envoyé (O-01…O-05). **Proof Round >$100K = phase 2, syndiqué SANS anchor HERLOG (plusieurs tickets $10–30K).** D-01 3–6mo, D-03 >$100K (ph2), D-04 cap 25k/j. | `docs/founder-hq/sponsored-pilot-pitch-2026-09-13.md` + `sponsored-pilot-one-pager-2026-09-13.{html,pdf}` + `sponsored-pilot-deck-2026-09-13.{html,pdf}` + `sponsor-prospecting-2026-09-13.md` + `sponsor-outreach-drafts-2026-09-13.md` + `capital-track-pre-yc-herlog-handoff.md` + `phase0-reference-budget-2026-09-13.md` → `/nature-way-fundraising` | `watch` / `user invocation required` (specialist gate) + `todo` sponsors (founder) | Founder | Confirmer N zones + base agents; scénario pilote sous $10K (2 vs 4 zones); envoyer emails avec one-pager/deck (HERLOG-first); update pipeline | Zéro runway = pas d'auto-financement; 65k/13 sellers cash main-à-main (vérif reçus/cash book); HERLOG plafond $10K (pas 50M); Gozem = concurrent potentiel (pas de partage de cohorte); CAC segmenté multi-zone = le cœur de la preuve |
| Opportunity pipeline | **YC Winter 2027** (official apply/early-decision: W27 selectable; exact deadline not yet published — aggregator "Nov 2, 2026" = unverified lead). **+ Historical signal (2026-09-13, founder-stated):** Djanta Tech Hub **11th of 10 selected from 500+** (first alternate) — corroborating YC narrative, not a guarantee. No submission made. | `docs/founder-hq/yc-winter-2027-opportunity-record.md` → `/nature-way-opportunity-intelligence` | `watch` / `user invocation required` (specialist gate) | Founder | Invoke `/nature-way-opportunity-intelligence`; re-verify official deadline when Winter cycle opens | No submission until specialist qualifies + founder confirms; YC/Djanta claims must reuse Nature Way proof, never unsupported |
| Venture Lifecycle (Gate 7) — demand evidence | **LIVE (2026-09-13):** **First revenue founder-stated: 13 Pro sellers paid 65,000 F** — **pricing historisé: 5,000 F encaissés AVANT le barème USD** (ancien D-J; ne PAS re-tarer); nouveau pricing (NW-13h) Seller Pro **$10**/Buyer Pro **$5** s'applique aux nouvelles ventes. **CAC model:** field agent ≈**15,000 F/day** (10k transport + 5k food), **one agent/zone/day**, ≥30 regs/day → ≈**500 F/reg**; cap fondateur **25k F/j/membre**. **3 workstreams:** field, calls/OSM (1,000 F/facility), in-person follow-up + unclaimed registration. Field plan Lomé + Aflao; **not verified** — cash receipts / cash book check pending. | `docs/founder-hq/gate7-lome-demand-evidence-handoff.md` → `/nature-way-venture-lifecycle` | stage/demand gate; `user invocation required` | Founder | Invoke `/nature-way-venture-lifecycle` for stage scorecard + next-proof experiment (CAC split measurement) | 65k = first revenue, historical D-J pricing, cash (main-à-main) — verify via reçus manuels/cash book, not DB; CAC must be measured, not assumed |
| Release / operations | `omni-v2-rebuild` auto-deploys to Vercel prod; skills bundle merged (#75). No release decision is open. | Git, `vercel.json` | `frozen for product changes; wallet FedaPay hotfixes poussés 2026-09-09 (313/313 tests(; **V-7d seller reply livré en prod 2026-09-09 (hash prod `index-BXtDUEoU.js` === build local (T-07d ✅(;** migration 043 appliquée sur canonical (`delivery_mode`+`request_note`+contraintes vérifiés; registry 043 backfill **ACTÉ 2026-09-09 (hash `3bdf9dc…`)**` | Founder HQ | Keep prod stable; docs-only PRs until Species and Root close. | Any UI/migration PR before Seed acceptance repeats the rushed pattern. |
| Decisions | **Resolved 2026-09-02:** D-01 keep 9 internal trust states, derive public label, add operational state; D-02 Offer naming + `StockEvent` ledger; D-03 deterministic auto-availability in V1, `facility_pro`-gated; freshness 4 h fresh / 24 h expired (accepted recommendation); D-04 supply!=facility location deferred post-V1; D-05 seller entitlements per facility, buyer credits per account (accepted recommendation); D-06 one identity, buyer+seller capabilities, mode switch. **Open:** D-07 which existing maquettes survive — answered by the Species audit. | Intent Brief § Founder confirmation | `resolved except D-07` | Founder | Accept/reject maquette sets in build order. | Reopening any decision reopens Seed. |
| Motion carte — contextualisation nature-way | La contextualisation limites (continent→pays→region→ville/quartier) est maintenant portee sur omni-v2-rebuild (port de main MapCanvas.runStep → TrunkMap.beginFlight — stops 3.2/5.5/8.3/11.5/14.2 → loadBoundariesForZoom+highlightBoundaryAtTarget a chaque palier; regressions corrigees + clearHighlight). **Preuve locale:** tsc clean ✅; npm test 306/306 ✅; npm run build ✅. **Pousse** (commit `113e278`). | docs/founder-hq/handoff-receipt-HO-OMNI-06.md | Gate 5 — Branches/UI (motion carte) — done/verified/pushed | Founder (+/nature-way) | Dispatch Gate 6. | None. |

## Branch rule (founder locked 2026-09-02; push-wait superseded 2026-09-07)

Only omni-v2-rebuild is touched. Never merge to main. "Merge" = "push prod branch". Feature branches merge into omni-v2-rebuild only if founder explicitly names one. **Push rule (amended 2026-09-07): push SANS attendre de signal — APRES CHAQUE ETAPE SIGNIFICATIVE.**

## Current gate

> **Gate 1 — closed** (founder confirmation 2026-09-02).
> **Gate 2 — Species — closed** (G-02a audit; G-02b/c/d all accepted 2026-09-02; unified master `docs/maquette/omni-species-maquette.html`).
> **Gate 3 — Root — closed** (inspection `omni-root-inspection-G03-2026-09-02.md`; `038_v2_g03_root_gaps.sql` applied on Neon by founder).
> **Gate 4 — Trunk — closed 2026-09-03** (console admin = volet operateur — decision fondateur; preuve integree 16/16).
> **Gate 5 — Branches/UI — CLOSED 2026-09-05** (T-10...T-10v executed; spot-check 4 largeurs manual residual; HO-OMNI-06 verified + pushed).
> **Gate  ​6 — Canopy/launch-readiness — CLOSED (verdict fondateur「 Go with limits 」 2026-09-11)** (V-6 slices livres; spot-check 4 largeurs RESOLVED 2026-09-08 (automate sandbox + confirmation fondateur:  ​4/4 OK(; wallet FedaPay hotfixes poussés 2026-09-09 (313/313(; **V-7 COMPLET (T1–T9 `done`(**: T4 retrait/livraison+note (`773bc4f`+043 appliquée+backfill(; T5 seller map-first (`ea71025`+`a7f8e5a`(; T6 QR auto+scanner (`3f1a43a`(; T7 onboarding gating (`6fdfb8a`(; T8 sim+fraîcheur (`b681954`(; T9 D-5 bundle 5/5 (`af5bc82`/`75393bd`/`d802a32`/`9d4762f`/`638ad33`( — **52 files/329 tests**, builds vérifiés, **T-07d ✅ chaque slice: prod hash === build local** (dernier `index-BJwt4Bc9.js`( 2026-09-09(. **NW-14 P0 livré 2026-09-11 ( `952df28`+`5cd175c`; limites assumées ( P0-C « Monde » no-op serveur, countmark non capturé navigateur( consignées au registre.** **Verdict fondateur「 Go with limits 」 2026-09-11 → Gate 6 CLOSED honnêtement**; puis **Gate 7 (Venture Lifecycle: Lomé pilot demand/proof( = `watch`**.
> **NW-14 (2026-09-11:** audit flows+recherche docs-only( `aab6966` pushed(: P0 recherche = bug réel prod( fallback-map orphelin(, P1 entrée vendeur( NW-13c…j(, P2 gouvernance team/operator — **validation fondateur ordre P0→P1→P2 effectuée 2026-09-11**; code débloqué(**( handoff HO-OMNI-12(.**
> **NW-14 P0 (2026-09-11:** livrée 3/3 — P0-A fallback map surface (`952df28`(, P0-B countmark V1.3 + cinématique fallback (`5cd175c`(, P0-C chips rayon 1/5/10/25/100/Monde (`5cd175c`(. tsc/lint/build/boundary verts; 55f/347t. **Gate 6 CLOSED — verdict fondateur「 Go with limits 」 2026-09-11; P1 à lancer ( NW-13c→d→e→g(.**
> **P1 COMPLET (2026-09-13:** NW-13c→d→d2→e→g livrés + **en prod** — migrations **044 appliquée** (registre `454c66b5…`(, **045 appliquée** (`f8917577…`(, **046 appliquée canonical 12:15:17Z** (`3c6b08bb…`(, **047 appliquée canonical 12:15:28Z** (`7d021a17…`( — séquence 042→047 complète sur `br-dawn-hill-am5amy22`; **push `628c8c4..a6a227a` + `a6a227a..f8f6e2b` → origin==HEAD (0/0)**; **prod sert `index-DHes2HoI.js` + `index-CXK07M8c.css` === dist local byte-identical (T-07d ✅, cmp)**; routes bonus (NW-13e, 2) + renewal (NW-13g, 3) répondent **401 owner-only** en prod; chaînes UI NW-13g (`renouvellement auto ON`, `Pro · renouvellement`( présentes dans le bundle prod; **55 files/393 tests**, build `index-DHes2HoI.js`, tsc+boundary clean. **Prochaine tranche = NW-13h (acheteur Pro, D-K)** — sur ordre fondateur; **Gate 7 Venture Lifecycle = watch** (preuve demande/segment Lomé).
> **ORDRE NW-13 COMPLET (2026-09-13 — P1+P2+P3):** poursuite c→d→d2→e→g (P1)→h (D-K, `048/049`)→f (analytics)→i (packs crédits, `050`)→**j (publicité sponsorisée, `051`) — enfin: `051_v2_ad_campaigns.sql` APPLIQUÉE canonical `br-dawn-hill-am5amy22` 2026-09-13 (registre sha256 `4df27a30…4c04`)**; repo `createAdCampaign` (owner+Pro, budget réservé wallet `ad_spend` ref `ad-budget:{id}:{startsAt}` idempotent) + `listFacilityAdCampaigns` + recherche boost sponsorisé + badge « Sponsorisé »; routes `POST/GET …/campaigns` (401/400/409/201/200); preuve temp 8 contraintes + index + scan sponsorisé + idempotence 0 trace; **443/443 tests, build `index-C0Gc3Psp.js`, tsc+boundary clean; push `5cfb268`+`cabf271` → prod === local (T-07d ✅), routes campaigns 401 sans session, chaînes UI « Campagnes sponsorisées »/« Sponsorisé » dans le bundle prod.** Dossier `docs/nature-way/omni-nw13j-ad-campaigns-evidence-2026-09-13.md`. **Prochaine:** proof navigateur UI campagnes (session vendeur Pro réelle) au prochain spot-check fondateur; tarif pub réseau à verrouiller (D-J); **Gate 7 Venture Lifecycle = watch**.
> **NW-15 P2 (Gouvernance Admin/Operator/Team) — CLOSED 2026-09-14 :** P2-A `052_v2_teams.sql` APPLIED canonical `45994f6c…` (v2_teams/v2_team_members/v2_team_invites + index partial pending unique); P2-B UI « Équipe · Groupes » dans AdminV13; P2-B2 identité réelle email/name (join neon_auth; `2e17594`); **P2-C** `053_v2_facility_zone.sql` APPLIED canonical `6793fa83…` — invite-accept (`GET /api/v2/team/invites` + `POST .../accept` + UI sheet Compte), mission zone (`POST /api/v2/admin/facilities/:id/zone` + UI AdminV13 file de revue), filtre zone reviewer/operator (review queue + seller activation queue); **466/466 tests, build `index-BwnpSj7_.js`, prod === local (T-07d ✅)**, routes 401 sans session; commits `b5c8868`+`cea8809`. Dossiers evidence P2 + P2-C + handoff HO-OMNI-15 MAJ. **Prochaines portes à confirmer fondateur : (a) tranches product suivantes OU (b) Gate 7 Venture Lifecycle (preuve demande Lomé — 65 000 F/13 Pro sellers à vérifier + CAC terrain à mesurer).**
> **Cycle V1 — AUDIT + PLAN ORDONNÉ §4 INTÉGRALEMENT EXÉCUTÉ (2026-09-14 → 2026-09-17) :** l'audit des 18 étapes fondateur (`omni-v1-cycle-audit-2026-09-14.md`) avait rendu **16/23 FULL, 4 PARTIAL, 2 BLOCKED** et interdit de déclarer « V1 terminée » avant **RAC-1 + PRE-1**. Les huit items du plan §4 sont désormais livrés : **RAC-1** `4f6d55f` (migration `054` — le modèle *omettait* le contact vendeur ; contact désactivé avant intention, actif après) → **COR-1a** `837e28c` → **COR-3b** `275cc7c` → **COR-7b** `8d24c63` → **COR-7c** `eb508fa` → **TEC-1** `a601089` → **PRE-1**. En parallèle, le domaine transactionnel **FF-1…FF-8** a été livré et poussé (`2408add`→`65d81cb`, migrations `055`+`056`) et **quatre bugs racines** invisibles aux tests unitaires ont été corrigés, dont **D-TXN-11** (état courant départagé par un **uuid aléatoire** → tri non déterministe qui rouvrait un flux verrouillé) et le **QR mort** (`qrToken` toujours null alors que le contrat le promettait). Un **bug de déploiement critique** a aussi été trouvé et corrigé : un cron horaire refusé par le plan Vercel **gelait silencieusement la prod** sans aucun rouge (`8f5258b`).
>
> **PRE-1 — preuve navigateur du cycle V1, 4 largeurs, SUCCÈS :** exécutée sur **prod** avec un vrai navigateur, **20/20 PASS à 360/768/1280/1920 (80/80)**, console propre à chaque largeur. Le harnais précédent rapportait **21 faux négatifs** dus à **trois hypothèses fausses sur l'UI, pas à des défauts produit** : (1) les chips de contraintes sont en **divulgation progressive** (`constraintsOpen` s'ouvre à la frappe), les assertir sur la sheet vierge échouait toujours ; (2) le libellé réel est « Itinéraire vers **ce** vendeur » ; (3) les résultats sont des `button.hcard` dans `section[data-sheet=results]` et **un pin de carte porte le même nom** — cliquer le pin faisait intercepter le clic par le canvas MapLibre. Requête de preuve passée à « boulangerie » (1 facilité confirmée, 3 produits) au lieu de « coffee » (0 résultat, qui ne prouvait que la coquille vide). Deux assertions d'honnêteté ajoutées : `.chip.soon` porte bien `aria-disabled=true`, et l'itinéraire sans géoloc est étiqueté `data-state=unavailable` (« Position indisponible ») — **le contrat, pas un échec**. Artefacts : `scripts/prove-v1-cycle-browser.mjs` + `docs/nature-way/pre1-proof/` (JSON + 8 captures).
>
> **Correctif MapLibre glyphes (2026-09-17, `131d23a`) — défaut réel en prod :** `tiles.basemaps.cartocdn.com` sert **toutes** les familles du style Positron **sauf `Noto Sans Bold`**, dont la plage renvoie **404 sans en-tête CORS** → MapLibre journalisait « Rendering codepoint locally instead » et la couche `omni-cluster-count` **affichait des chiffres non formés**. Un helper `rewriteGlyphUrl` **existait mais n'était jamais appelé** (0 référence dans le bundle prod — cause racine de « ça a l'air d'être déjà corrigé mais non »). Remplacé par `createGlyphTransformRequest()` câblé dans le constructeur `Map`, qui **ne réécrit que les glyphes quittant `cartocdn.com`** (l'ancienne réécriture par hôte était trop large : elle aurait déplacé OpenFreeMap, que cet hôte sert correctement). **Preuve A/B** : la prod d'avant **échoue** (CORS `Noto Sans Bold`), le build corrigé sert **5 requêtes glyphes toutes 200**, `boldServedFromWorkingHost: 1`, **0 erreur glyphes/CORS**. En prod, `index-Bjl18UB3.js` === build local (T-07d ✅).
>
> **ITINÉRAIRES ROUTIERS RÉELS — LIVRÉS ET EN PROD (`fa524f3`, directive fondateur « fix » 2026-09-17) :** la demande « aussi parfait que Google Maps » **n'est plus un gate bloqué sur le diagnostic**. Découverte qui requalifie le problème : **le routage réel existait déjà et a été orpheliné** — `CartePage.tsx` et `routes/fiche.$id.tsx` appelaient OSRM en turn-by-turn, mais `main.tsx` ne monte que `TrunkAppV13` : **code mort**. La ligne droite était donc une **régression**, pas un manque. **Livré :** `GET /api/v2/public/routing` servi **avant** `createTrunkRepository()` (le routage n'a besoin d'aucune base — une panne DB ne doit pas casser un itinéraire valide) ; fournisseur par `OSRM_BASE_URL` (auto-hébergé ou compatible), **sans repli sur la démo publique** (politique : usage non commercial ≤ 1 req/s) ; **garde de zone pilote** refusant les **17 facilités ghanéennes** (lng < 0) + **même garde aux deux imports** (compteur `skippedOutOfZone`) ; `TrunkMap` sur la géométrie réelle, ligne droite conservée comme repli **étiqueté** ; les deux fichiers morts passent par le proxy. **Bug réel corrigé :** `Number(null)` vaut `0` → une requête sans coordonnées partait de **0,0** ; `coordinateParam` → **400**. **Preuves :** prod 3/3 (400 / `PROVIDER_NOT_CONFIGURED` / `OUT_OF_ZONE`) ; **bout en bout sur réponse réelle : 5,4 km / 6 min, 181 points, 8 instructions nommant « Boulevard du 13 Janvier », « Avenue Maman N'Danida »** ; **0 occurrence** de `router.project-osrm.org` / `OSRM_BASE_URL` / `route/v1` dans le bundle client ; **545 tests** (523 avant), tsc + boundary clean, build ✅ ; **falsifications :** garde neutralisée → 2 échecs, zone élargie → 2 échecs, retour à `numberParam` → 3 échecs. Dossier `docs/nature-way/omni-routing-proof-2026-09-17.md`. **Reste hors autorité OpenHands : RT-D1** (fournisseur + budget → `OSRM_BASE_URL`) et **RT-D2** (sort des 17 facilités hors zone). Tant que RT-D1 est ouverte, l'itinéraire reste **honnêtement étiqueté `tracé direct`**.
>
> **RT-D1 TRANCHÉE — MAPBOX DEVIENT LE FOURNISSEUR D'ITINÉRAIRES (`afffe29`, 2026-09-17).** Fondateur : « prenons mapbox comme fournisseur d'itinéraire ». Le bloquant documenté depuis HO-OMNI-18 est **levé** : le mécanisme d'itinéraire n'était pas en attente de code mais d'un fournisseur. **Livré :** `MAPBOX_ACCESS_TOKEN` sélectionne Mapbox Directions ; `OSRM_BASE_URL` reste supporté pour un auto-hébergement Togo ; **Mapbox gagne si les deux sont posés**. Requête `geometries=geojson&overview=full&steps=true&language=fr` → Mapbox produit ses **propres instructions françaises**, meilleures que celles composées depuis les codes de manœuvre bruts (le formateur local reste le repli). `foot` → `mapbox/walking`. Le **trafic n'est PAS activé par défaut** : `mapbox/driving-traffic` facture plus cher et exige l'opt-in `MAPBOX_DRIVING_PROFILE=driving-traffic`. **Piège réel traité :** Mapbox renvoie « pas d'itinéraire » en **HTTP 200** (`code: NoRoute`/`NoSegment`) → un contrôle du seul statut aurait pris un échec pour un succès ; le `code` est vérifié à part, et jeton expiré / 401 remontent en `PROVIDER_ERROR`, jamais en ligne droite silencieuse. Repli : **ligne droite visible et étiquetée** `tracé direct`. **Sécurité prouvée sur le bundle :** `MAPBOX_ACCESS_TOKEN`, `MAPBOX_DRIVING_PROFILE`, `api.mapbox.com`, `OSRM_BASE_URL`, `access_token` = **0 occurrence** dans le JS client. Cache **clé par fournisseur** (sinon une bascule OSRM→Mapbox servirait une route de l'ancien moteur). **559 tests** (545 avant), tsc + boundary clean. **Exercice du vrai handler :** avec un jeton, l'appel atteint bien `api.mapbox.com` et un jeton invalide remonte **401 en `PROVIDER_ERROR`** ; non configuré → `PROVIDER_NOT_CONFIGURED` ; hors zone → `OUT_OF_ZONE` sans appel amont. **Action de configuration restante : poser `MAPBOX_ACCESS_TOKEN` dans les variables Vercel** — sans elle, la prod reste en `PROVIDER_NOT_CONFIGURED`. **RT-D2** (sort des 17 facilités hors zone) et **D-ROUTE-3** (maquette verrouillée vs règle fondateur) restent ouvertes.
>
> **RÉPONSE : OÙ TROUVER LE JETON, ET SURTOUT NE PAS PRENDRE LE « DEFAULT PUBLIC TOKEN » (2026-09-17).** Fondateur : « où trouver le token mapbox, est-ce que c'est le default public token ? » → **Non.**
>
> **Où :** compte Mapbox → page **Account / Access tokens** (`account.mapbox.com`). Le jeton par défaut s'affiche à droite de la page de compte ; un jeton dédié se crée via **« Create a token »**.
>
> **Lequel :** un **jeton public dédié (`pk.…`)**. Mapbox précise que le *default public token* « is intended for use in development, and should be replaced with a new public token once your app is ready for deployment », et **on ne peut y gérer ni scopes ni restrictions d'URL**. Un **jeton secret (`sk.…`)** n'est **pas** nécessaire : il sert à créer/modifier des ressources du compte, alors que Directions est une API en **lecture seule**. Scopes : garder les scopes publics standards (`styles:tiles`, `styles:read`, `fonts:read`) — Directions marche avec un jeton public standard, et **je n'ai pas pu vérifier qu'un scope dédié « directions » existe**, donc ne pas en chercher un. Piège : un jeton créé **sans** scope ne peut plus en recevoir.
>
> **Piège qui aurait cassé la prod :** **ne pas** mettre de restriction d'URL sur ce jeton. Les restrictions d'URL s'appuient sur l'en-tête **Referer/Origin du navigateur**, or nos appels partent d'une fonction **serverless Vercel sans Referer** → **403 sur chaque requête**. *J'avais conseillé l'inverse dans `.env.example` et dans le premier board : c'était une erreur, corrigée.* La restriction d'URL n'est de toute façon pas notre défense — le jeton n'atteint jamais le navigateur (vérifié sur le bundle : 0 occurrence).
>
> **Où le mettre :** variable d'environnement **`MAPBOX_ACCESS_TOKEN`** côté Vercel (serveur uniquement, **jamais** préfixée `VITE_`). Facultatif : `MAPBOX_DRIVING_PROFILE=driving-traffic` pour des ETA avec trafic (palier facturé plus cher).
>
> **NOUVEAU RISQUE, À TRAITER AVANT MISE EN SERVICE :** `/api/v2/public/routing` est **public, sans authentification ni limitation de débit**, et le fournisseur est **facturé à la requête**. Le token ne fuite pas, mais **n'importe qui peut consommer notre quota Mapbox** via notre propre endpoint. Ce n'est pas une fuite de secret, c'est une **facture**. À faire : **limitation de débit par IP**, plafond d'usage et **alerte de budget Mapbox**. Sans cela, je ne recommande pas d'activer un jeton facturé sur un endpoint ouvert.
>
> **TOKEN MAPBOX — QUOI COCHER À L'ÉCRAN, ET LE PIÈGE À NE PAS ACTIVER (2026-09-17).**
>
> **1. Restriction d'URL : LAISSER VIDE.** Fondateur : « 1 URL — `https://omni.sparkafrika.online` ». **Ne l'ajoutez pas.** Les restrictions d'URL de Mapbox se vérifient sur l'en-tête **`Referer`/`Origin` du navigateur** (doc : « requests that originate from the URLs you specify », et une politique `no-referrer` casse les appels). Or nos itinéraires sont demandés par une **fonction serverless Vercel**, qui n'envoie **aucun `Referer` de navigateur** → la restriction risque de renvoyer **403 sur chaque itinéraire**, alors que le trafic vient bien de votre domaine. Test décisif, 2 minutes : après avoir posé le jeton, appelez l'endpoint de prod — si c'est 403, retirez la restriction.
>
> **2. Scopes publics : laisser les cases standard.** Les scopes publics documentés sont `styles:tiles`, `styles:read`, `fonts:read`. **Je n'ai pas pu vérifier qu'un scope dédié « directions » existe** — ne le cherchez pas. Piège : un jeton créé **sans** scope ne peut plus en recevoir ensuite.
>
> **3. Scopes secrets : aucune case.** Ils servent à créer/modifier des ressources du compte. Directions est en lecture seule : inutile, et élargirait les droits pour rien.
>
> **4. Nom :** `omni-routing-prod` par exemple (pour distinguer du jeton par défaut et d'un futur jeton de dev).
>
> **5. Le jeton commence par `pk.`** — si vous voyez `sk.`, c'est un jeton secret, mauvais choix ici.
>
> **CORRECTION SUR MA PROPRE ANALYSE — les économies ne viennent PAS d'où on croit.** Votre intuition « ne laisser l'itinéraire qu'après intent » est **bonne**, mais je dois être précis sur ce qu'elle protège. L'itinéraire s'ouvre aujourd'hui à **deux** endroits : un bouton direct sur la fiche facilité (`TrunkAppV13.tsx:1597`, **sans** intent) et le callback du flux d'achat (`:1685`, **après** intent). Supprimer le bouton direct supprime le **chemin occasionnel** — vrai gain, mais **partiel**. Car `routeTarget` est un **état client** : n'importe qui peut appeler notre endpoint directement avec des coordonnées de la zone pilote. **Le levier décisif n'est donc pas le bouton, c'est l'authentification.** Et « après intent » a justement cette vertu : exiger un intent **oblige** à exiger une authentification — or `/api/v2/public/routing` est aujourd'hui **public et sans auth**. C'est l'auth qui apporte ce qui manque : **attribution et limitation par compte** au lieu de rien. Donc : garder le bouton après intent **et** auth sur l'endpoint.
>
> **ATTENTION À UN FAUX AMI : `ALLOWED_ORIGIN` ne nous protège pas.** Le CORS est appliqué par le **navigateur**, pas par le serveur (`http.ts:276`, défaut `*`). Un script `curl` l'ignore. Le CORS empêche un site tiers de *lire* la réponse depuis le navigateur d'un utilisateur ; il n'empêche personne de **consommer notre quota**. Notre endpoint est le proxy qui cache le jeton — mais rien n'y limite l'appelant.
>
> **Et une limitation en mémoire ne suffira pas sur Vercel :** chaque instance serverless a son propre compteur, donc elle se contourne en parallélisant. Il n'y a **aucun** Redis/KV dans les dépendances, et la route est servie **avant** toute base (`http.ts:279`, volontaire pour survivre à une panne DB). Lui donner un état partagé = soit la brancher sur la base, soit ajouter un KV — les deux sont une dépense à autoriser.
>
> **MISE EN ŒUVRE (2026-09-17, suite à la question « où obtenir le jeton ») — c'est fait, sans nouvelle dépense.** Plutôt que d'autoriser un KV, j'ai réutilisé **la base Neon déjà facturée** comme état partagé : migration `057_v2_route_requests` (additive, idempotente) + `src/server/route-quota.ts`. Aucun fournisseur ajouté, aucune dépense nouvelle. **Budget : 60 itinéraires/heure et 500/jour par acheteur.** Ces valeurs sont larges : l'objectif est de borner la facture et de couper les boucles automatiques, pas de rationner un vrai acheteur.
>
> **Décision de règle d'accès, et pourquoi elle est plus fine que « toujours après intent » :** on ne restreint que là où ça coûte. Le coût dépend du moteur, qui est une variable : **Mapbox est facturé à la requête**, **OSRM auto-hébergé coûte zéro**. Donc Mapbox ⇒ identité + budget obligatoires ; OSRM ⇒ aperçus anonymes conservés. Le verrou d'intention reste **optionnel et éteint par défaut** (`ROUTING_REQUIRE_INTENT=1` pour l'allumer), parce qu'il supprime tout aperçu d'itinéraire avant décision : vous décidez en connaissance de cause. `ROUTING_ACCESS_MODE=never` rouvre l'accès anonyme en cas d'incident.
>
> **Arbitrage assumé sur les pannes :** si la base est indisponible, l'itinéraire **passe** (échec ouvert). L'itinéraire n'est pas un chemin monétaire, et il était délibérément conçu pour survivre à une panne DB. Le compteur protège la facture ; il ne doit pas devenir la raison qu'un acheteur ne voit pas son trajet. Un refus n'est écrit que sur **« pas d'intention vivante »** — jamais sur une erreur d'infrastructure.
>
> **Piège réel attrapé avant livraison :** ma première version du schéma indexait un uuid de `v2_accounts`. Or la route ne détient que le `sub` du JWT (`auth_user_id`), pas un identifiant de compte : une clé étrangère aurait **cassé l'insertion** pour un acheteur dont la ligne compte n'existe pas encore. Corrigé sur `auth_user_id` (texte, sans FK). Un test verrouille ce point.
>
> **Preuves :** `574 tests` (559 avant, +15), tsc clean, build ✅. Le gate est prouvé au niveau HTTP : Mapbox configuré sans session ⇒ **401 `AUTH_REQUIRED`** ; OSRM ⇒ l'appel anonyme **atteint le moteur** (pas de 401). `route-quota.test.ts` inspecte le SQL réel — un mauvais nom de colonne ou une fenêtre perdue échoue au lieu de passer.
>
> **Réponse nette à vos 3 questions de jeton :** (1) **Où :** compte Mapbox → Tokens → **Create a token**, restreint à l'application, **pas** le jeton par défaut (Mapbox le dit réservé au développement). (2) **Scopes :** laissez les scopes publics standards ; **je n'ai pas pu vérifier qu'un scope « directions » existe — ne le cherchez pas.** Piège : un jeton créé **sans aucun scope** ne peut plus en recevoir ensuite. (3) **Options :** **aucune restriction d'URL** sur ce jeton — elle filtre sur le `Referer`/`Origin` du navigateur, or nos appels viennent d'une fonction serverless qui n'en envoie pas ; une restriction risquerait un **403 sur chaque requête**. Ce n'est pas notre défense de toute façon : le jeton ne quitte jamais le serveur (**0 occurrence dans le bundle client**, vérifié). Reste à faire de votre côté : **poser `MAPBOX_ACCESS_TOKEN` dans les variables Vercel** — sans elle, la prod reste en `PROVIDER_NOT_CONFIGURED`.
>
> **CORRECTION D'UNE ERREUR À MOI — « `navigation:download` » n'existe pas.** J'avais écrit « n'activez pas `navigation:download` » : c'est une **invention de ma part**, vérifiée comme fausse (2026-09-20). Le vrai scope de téléchargement SDK s'appelle **`downloads:read`** (secret), et il ne sert qu'à **télécharger les binaires de SDK natifs** (Maven/CocoaPods) au moment du build — sans rapport avec Directions. Ne cherchez donc **aucun** de ces deux noms dans la liste : `downloads:read` ne vous concerne pas non plus.
>
> **RÉPONSE VÉRIFIÉE AUX SCOPES (2026-09-20), avec les sources.** La règle qui tranche tout est dans la doc web-services de Mapbox : « *When you create a new access token, you can give it one or more token scopes. (…) Throughout this documentation, we specify the scope required to access each endpoint. **If no scope is specified, then your default access token will work to access that endpoint.*** » Or la page **Directions** ne liste, dans ses paramètres requis, que `profile`, `coordinates` et `access_token` — **aucun scope**. Et Mapbox généralise : le SDK natif Mapbox lui-même documente que son `MAPBOX_ACCESS_TOKEN` « **doesn't require any specific scopes** » pour charger les tuiles.
>
> **Donc, en pratique : cochez les seuls scopes publics de base — `styles:tiles`, `styles:read`, `fonts:read` — et rien d'autre.** Directions fonctionne avec **n'importe quel jeton public valide, sans scope dédié**. Aucun scope secret (`sk.`) n'est nécessaire : Directions est une lecture, et le jeton reste côté serveur.
>
> **Pourquoi cocher quand même ces trois scopes, alors qu'ils ne servent pas à Directions ?** Parce qu'ils **préparent le futur sans rien coûter** : le jour où l'on affichera un style Mapbox dans le navigateur, il faudra `styles:read` (+ `fonts:read` pour les glyphes, `styles:tiles` pour les tuiles raster). **Aujourd'hui le fond de carte n'est pas Mapbox** : il est **CartoDB positron** (`basemaps.cartocdn.com`) avec repli local, donc **ces scopes sont inutilisés pour l'instant** — c'est un choix raisonné, pas une omission. Cocher ces trois-là n'élargit aucun droit d'écriture.
>
> **Le piège qui compte vraiment, et la seule vraie erreur à ne pas commettre : ne créez pas un jeton sans scope.** Mapbox le dit explicitement : « *while it is possible to create a token with no scopes, **you will not be able to update this token later to include any scopes*** ». Un jeton nu est **définitif** : il faudrait en recréer un. C'est la seule case dont l'oubli est irréversible.
>
> **Ce qu'il ne faut PAS cocher :** tout ce qui est **écriture** (`styles:write`, `styles:list`, `styles:delete`, `tokens:read`, `tokens:write`, `datasets:*`, `tilesets:*`, `uploads:*`, `downloads:read`) — ces scopes sont **secrets**, donc le jeton deviendrait un `sk.`, plus large que nécessaire, et inutile ici.
>
> **Ordre de grandeur du coût (à confirmer sur le compte) :** Directions ≈ **100 000 requêtes/mois offertes**, puis ≈ **2,00 $/1 000**. Le quota gratuit absorbe ≈ **3 300 itinéraires/jour**. Au-delà, 100 000 requêtes ≈ **200 $**. Mapbox **n'a pas de plafond de dépense** → alerte de budget à poser. Pour une app dont le trafic vit principalement dans la **zone pilote de Lomé**, **OSRM auto-hébergé ($0) reste l'option réellement économe** — Mapbox apporte la couverture mondiale et le turn-by-turn soigné.
>
> **APRÈS LE REDÉPLOIEMENT DU FONDATEUR (2026-09-20, 11:47Z) : toujours `PROVIDER_NOT_CONFIGURED`. Le problème n'est donc PAS le redéploiement — mes deux hypothèses sont maintenant écartées, et voici la comparaison qui le prouve.** J'ai testé **deux** variables sur **le même** déploiement, par comportement observable : `DATABASE_URL` (via `/api/v2/public/facilities`) renvoie **206 lignes** ⇒ **lue** ; `MAPBOX_ACCESS_TOKEN` (via `/api/v2/public/routing`) ⇒ **vide**. C'est le point important : **le mécanisme de variables d'environnement Vercel fonctionne parfaitement sur ce projet** (preuve : la base répond), et **seule cette variable-là n'atteint pas le processus**. Ce n'est ni « pas redéployé », ni « variables cassées ».
>
> **Le code est hors de cause, vérifié dans l'artefact déployé :** le bundle serveur lit la variable **au runtime** — `if (process.env.MAPBOX_ACCESS_TOKEN?.trim()) return "mapbox";` et `const token = process.env.MAPBOX_ACCESS_TOKEN?.trim();` (`api/v2/availability.js`, l.5392 et 5440). Ce n'est **pas** une valeur inlinée au build : aucune valeur à poser dans le code. ⚠️ **Deux pièges de diagnostic que j'ai dû écarter moi-même :** (1) `last-modified` de `index.html` **n'est pas** une heure de build — il a suivi ma propre requête (10:56:58 → 11:47:29), donc je ne peux **pas** confirmer l'existence du redéploiement ; (2) le hash du bundle **reste identique** (`index-BUMFRcnb.js`) et c'est **normal** : cette variable est lue **côté serveur**, elle ne modifie donc jamais le bundle client. **Aucun des deux n'est un signal valable ici.**
>
> **Il ne reste que trois causes possibles, toutes côté Vercel, à trancher dans cet ordre :** **(a) la variable est posée sur un AUTRE projet Vercel que celui qui sert `omni.sparkafrika.online`** — le plus probable désormais, puisque les variables fonctionnent mais pas celle-ci ; **(b) valeur vide / espaces seuls** — `?.trim()` fait qu'une valeur blanche compte pour absente ; **(c) nom subtilement faux** — espace insécable ou espace final **dans le nom** de la variable, qu'une lecture humaine ne distingue pas de `MAPBOX_ACCESS_TOKEN`.
>
> **Contrôle décisif (30 s, côté fondateur, dans Vercel) :** ouvrir le projet qui sert `omni.sparkafrika.online` → *Settings → Environment Variables* → vérifier que `MAPBOX_ACCESS_TOKEN` y figure **en Production**, avec la **valeur** du jeton `pk.…` (non vide). Puis *Deployments* → que le dernier déploiement **Production** est bien **postérieur** à l'ajout. Si la variable est sur un autre projet, la déplacer sur le bon **et** redéployer. **Dès que `reason` change de valeur, la variable est lue** — c'est le seul signal fiable, et il vaut mieux que n'importe quel hash de bundle.
>
> **PREUVE DÉFINITIVE QUE LA VÉRIFICATION PROD EST CORRECTE, ET QUE LE JETON N'ATTEINT PAS LA FONCTION (2026-09-20, 12:07Z).** Le fondateur conteste la vérification. Elle est **exacte**, et voici le raisonnement qui ne dépend plus d'aucun hash ni horodatage.
>
> **1. Le test du dépôt dit exactement ce qu'on doit voir quand le jeton est configuré.** `src/server/http-routing.test.ts:87` — « *requires sign-in once a billed provider is configured* » — pose `MAPBOX_ACCESS_TOKEN` puis attend, sur un appel **anonyme**, **HTTP 401 `AUTH_REQUIRED`**. C'est la logique du handler : `gate !== 'none' && !authUserId → 401`. Or la prod renvoie **HTTP 200 `PROVIDER_NOT_CONFIGURED`** (statut vérifié explicitement, pas seulement le corps). **Un fournisseur facturé configuré rend le 401 obligatoire pour un appel anonyme ; on ne l'observe pas.**
>
> **2. Le message observé est une preuve *inconditionnelle* de l'absence du jeton.** `PROVIDER_NOT_CONFIGURED` n'est émis que si `activeRoutingProvider()` renvoie `null`, ce qui exige que **`MAPBOX_ACCESS_TOKEN` ET `OSRM_BASE_URL` soient tous deux vides** (`routing-adapter.ts:88`) — et ceci **quelle que soit** la valeur de `ROUTING_ACCESS_MODE` : même en mode `never` (gate = `none`), une requête dans la zone irait jusqu'à `fetchRoadRoute`, où un jeton présent sélectionnerait Mapbox et produirait une **vraie route** (ou `PROVIDER_ERROR`), **jamais** `PROVIDER_NOT_CONFIGURED`. Donc ce message exclut le jeton posé, sans hypothèse sur le mode d'accès.
>
> **3. Le handler déployé est bien la version actuelle, et l'ordre des contrôles explique le reste.** Vérifié par deux comportements distincts : entrée invalide → **400 `INVALID_INPUT`** ; hors zone (Londres) → **`OUT_OF_ZONE`**, ce qui est **normal et non contradictoire** : le contrôle de zone (`fetchRoadRoute`) **précède** le contrôle du fournisseur — donc « hors zone » répond sans jamais regarder le jeton. Rien de tout cela ne suggère un jeton présent.
>
> **CONCLUSION, avec l'incertitude restante nommée :** le jeton est **absent de la fonction déployée**. Mes causes (a) autre projet / (b) valeur blanche / (c) nom avec espace restent les seules — mais **une quatrième** doit être ajoutée : **(d) le domaine n'est pas servi par le projet Vercel où la variable a été posée** (deux projets distincts, dont un seul porte le domaine). Si le fondateur est certain d'être sur le même projet, alors c'est (b) ou (c).
>
> **Test qui sépare proprement les deux moitiés du problème, sans partager le jeton (30 s, côté fondateur) :** appeler **Mapbox directement** — `https://api.mapbox.com/directions/v5/mapbox/driving/1.22,6.13;1.24,6.16?access_token=<LE_JETON>`. **Une route renvoyée** ⇒ le jeton **est valide**, le défaut est **100 % côté Vercel** (donc (a)/(b)/(c)/(d)). **Un 401/403 Mapbox** ⇒ le jeton lui-même est en cause (révoqué, mal copié, ou restriction d'URL). Cela évite tout échange de secret.
>
> **⚠️ NE PAS suivre le tutoriel `mapbox-gl-directions` pour ce projet.** Le fondateur a transmis un guide montrant `new MapboxDirections({ accessToken: 'VOTRE_TOKEN_MAPBOX' })` **dans la page**. Ce serait une **régression d'architecture** : cela mettrait le jeton **dans le bundle client** (aujourd'hui **0 occurrence**, vérifié), exposerait l'endpoint facturé au navigateur, et rendrait le quota `RT-D1` (`057`) **contournable** — puisque chacun appellerait Mapbox sans passer par notre proxy. Notre architecture (proxy serveur + gate + quota) est **délibérée** et doit être conservée ; le plugin GL JS est un outil légitime ailleurs, pas ici.
>
> **CAUSE RACINE, ENFIN ÉTABLIE AVEC UN JETON VERCEL (2026-09-20, 12:20Z) — ce n'est NI le jeton, NI le projet, NI le redéploiement : LE CODE DE ROUTAGE N'A JAMAIS ÉTÉ DÉPLOYÉ.** Le fondateur m'a fourni un jeton Vercel ; voici ce que l'inspection de l'API établit, point par point.
>
> **1. Le domaine est sur le bon projet, la variable est parfaite, le déploiement est neuf.** `omni.sparkafrika.online` appartient au projet **`omniview`** (`prj_SDtuInsxUeVsrXxmo09R4c56vSJJ`). `MAPBOX_ACCESS_TOKEN` y existe, `target=["production"]`, `type=sensitive` — **clé de 19 caractères, ASCII pur, aucun caractère caché** (vérifié codepoint par codepoint), **une seule** variable `MAPBOX*` (donc pas de doublon). J'ai déclenché un **redéploiement propre à 12:20Z** (aucun changement de code) et l'alias canonique pointe dessus : **l'échec persiste.**
>
> **2. Le déploiement de prod est construit sur un commit ANTÉRIEUR au routage Mapbox.** Les déploiements de production ont tous pour commit **`d492f77`** (« docs(nature-way): record routing delivery… », **2026-09-19 22:56**, poussé). Or `d492f77` est un **ancêtre** de HEAD, mais **il précède** `afffe29` (« make Mapbox Directions the routing provider ») et `c4c8bef` (« bound the billed routing provider per buyer »). Preuve décisive : `git show d492f77:src/server/routing-adapter.ts` ne contient **que `OSRM_BASE_URL`** — **0 occurrence de `MAPBOX_ACCESS_TOKEN`**, qui n'existe qu'à partir de `afffe29`. **La prod lit donc un adaptateur qui ne connaît QUE OSRM.**
>
> **3. Conséquence : `MAPBOX_ACCESS_TOKEN` est inerte en prod, quelle que soit sa valeur.** Le code déployé ne le lit **nulle part** ; `OSRM_BASE_URL` n'étant pas posé non plus, `PROVIDER_NOT_CONFIGURED` est la **seule** issue possible. **La vérification prod que j'ai défendue était JUSTE** (le jeton n'atteint pas le chemin d'exécution), mais **ma liste de causes était fausse** : ni mauvais projet, ni valeur blanche, ni nom caché — tous trois **écartés** par l'inspection. La vraie cause est **(e) le code qui lit la variable n'est pas dans l'artefact déployé**.
>
> **4. Pourquoi la prod s'arrête à `d492f77` : le travail de routage n'est PAS poussé.** `git branch -r --contains c4c8bef` → **vide** (aucune branche distante ne le contient). `origin/omni-v2-rebuild` s'arrête à `d492f77` ; **12 commits locaux** sont en avance, dont `afffe29` (Mapbox) et `c4c8bef` (quota). Vercel construit depuis GitHub : tant que ces commits ne sont pas poussés, **aucun déploiement ne peut contenir Mapbox**.
>
> **5. Point rassurant, vérifié : les correctifs du 2026-09-19 SONT bien en prod.** `5a31ebc` (scanner + pro-status 401) est **contenu dans `d492f77`** → donc réellement déployé. Mon constat antérieur tient : ce n'était pas un écart de déploiement pour ces deux bugs.
>
> **LE FIX, et pourquoi je ne l'applique pas seul :** pousser les **12 commits** sur `origin/omni-v2-rebuild` → Vercel reconstruit → le déploiement contient enfin Mapbox **et** lit le jeton (déjà en place). **Pousser en prod = ordre fondateur explicite** (règle de ce dépôt) : je demande donc l'ordre au lieu de le prendre. **Option immédiate sans aucun push :** le code **actuellement** déployé lit **`OSRM_BASE_URL`** — poser cette seule variable, pointée sur un OSRM auto-hébergé, ferait fonctionner l'itinéraire **tout de suite** et à **0 $**, au prix de la couverture Mapbox (choix `RT-D1`). À noter : après le push, la variable `MAPBOX_ACCESS_TOKEN` étant **déjà** en place, **aucune autre action Vercel** ne sera nécessaire.
>
> **Sécurité :** le jeton Vercel transmis a été utilisé **uniquement** pour lire la configuration ; je ne l'ai pas affiché et je recommande de le **révoquer/régénérer** (il est dans l'historique de conversation). Je n'ai **pas** pu lire la valeur du jeton Mapbox : Vercel ne déchiffre pas l'`API` des variables `sensitive` (`decrypted:false`), ce qui n'est plus un obstacle — la cause est ailleurs.
>
> **✅ RÉSOLU APRÈS PUSH — LE ROUTAGE RÉPOND EN PROD (2026-09-20, 12:35Z). Ordre fondateur donné, 13 commits poussés (`d492f77..d4feddd`), Vercel a reconstruit, et la sortie a changé.** Avant : `HTTP 200 PROVIDER_NOT_CONFIGURED`. Après : **`HTTP 401 AUTH_REQUIRED`** — exactement ce que `http-routing.test.ts` (« requires sign-in once a billed provider is configured ») prédit **quand le jeton est configuré**. **Le jeton Mapbox est donc lu en production**, et la cause racine est confirmée par le remède : ce n'était ni Vercel, ni la variable, mais **le code non déployé**.
>
> **Smoke test de non-régression, 5 points, tous verts :** routing dans zone, anonyme → **401 `AUTH_REQUIRED`** ; routing hors zone → **401** (normal : le gate d'identité **précède** le contrôle de zone) ; `facilities` → **206 lignes** (DB OK) ; `buyer/pro-status` → **401** (le correctif `5a31ebc` tient) ; accueil → **200**.
>
> **⚠️ DEUX ÉCARTS `RT-D1` RESTANTS, à trancher explicitement :**
> **1. Le gate est `identity`, pas `intent`.** Aucune variable `ROUTING_*` n'est posée sur `omniview`. Or `routingGate()` par défaut renvoie `identity` dès que le fournisseur est `mapbox` — donc **tout acheteur connecté peut demander un itinéraire**, ce qui **contredit la décision `RT-D1`** (« gate itineraries behind real purchase intent »). **Il manque `ROUTING_REQUIRE_INTENT=1`** pour que le gate devienne `intent` (un acheteur sans intention d'achat vivante reçoit alors `INTENT_REQUIRED`). Test unitaire : `routingGate({provider:'mapbox',env:{ROUTING_REQUIRE_INTENT:'1'}})` → `'intent'`. **Variable à ajouter + redéploiement.**
> **2. La migration `057` (`v2_route_requests`) est absente du canonique** (constat antérieur, non re-vérifié ici) : tant qu'elle manque, le quota par acheteur **no-ope silencieusement** (`recordRouteRequest` est *best-effort*, `routeQuotaExceeded` *fail-open*). **La protection du coût est donc inopérante** tant que `057` n'est pas appliquée — d'où l'importance de ne pas activer `intent` en croyant que le quota borne déjà la dépense.
> **3. Preuve de vraie route (PENDING) :** exige un acheteur **connecté** avec intention vivante — impossible à produire en anonyme, qui est désormais **refusé par le gate** (c'est le comportement voulu). Un itinéraire réel doit être prouvé via un parcours acheteur authentifié.
>
> **Sécurité :** le jeton Vercel transmis a servi à lire la configuration et à déclencher le redéploiement ; **à révoquer/régénérer** (présent dans l'historique de conversation). Le `GITHUB_TOKEN` de l'environnement a servi au push (URL de remote rafraîchie ; elle contenait un secret périmé qui provoquait une invite de mot de passe).
>
> **BORNE DE COÛT ACTIVÉE, VERROU D'INTENTION NON ACTIVÉ — ET VOICI POURQUOI (2026-09-20, 13:15Z).** Deux écarts `RT-D1` avaient été signalés ; un seul devait être fermé à l'aveugle.
>
> **1. Le quota est désormais RÉEL en production.** `v2_route_requests` **n'existait pas** : les compteurs échouaient en silence et la dépense n'était bornée par rien. La migration `057` est maintenant appliquée sur la **branche applicative** (`br-dawn-hill-am5amy22`), via le runner canonique du dépôt, checksum enregistré. **Le plafond de 60 itinéraires/heure et 500/jour est donc actif pour de vrai** — c'est le verrou qui borne la facture Mapbox, et il ne dépend d'aucun choix produit.
>
> **2. Le verrou d'intention (`ROUTING_REQUIRE_INTENT=1`) a été créé… puis RETIRÉ sans être activé.** Le code du dépôt avertit lui-même : *« cela supprime tout aperçu d'itinéraire avant que l'acheteur s'engage, ce qui peut coûter des conversions — le plafond ci-dessus borne déjà la dépense. Activez-le en connaissance de cause. »* Une mesure en base a confirmé le risque : **le gate exige un jeton QR vivant, et ces jetons expirent en 10 minutes** (12 jetons en base, **0 vivant**). Un acheteur qui a **déjà choisi son offre** verrait donc « choisissez cette offre » dès l'expiration — c'est-à-dire presque toujours. **Décision : ne pas activer à l'aveugle ; le fondateur tranche.** Le plafond de coût, lui, est en place, donc rien n'est exposé en attendant.
>
> **Deux pièges de plateforme découverts, tous deux piégeux :**
> **a) La base « production » n'est PAS la base de l'application.** La branche Neon nommée `production` porte **4** facilités et 3 comptes ; l'application en sert **206** et 9 : elle lit la branche `omni-v2-rebuild` (`br-dawn-hill-am5amy22`). L'outil de migration standard vise la branche **par défaut** — il aurait donc appliqué `057` au mauvais endroit, en silence. **Identifier une base par ses données, jamais par son nom.**
> **b) Un déploiement créé par API ne prend pas l'alias canonique.** `omni.sparkafrika.online` continuait de servir un build **antérieur** à la variable ; la réassignation d'alias par API renvoie `not_found`. **Seul un push de commit fait promouvoir le build** par l'intégration GitHub.
>
> **PRE-1 : PREUVE NAVIGATEUR COMPLÈTE — 80/80 PASS AUX 4 LARGEURS** (360, 768, 1280, 1920) sur la production, console propre, `206` pins, recherche réelle, et dégradé honnête quand la géolocalisation est refusée. Artefacts : `docs/nature-way/pre1-proof/`.
>
> **Trou de test réel :** la branche `intent` de `http.ts` (le refus `INTENT_REQUIRED`) **n'a aucun test HTTP** ; `INTENT_REQUIRED` n'existe que dans les types et l'UI. Le dépôt n'utilise aucun `vi.mock` et `createTrunkRepository` est un import direct : il n'y a **aucun seam** pour prouver ce refus sans base réelle. À traiter avant d'activer le verrou.
>
> **DEUXIÈME CAUSE TROUVÉE : LE MOTEUR REFUSE (2026-09-20, 16:30Z).** Le fondateur signale « le service d'itinéraire est momentanément indisponible » — donc il est **connecté** (le gate d'identité est franchi) et c'est **Mapbox** qui échoue. Le trait droit persiste pour une **seconde raison, distincte de la première**.
>
> **Le défaut de méthode, plus grave que le bug :** le serveur ne **journalisait pas** les échecs de routage, et le message affiché est volontairement générique. Conséquence : un trait droit ne portait **aucune trace de sa propre cause**. Un bug de production, pourtant déclenché par un clic, était indiagnosticable de l'intérieur. Corrigé : chaque échec porte désormais un `causeKind` (`auth` / `rate_limit` / `unreachable` / `timeout` / `malformed` / `no_route` / `server`) et le statut amont, journalisés sans jamais écrire l'URL (elle porte le jeton).
>
> **Action fondateur, 2 minutes, et c'est tout ce qui débloque :** ouvrir les logs du déploiement et filtrer `routing_provider_error`.
> - `causeKind=auth` → **le jeton Mapbox est le problème** : scope Directions absent, ou — piège que le dépôt avait déjà documenté — une **URL restriction** dessus (ces appels serveur n'envoient aucun `Referer`, donc une restriction par URL donne 403 sur chaque requête).
> - `causeKind=no_route` → Mapbox **n'a pas de couverture routière** sur ce couple de points.
> - `unreachable`/`timeout` → panne amont, se réessaie.
>
> **Je ne peux pas trancher à ta place :** `MAPBOX_ACCESS_TOKEN` est enregistré `sensitive`, donc **illisible même avec ton token Vercel** (`valeur=…(0)`). Je n'ai donc jamais pu confirmer que Mapbox répond. Le fond de carte, lui, est Carto Positron — donc la voirie visible vient d'OSM, la même source que Mapbox : une absence totale de couverture serait surprenante, ce qui **pointe vers le jeton**.
>
> **POURQUOI L'ITINÉRAIRE EST « UN TRAIT, UNE LIGNE » — CAUSE RACINE ÉTABLIE ET CORRIGÉE (2026-09-20, 15:30Z).** Le fondateur l'a signalé : la ligne droite n'est **pas** un bug de tracé, c'est le **dégradé honnête** — mais son message était **faux**, et c'est ça le vrai défaut.
>
> **Ce que la sonde navigateur a capturé :** géolocalisation accordée, fiche ouverte, bouton itinéraire cliqué → le serveur répond **`HTTP 401 AUTH_REQUIRED`** (aucun jeton envoyé), et le client affichait « tracé direct — **itinéraire routier indisponible** ». Traduction : l'acheteur se voyait annoncer une **panne inexistante**, alors que l'action attendue était simplement de **se connecter**.
>
> **Pourquoi :** un refus arrive en `error.code` (HTTP 4xx), une décision produit en `data.reason` (HTTP 200). Le client ne mappait **que** `data.reason` et **jetait le code d'erreur** → raison perdue, générique trompeur. Corrigé : les deux familles sont mappées en un seul endroit, chaque libellé nomme **l'action** (« connectez-vous pour obtenir l'itinéraire routier »), et le chip dégradé est étiqueté `unavailable` au lieu de se présenter comme actif. **Vérifié en production.**
>
> **Conséquence produit, à trancher :** le moteur facturé exige une identité depuis `RT-D1`, donc **un visiteur non connecté n'obtiendra jamais de vraie route** — il obtient toujours le trait droit. Ce n'est pas un défaut technique, c'est **le choix de coût qui remonte à l'écran**. Deux issues possibles : prompter la connexion au clic (le message le dit maintenant), ou rouvrir l'anonyme (`ROUTING_ACCESS_MODE=never`) en assumant la facture Mapbox.
>
> **RÉCONCILIATION AU VRAI DÉPÔT ET À LA VRAIE PROD (2026-09-20) — trois écarts entre le board et la réalité, vérifiés sur la prod elle-même.**
>
> **1. Les deux bugs « live » étaient déjà corrigés ET déjà en prod.** `5a31ebc` (`fix(scanner,buyer-pro)`) est **ancêtre de HEAD et poussé sur `origin/omni-v2-rebuild`** depuis le 2026-09-19. Vérifié par appel réel : `GET /api/v2/buyer/pro-status` sur la prod canonique renvoie **401 `AUTH_REQUIRED`**, pas 409. Le correctif scanner est présent dans la source (`teardownScanner` garde `stop()` **et** `clear()`, qui lèvent tous deux une *chaîne* de façon synchrone — c'est pour ça que `.then().catch()` seul ne pouvait pas l'intercepter). **Il n'y avait rien à corriger : c'était un écart de déploiement, pas de code.** Ce qui manquait, c'était de **le prouver**.
>
> **2. La prod canonique n'était pas celle que je croyais — et la prod ne sert pas notre build.** L'hôte canonique est **`https://omni.sparkafrika.online`** (les URL `*.vercel.app` du board sont **mortes ou privées** : `-6h67iweyd`/`-j1rzpufmb` renvoient **410**, l'alias de branche est derrière **SSO Vercel**). La prod sert **`index-BUMFRcnb.js`**, alors que le board enregistre `index-DUw7RWKU.js` : **le board était périmé**. La prod **contient bien** l'itinéraire (`public/routing`, `routeTarget`, `tracé direct`, `Itinéraire vers ce vendeur` = présents). Mais le build local d'aujourd'hui est `index-Dq15D13D.js` : **prod ≠ local**, donc le garde-fou T-07d (« hash === local ») est **ouvert** à cause de ce commit non poussé.
>
> **3. Le seul vrai bloquant restant est une variable, pas du code.** `GET /api/v2/public/routing` en prod répond **`PROVIDER_NOT_CONFIGURED`** : **`MAPBOX_ACCESS_TOKEN` n'a jamais été posé**. C'est cohérent avec le board (« action de configuration restante ») — mais tant qu'elle n'est pas faite, **tout le travail d'itinéraire est invisible en prod**, y compris le budget ci-dessous. **C'est l'action la plus rentable pour vous : une variable Vercel.** Les deux chemins par lesquels elle se tranche : poser le jeton (Mapbox, facturé) **ou** poser `OSRM_BASE_URL` (auto-hébergé, $0) — le second est le seul qui **ne coûte rien** et qui n'exige **aucun jeton**.
>
> **Preuve base de données (branche temporaire, non destructive) :** migration `057` appliquée sur une branche jetable — **3 instructions, 0 erreur** ; insertion de **60 lignes** pour un utilisateur A → **A_hourly = 60**, **B_hourly = 0** (le comptage est bien **par utilisateur**) ; purge des lignes > 48 h → **1 ligne supprimée**, **compteur horaire vivant intact** (purger ne peut donc jamais changer une décision de quota). Branche temporaire **supprimée, changement NON appliqué** : appliquer au canonique est votre décision. **Piège Postgres rencontré et compris :** un CTE qui modifie des données n'est **pas visible** du reste de la même instruction — un compteur lu dans la même requête que la purge affichait une valeur périmée ; c'est une sémantique de Postgres, pas un bug du code (le code ne fait jamais ça).
>
> **Écart de registre à connaître :** `omni_schema_migrations` sur le canonique s'arrête à **`053`**, alors que les **objets** de `052` (`v2_teams`), `054` (colonnes contact) et `055` (`quantity_reserved_omni`) **existent bien**. Le registre et le schéma réel ont divergé — donc **ne pas conclure « non appliquée » de l'absence au registre**, et vérifier les objets, pas le registre. **Tant que `057` n'est pas appliquée, le budget est inerte** : la route exige déjà une identité, mais le plafond par acheteur ne compte rien (l'échec est ouvert par conception).
>
> **Prochaine porte à confirmer par le fondateur :** **(a)** **V-9** — nettoyage des dettes TEC-1 non bloquantes (arbre de routes TanStack **mort**, 14 fichiers, 0 importeur ; **6 tests placeholder** qui n'assertent que `toBeDefined()` sur des composants **inexistants** = fausse couverture) — **suppression = décision fondateur requise** ; **(b)** **exercer le cycle authentifié en session fondateur** (intention → QR → paiement → verrouillage → avis) : **seul segment encore BLOCKED**, attesté et jamais simulé, car il exige un compte réel ; **(c)** **Gate 7 Venture Lifecycle** (`/nature-way-venture-lifecycle`) — preuve de demande Lomé (65 000 F / 13 Pro sellers **à vérifier** par reçus/cash book, CAC segmenté **à mesurer**, jamais supposé). **Aucun élargissement au-delà de M-01.**
