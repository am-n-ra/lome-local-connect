# Skill Handoff and Activation Receipt

> **Request ID:** `HO-OMNI-13`
> **Founder HQ timestamp:** 2026-09-11 (UTC)
> **Primary authority:** Nature Way
> **Exact invocation:** `/nature-way go with limits /nature-way-founder-hq` → dispatch P1
> **Activation status:** `ACTIVATED` (fondateur a relancé `/nature-way go with limits /nature-way-founder-hq` → **P1 lancé**, ordre NW-13c→13d→13e→13g; « allons y » 2026-09-13 → **NW-13d lancé**; amendement D-G « oui approuve go » 2026-09-13 → **NW-13d-2 lancé**)
> **Outcome:** `DONE — NW-13c (P1-A) facility creation server+UI` + `DONE — NW-13d (P1-B) bulk credits server, 3 Free / ~100 Pro` + `DONE — NW-13d-2 (P1-B2) facturation bulk ceil(N/100) + manuel gratuit` + `DONE — NW-13e (P1-C) bonus confiance D-H (users distincts)` + `DONE — NW-13g (P1-D) renouvellement Pro auto D-I` sur `omni-v2-rebuild`; suite verrouillée; **P1 COMPLET (NW-13c→d→d2→e→g)**; tsc/tests/build/boundary verts; **migrations 044 + 045 APPLIED canonical 2026-09-13 (Neon MCP) + registres + preuves live**; **migrations 046 (12:15:17Z, `3c6b08bb…`) + 047 (12:15:28Z, `7d021a17…`) APPLIED canonical `br-dawn-hill-am5amy22` (ordre fondateur « vas y »)** — séquence 042→047 complète au registre; **push prod exécuté** `628c8c4..a6a227a` + `a6a227a..f8f6e2b` → origin==HEAD; **prod sert `index-DHes2HoI.js` + `index-CXK07M8c.css` === dist local byte-identical (T-07d ✅, cmp)**; routes bonus (2) + renewal (3) répondent 401 owner-only en prod; chaînes UI NW-13g (`renouvellement auto ON`, `Pro · renouvellement`) présentes dans bundle prod; proof navigateur UI visuelle (session seller réelle) = résidu honnête.

## Handoff input

| Field | Value |
|---|---|
| User objective | Fondateur: « /nature-way go with limits /nature-way-founder-hq » → **P1 lancé**; livrer **NW-13c (P1-A): facility creation server+UI** — formulaire minimal, 3 types (`fixe`/`mobile`/`digital`), zone/rayon pour mobile, digital sans point géo, trust `unconfirmed`, puis proof flow; **vendeur universel** ( tout compte authentifié peut créer (. Le problème fixé: un nouveau vendeur légitime ne peut ni voir l'espace seller, ni créer sa facilité, ni claim depuis le bon endroit. |
| Current milestone and gate | M-01 pilot-ready V1. **Gate 6 CLOSED 2026-09-11** (`Go with limits`); Gate 7 Venture Lifecycle = watch. P1 ( NW-13c→d→e→g ( actif. |
| Structural path / venture stage | Canopy/launch-readiness → tranches vendeur P1; branche `omni-v2-rebuild`. |
| Relevant artifacts and proof | NW-13c evidence `docs/nature-way/omni-nw13c-facility-creation-evidence-2026-09-11.md`; migration `db/migrations/044_v2_facility_type_rayon.sql`; code backend `src/server/trunk-repository.ts` + `src/server/http.ts`; client `src/trunk/api.ts` + `types.ts`; UI `src/trunk/SellerV13.tsx`; plan/board `docs/founder-hq/*`; NW-13 `docs/nature-way/omni-nw-13-seller-entry-respec-2026-09-10.md`. |
| Dependencies and constraints | Migration 044 **appliquée sur canonical 2026-09-13 (Neon MCP)** + enregistrée `omni_schema_migrations` + preuve INSERT digital/mobile; sandbox sans DATABASE_URL n'est plus un blocage pour la DB ( le MCP y supplée ( ; proof navigateur réel non faite; push prod = **ordre fondateur explicite** (guardrail T-07d(; branche `omni-v2-rebuild` jamais merger `main`. |
| Secondary route | None. |
| Expected specialist return | NW-13c complet: gate, evidence ( tsc/tests/build/prod hash===local ( , residual gap, owner, next action; registre + plan/board + receipt MAJ; **Gate 6 honnête maintenue** ( rien n'a changé le verdict ( ; Gate 7 watch. |

## Specialist resource receipt

| Resource status | Exact path or explanation |
|---|---|
| Loaded | `.agents/skills/nature-way/SKILL.md` ( via route ( ; `.agents/skills/nature-way-founder-hq/` references + templates (dont `intra-skill-execution-controller.md`, `skill-handoff-receipt.md` → ce receipt( |
| Template instantiated | `.agents/skills/nature-way-founder-hq/templates/skill-handoff-receipt.md` → ce receipt; pas de nouveau plan local requis ( plan NW-PROD-OMNI-01 existant, tranches NW-13c…g ajoutées ( |
| Not loaded / reason | `templates/founder-hq-master-plan.md` — plan HQ existing; skills spécialistes hors route — chargés par `/nature-way` à l'activation. |

## Dispatch result

NW-13c (P1-A) **livré et verrouillé**: suite 55 files / **360 tests** (+13: repo 5, http 6, client 2(, tsc clean, `tsc -b` + vite build verts ( bundle `index-CBKUqllX.js` (, `check:boundary` clean. **Migration 044 APPLIED sur canonical `br-dawn-hill-am5amy22` 2026-09-13 (Neon MCP)** + enregistrée `omni_schema_migrations` (checksum `454c66b5…`) + preuve live (INSERT digital sans point + mobile rayon 25 → acceptés, cleanup). UI P2→ formulaire minimal multi-type → POST → refresh → P3; garde `seller_ready` retirée ( vendeur universel (; slot free provisionné (D-J(; trust `unconfirmed` (D-B(. **Residual gaps:** proof navigateur réel non exécuté ( sandbox (; route HTTP create non exercée end-to-end en prod ( pas de push (; bundle prod non mis à jour ( pas de push, guardrail T-07d(.

NW-13d (P1-B) **livré et verrouillé**: suite 55 files / **367 tests** (+7: repo 3, http 3, api 1(, tsc clean, `tsc -b` + vite build verts ( bundle `index-1lygK87G.js` (, `check:boundary` clean. **Comptage serveur**: `v2_buyer_credit_accounts` ( compteur mensuel: plan free/pro, monthly_quota 3/≈100, period_month, credits_used, extra_credits; UNIQUE buyer ( + `v2_availability_credit_ledger` ( audit: bulk_debit/monthly_grant/pack_credit/reversal (: **migration 045 APPLIED canonical 2026-09-13 (Neon MCP) 12 statements + registre `f8917577…` + preuve live (reset mensuel period_month→2026-09 credits_used→0; pack extra_credits=20 → remaining 23; débit → 22; 0 trace)**. Repo `getOrCreateCreditStanding`/`getBuyerCreditSummary` + `createAvailabilityRequest` débite 1/facilité (`cardinality(facility_scope)`) dans UN guarded statement, idempotence sans re-débit, `InsufficientCreditsError` si épuisé. HTTP `GET /api/v2/buyer/credits` + POST avail → 403 `INSUFFICIENT_CREDITS` + validator pur `validateAvailabilityRequestCreate`. Client `getBuyerCreditSummary` + `AvailabilityResult` étendu. UI BuyerFlowV13: compteur « Crédits bulk — X/quota » + état épuisé « rechargez en packs ». **Residual gaps:** achat packs réel = **NW-13i** ( `extra_credits` capacité prête, pas de vente ( ; plan buyer Pro ≈100 = **NW-13h** ( quota 3 free appliqué (.

NW-13d-2 (P1-B2) **livré et verrouillé** (amendement D-G « oui approuve go » 2026-09-13(: suite 55 files / **375 tests** (+8: repo 5, http 4, api 1, adaptés 4(, tsc clean, `tsc -b` + vite build verts ( bundle `index-5yeSIqD5.js` (, `check:boundary` clean. **Nouveau modèle de facturation**: vérif **manuelle (1 facilité) = 0 crédit** ( `createAvailabilityRequest` sans debit/credit_spend (; **bulk = `ceil(N/100)` SANS plafond** — `createBulkAvailabilityRequest` ( `facilityIds[]` ≥2 distincts, `facility_scope`=array complet, débit `Math.ceil(N/100)` guarded + ledger, paramétrisation `({{ids}})::text[]::uuid[]` sans concaténation, refus `InsufficientCreditsError` **« Missing M » chiffré** (; HTTP validator pur `validateBulkAvailabilityRequestCreate` + route `POST /api/v2/bulk-availability`; client `requestBulkAvailability` + type `BulkAvailabilityResult`; **UI TrunkAppV13**: sheet bulk refondu — encart coût live (N facil = K credits, solde, après-envoi, « il manque M » si insuffisant), filtre rayon 5/10/25/50 km, cases à cocher + tout/aucune, **UN** bulk + poll groupé; BuyerFlowV13 « Vérification manuelle gratuite » sans blocage. **Preuve canonique live Neon MCP T1-T5** (manual 0 / bulk 2→1 / refus 250→3 « Missing 1 » / cleanup 0 trace). **Residual gaps:** proof navigateur réel non exécuté ( sandbox (; route bulk HTTP non exercée end-to-end en prod ( pas de push (; achat packs réel = **NW-13i**, plan buyer Pro ≈100 = **NW-13h**.

NW-13e (P1-C) **livré et verrouillé** (D-H fondateur: bonus 20 USD/10 000 XOF minor débloqué après **3 ventes à users distincts**): suite 55 files / **380 tests** (+5, adaptés 2(, tsc clean, `tsc -b` + vite build verts ( bundle `index-CpV0tuGd.js` (, `check:boundary` clean. **Migration `046_v2_seller_bonus_trust.sql`** ( `v2_seller_unlock_progress` PK (facility_id, buyer_account_id) = « users distincts » + `v2_seller_unlocks` UNIQUE facility/type, `pro_test_credit_20_usd`, locked/eligible/granted, 10000 (: **APPLIED + prouvée sur branche temp Neon `br-odd-credit-amrpqgev` T1-T5** (3 buyers distincts → 3 lignes même rejoué; upsert → eligible; re-upsert → granted préservé; CHECK `unlock_type` rejette `pro_bonus_bogus`; cleanup 0 trace) — **apply canonical = ordre fondateur requis**. Repo: `submitTransactionRating` CTE distincts (`ON CONFLICT DO NOTHING`, `qualifying_sales = least(3, count distinct)`, upsert `unlock_object`), `getFacilityBonusStatus`, `unlockFacilityBonus` (owner + trust + ≥3 + bonus null + wallet `for update`, granted once). HTTP: 2 routes owner (GET/POST `/api/v2/seller/facilities/:id/bonus[/unlock]`). Client/types: `getFacilityBonusStatus`/`unlockFacilityBonus`/`FacilityBonusStatus`. **UI SellerV13**: bandeau « Bonus confiance » (X/3 acheteurs distincts · 20 USD; eligible → « Débloquer »; granted → « ✓ 20 USD crédités »). **Residual gaps:** proof navigateur réel non exécuté ( sandbox (; routes bonus non exercées end-to-end en prod ( pas de push (; `seller_unlocks` legacy supabase non branché.

NW-13g (P1-D) **livré et verrouillé** (D-I fondateur: renouvellement Pro **automatique via wallet** + **opt-out explicite** + **rappel**): suite 55 files / **393 tests** (+8 repo, +1 http, +3 api), tsc clean, `tsc -b` + vite build verts ( bundle `index-DHes2HoI.js` (, `check:boundary` clean. **Migration `047_v2_facility_pro_renewal.sql`** ( `v2_facility_renewal_runs` audit: facility_id FK CASCADE, prior/new/spend entitlement ids, run_at, status CHECK `(succeeded, insufficient_funds, skipped, failed)`, note, created_at, index (facility_id, run_at desc) (: **APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 12:15:28Z (registre `7d021a17…`)**, preuve branche temp Neon T1-T7 (shape 9 colonnes + comment + index; insert FK `succeeded` OK; CHECK `bogus` rejeté; plan véridique `pro_active`→`pro_expired`; opt-in flip persisté; cleanup 0 trace). Repo: `getFacilityRenewalStatus` (plan **véridique** pro_active/pro_expired/free dérivé de l'entitlement + renewalOptIn/daysLeft/proPriceMinor/solde/sufficientFunds) + `setFacilityRenewalOptIn` (refuse sans activation) + `renewFacilityPro` (spend wallet ref `facility-pro-renew:{id}:{period}` + entitlement 30 j + `commercial_plan='pro_active'` + audit; **insufficient_funds sans spend → reste pro_expired**; skipped si pas dû) + `getWalletOverview` enrichi. HTTP: 3 routes owner (GET/`renewal-status`, POST/`renewal-opt-in`, POST/`renew`; 409 WalletPolicyError). Client/types: `getFacilityRenewalStatus`/`setFacilityRenewalOptIn`/`renewFacilityPro` + types + `WalletFacilitySummary` étendu. **UI SellerV13**: carte « Pro · renouvellement » (toggle opt-in, jours restants, rappel solde insuffisant, bouton « Renouveler Pro maintenant » si pro_expired) + wallet sheet. **EN PROD**: push `628c8c4..a6a227a` (NW-13e+13g) exécuté ordre fondateur + apply canonical 046/047 12:15Z + docs `a6a227a..f8f6e2b` → origin==HEAD; **prod sert `index-DHes2HoI.js` + `index-CXK07M8c.css` === dist local byte-identical (T-07d ✅, cmp)**; 3 routes renewal répondent 401 `AUTH_REQUIRED` owner-only (messages distincts); chaînes UI NW-13g (`renouvellement auto ON`, `Pro · renouvellement`, `Renouveler Pro maintenant`) présentes dans bundle prod. **Residual gaps:** proof navigateur UI visuelle (session seller réelle) non exécutée ( sandbox — chaînes UI vérifiées dans bundle prod ( ; job de rappel/renouvellement asynchrone automatique = **hors scope P1** ( watch Gate 7 ( ; barème D-J ( Pro vendeur 5 000 XOF/mois ( à confirmer; `seller_unlocks` legacy supabase non branché.

**Next smallest action:** **P1 COMPLET** ( NW-13c→d→d2→e→g( livré + migrations 042→047 appliquées canonical + push prod exécuté + prod hash === local ( T-07d ✅ (. **Prochaine tranche = NW-13h (acheteur Pro, D-K: favoris persistants + comparateur multi-critères + quotas)** — sur ordre fondateur; Gate 6 reste `CLOSED` ( rien n'a changé le verdict ( ; Gate 7 Venture Lifecycle = watch (preuve demande/segment Lomé).


---

## NW-13h (P1-E) — dispatch update (2026-09-13)

**NW-13h (acheteur Pro, D-K) LIVRÉ et verrouillé**: suite **55 files / 414 tests** (+14 repo, +1 http, +4 api), tsc clean, `tsc -b` + vite build verts (bundle `index-BHo1CImV.js`), `check:boundary` clean. **Décision D-K verrouillée fondateur**: Acheteur Pro maintenant — favoris + comparateur first; **prix 2 500 XOF/mois = 250000 minor**; défauts **(a)** greenlit (favoris persistants table dédiée; activation = wallet véridique kind `buyer_pro_spend` + entitlement compte 30 j `v2_buyer_pro_*`; opt-out/rappel symétriques NW-13g; plan crédit bulk `pro` quota 100).
**Migrations `048_v2_account_favorites.sql` + `049_v2_buyer_pro.sql` APPLIED canonical `br-dawn-hill-am5amy22` 2026-09-13 16:01Z** via `scripts/apply-migration.mjs` (registre `048:1055b32c…` + `049:1819973a…`, re-run `already_applied`; 3 + 8 statements; le script backfille le registre). **Live canonical proof T1-T7**: favori INSERT (seed) → OK; `buyer_pro_spend` 250000 (wallet seed) → OK; credit acct `pro` quota 100 → OK; entitlement 30 j opt-in true ends 2026-10-13 → OK; run `succeeded` FK spend → OK; INSERT kind `buyer_pro_bogus` → **CHECK reject**; cleanup (disable trigger append-only → delete → re-enable `O`) + delete ent/credit/run/fav → **0 trace**. NB: ledger append-only (trigger `v2_wallet_ledger_append_only_guard`) — toute preuve ledger requiert disable→delete→enable.
**Backend**: repo favoris (`listFavorites`/`addFavorite`/`removeFavorite`) + buyer pro (`getBuyerProStatus` plan véridique + `activateBuyerPro` + `setBuyerProRenewalOptIn` + `renewBuyerPro` avec **fix stale accountId fallback**); HTTP 7 routes buyer (favoris CRUD + pro-status/activate/opt-in/renew, 401/400 + 409 POLICY_REJECTED); client 8 fonctions + 6 types. **UI**: étoile favori (facility header) + menu buyer « Favoris » + sheet `favorites` (hcards + retirer); carte Buyer Pro wallet + Plans sheet buyer (2 500 F états actif/expiré/free, toggle opt-in, rappel solde); **quota comparateur** `openCompare` bloque >1 (free)/>5 (pro) + banner `compareBlocked` + bouton Passer Pro. **Prod 401 owner-only / push prod NON exécuté** (ordre fondateur requis, T-07d). **Residual gaps**: proof navigateur UI visuelle non exécutée; recommandations = tranche suivante (fondateur « favorites + comparateur first, recommendations later »); achat packs crédits réel = **NW-13i**; job async rappel/renouvellement hors P1 (watch Gate 7). Dossier: `docs/nature-way/omni-nw13h-buyer-pro-evidence-2026-09-13.md`.
**Next smallest action**: fondateur → **push prod (T-07d)** + proof navigateur; puis **NW-13i (mobile money/packs)** ou **recommandations (NW-13h suite)** sur ordre fondateur. Gate 6 reste `CLOSED` (rien n'a changé le verdict); Gate 7 watch.
