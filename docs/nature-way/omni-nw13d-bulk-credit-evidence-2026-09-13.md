# Production Evidence Register — NW-13d « Crédits bulk serveur » (P1-B)

> **Target maturity:** M-01 pilot-ready V1 (canopy/launch-readiness); P1 ( NW-13c→13d→13e→13g (
> **Decision:** D-G verrouillée ( fondateur, NW-13 spec : 3 crédits/mois Free; Pro ≈100/mois; 1 besoin = 1 bulk; demande à N facilités consomme N; surplus rachetable en packs (. Gate 6 `CLOSED` « Go with limits » (verdict 2026-09-11); **« allons y » 2026-09-13** → NW-13d lancé.
> **As of:** 2026-09-13
> **Release owner:** Nature Way (route `/nature-way`); arbitre fondateur.
> **Scope NW-13d (P1-B):** comptage serveur de crédits bulk par buyer — reset mensuel, débit 1/facilité ciblée, ledger d'audit, erreur `INSUFFICIENT_CREDITS` (403), solde exposé (`GET /api/v2/buyer/credits` + réponse de demande), UI acheteur affiche le solde et bloque si épuisé. Packs: capacité `extra_credits` stockée; achat réel = NW-13i ( hors périmètre, honnête (.
> **Décisions de périmètre (résolution d'ambiguïté):** quota `free`=3 pour tout buyer sans plan pro ( buyer Pro réel = NW-13h (; débit = `cardinality(facility_scope)` (1 aujourd'hui par POST mono-facilité, correct pour le multi futur car `facility_scope` est ARRAY dès NW-13c (; `extra_credits` = capacité packs, paiement Mobile Money hors scope → NW-13i.

| Acceptance / control | Method or scenario | Environment and data basis | Result / evidence link | Evidence class | Residual gap / limit | Owner and review trigger |
|---|---|---|---|---|---|---|
| Migration 045 — tables crédits bulk | `v2_buyer_credit_accounts` (compteur: plan free/pro, monthly_quota, period_month, credits_used, extra_credits; UNIQUE buyer) + `v2_availability_credit_ledger` (audit: kind bulk_debit/grant/pack/reversal, amount signé ≠0, request_id FK SET NULL). Additive, idempotent (IF NOT EXISTS), re-run sûr | `db/migrations/045_v2_buyer_credit_accounts.sql`; canonical `br-dawn-hill-am5amy22` | **APPLIED 2026-09-13 on canonical via Neon MCP** (12 statements, transaction atomique); schéma vérifié via describe_table_schema (colonnes+CHECK+FK+index, size 0 = aucune donnée touchée; **inséré registre `omni_schema_migrations`** filename `db/migrations/045_v2_buyer_credit_accounts.sql`, checksum fichier `f8917577…`, applied_at 2026-09-13T09:32:55Z | `applied on DB` (MCP, vérifié post-apply ( | — | Nature Way / re-vérification à chaque push prod |
| Repo — standing crédit + débit atomique | `getOrCreateCreditStanding` / `getBuyerCreditSummary` (upsert reset mensuel: `period_month<>courant → credits_used=0`); `createAvailabilityRequest` pré-vérifie `remaining >= cost` → `InsufficientCreditsError`, puis débite dans UN guarded statement (CTE `credit_spend` + `credit_ledger_insert`), **idempotence sans re-débit** (replay: request_insert vide → pas de débit) | `src/server/trunk-repository.ts`; tests repo (3 nouveaux + 3 adaptés) | Repo tests verts; assertions sur CTE `credit_spend`/`cardinality(facility_scope)`/`v2_availability_credit_ledger`; retour étendu `creditCost/creditsRemaining/monthlyQuota/plan`; course guard: `is_new && !debited → InsufficientCreditsError` | `reproduced` ( unitaire, code ( | Deux requêtes (standing puis main) = fenêtre de course de concurrence entre deux demandes simultanées du même buyer; garde SQL `>= cost` la neutralise (débit refusé). Granularité acceptable ( idempotency par request ( | Nature Way / charge réelle (monitoring >3/s pour un buyer) |
| HTTP — route + mapping | `GET /api/v2/buyer/credits` (200 summary / 401 sans auth / 404 si aucun compte); `POST /api/v2/availability` → validator pur `validateAvailabilityRequestCreate` + 403 `INSUFFICIENT_CREDITS` via `toApiErrorResponse` | `src/server/http.ts` + `src/server/http.test.ts` (3 nouveaux( | tsc clean; http.test vert (mapping 403, validator normalize, reject) | `reproduced` ( unitaire, code ( | Route non exercée end-to-end en prod ( nécessite Neon + Auth ( | Nature Way / proof API après apply+push |
| Client/types — solde + résultat étendu | `getBuyerCreditSummary` client; `BuyerCreditSummary` type; `AvailabilityResult` étendu (creditCost/creditsRemaining/monthlyQuota/plan( | `src/trunk/api.ts` + `src/trunk/types.ts` + `src/trunk/api.test.ts` (+1( | tsc clean; api.test vert (route + parse summary) | `reproduced` ( unitaire, code ( | — | — |
| UI — compteur crédits + épuisé | BuyerFlowV13: charge `getBuyerCreditSummary` au montage + après envoi; affiche « Crédits bulk — X restant(s) / quota »; si `creditsRemaining <= 0`: bouton disabled + message « rechargez en packs »; erreur 403 INSUFFICIENT affichée | `src/trunk/BuyerFlowV13.tsx`; build vert | tsc clean; build vert; boundary clean | `reproduced` ( code ( ; visuel navigateur non capturé | Aucun navigateur réel ( pas de capture du compteur ( | Nature Way / audit visuel navigateur + fondateur spot-check |
| Preuve locale complète | tsc; suite vitest; `tsc -b` + vite build; boundary | commandes locales | 55 files/**367 tests** (+7: repo 3, http 3, api 1), tsc clean, `tsc -b` + vite build verts ( bundle `index-1lygK87G.js` (, `check:boundary` clean; bundles serverless (12( régénérés ( route availability.js porte 11 hits crédits ( | `reproduced` ( commandes ( | — | — |
| Preuve canonique live (Neon MCP) | Transaction de preuve: standing free/3, débit −1 (ledger bulk_debit), formule remaining; puis reset mensuel (period_month passé → credits_used=0) + pack `extra_credits=20` → remaining 23 → débit → 22; **cleanup 0 trace** | canonical `br-dawn-hill-am5amy22` | 2 transactions proof2; résultats: first standing remaining=2 après 1 débit; reset `period_month→2026-09, credits_used→0, remaining=3`; pack → remaining 23; débit → 22; counts finaux `accounts=0, standings=0, ledger_rows=0` | `applied on DB` ( preuve live, cleanup tracé ( | — | Nature Way / ré-audit avant prod |

## History and rationalité

- **NW-13d tranche P1-B** — porte D-G de la spec NW-13 côté serveur: un buyer confirmé dispose de crédits bulk mensuels (3 free), chaque demande de dispo consomme 1 crédit par facilité ciblée, l'épuisement bloque proprement (403) au lieu d'accepter une demande non finançable, et le solde est visible côté acheteur.
- Suite monte **360 → 367 tests** (+7): repo 3 (InsufficientCredits, standing/reset, summary), http 3 (mapping 403, validator normalize, reject), client 1 (getBuyerCreditSummary). Tests repo availability existants adaptés au stub alterné (2 requêtes: standing puis main).
- **Guardrail T-07d:** non poussé ( pas d'ordre fondateur (; le futur push prod devra vérifier la prod hash (`index-1lygK87G.js`(.

## Exposure plan

Audience: fondateur ( verdict Gate / ordre push ( + pionnier Lomé. Rollout: branch `omni-v2-rebuild` uniquement; pousse prod = ordre fondateur ( guardrail T-07d (. Guardrail: ne jamais merger vers `main`. Rollback: revert commit + rebuild + lever `INSUFFICIENT_CREDITS` ( re-point l'erreur 403. **Migration 045 appliquée sur canonical 2026-09-13 (MCP) + registre complété.** Limites assumées restantes: proof navigateur réel non exécuté ( sandbox sans DB/Auth (; prod hash non mis à jour ( pas de push (; achat packs = NW-13i prévu ( `extra_credits` capacité prête, tunnel d'achat absent (; plan buyer Pro ≈100 = NW-13h ( quota 3 appliqué par défaut (.